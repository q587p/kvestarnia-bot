# 0.2.17 - Big Barrel Brother Raid MVP

## Goal

Ship the first feature-flagged Big Barrel Brother route for level `8+` characters on top of the temporary party foundation and one-boss runtime.

## Naming and allusion

Use `Big Barrel Brother` as the English planning and technical name to preserve the Big Brother surveillance allusion from Orwell's *Nineteen Eighty-Four* as summarized in <https://en.wikipedia.org/wiki/Big_Brother_(Nineteen_Eighty-Four)>. Keep the Ukrainian player-facing boss name `Старший Брат Бочки`, following the established Ukrainian rendering `Старший Брат` summarized in <https://uk.wikipedia.org/wiki/Старший_Брат>. Do not rename Ukrainian player-facing copy away from that form.

## Scope

- Add `BIG_BARREL_BROTHER_RAID_ENABLED`.
- Keep level `1-7` and pre-existing legacy pending Barrel rows on the legacy Barrel route.
- Create level `8+` Big Barrel Brother recruiting parties from `/raid` / the Barrel location only while the flag is enabled.
- Reuse `PartySession`, `PartyParticipant`, `party_boss_sessions`, `party_boss_actions` and `ActiveCombatLease(kind = "party-boss")`.
- Start Big parties with `rulesVersion = big-barrel-brother-v1`, `bossKey = big-barrel-brother`, frozen roster/resources/boss HP and one shared boss target.
- Let each living participant submit one action per turn; duplicate/stale callbacks replay canonical state.
- Timeout resolution fills missing actions with deterministic defend.
- Victory claims the canonical `tavern.friday-barrel-raid` success key for meaningful participants in the frozen Barrel period, with stored XP/gold/resource outcome and the existing Barrel item grants.
- Failure grants no success, gold or items in this slice.
- Terminal resolution releases party-boss leases and live party membership keys.

## Non-goals

- No guilds, permanent parties, matchmaking, public roster, Mini App, market, trade, crafting or auction.
- No broad combat rewrite, targetable adds, combat items, one-use manatky, healing role, resurrection, threat/aggro system or loot-master flow.
- No hidden runtime cap, enrage timer, turn-number terminal pressure or 13-round auto-loss.
- No exact reward/odds/formula preview before commitment.
- No affinity spotlight/trophy subsystem yet; rewards intentionally reuse the existing Barrel grant set.

## Acceptance criteria

- Level 7 legacy Barrel behavior remains unchanged.
- Level 8+ opens Big Barrel Brother recruiting only behind `BIG_BARREL_BROTHER_RAID_ENABLED`.
- Deep links and nearby invites join the exact same party token.
- Starting freezes roster, period, boss scale and participant resources.
- Every living participant can submit exactly one action per turn.
- Timeout fills missing actions as defend.
- Runtime ends only when boss HP reaches `0`, all active participants are unable to continue, or lifecycle cleanup cancels/invalidates the raid.
- A fight still active after the 13-round simulation horizon remains active in runtime.
- Deterministic Big Barrel Brother balance probes by 13-round horizon: solo baseline `0/400` wins (`0%`), prepared 3-player entry party `162/400` wins (`40.5%`), both with `0` unresolved-by-horizon in the checked matrix.
- Victory writes the existing Barrel success gate once per meaningful participant; replay does not duplicate rewards.
- Failure does not write Barrel success.
- Terminal and replay paths release active combat leases/live party keys.
- Local `/dev_raid_reset` clears the current Barrel pending timer/completion gate for same-period QA without granting rewards.

## Relevant files / search terms

- `src/domain/partyBoss/partyBoss.ts`
- `src/services/partyBossService.ts`
- `src/services/partySessionService.ts`
- `src/services/tavernRaidService.ts`
- `src/db/repositories/prismaPartyBossRepository.ts`
- `src/bot/commands/tavernCommand.ts`
- `src/bot/keyboards/partySessionKeyboard.ts`
- `src/bot/presenters/partySessionPresenter.ts`
- `BIG_BARREL_BROTHER_RAID_ENABLED`
- `big-barrel-brother-v1`
- `tavern.friday-barrel-raid`

## Focused tests

- `tests/domain/partyBoss.test.ts`
- `tests/db/prismaPartyBossRepository.integration.test.ts`
- `tests/bot/partySessionKeyboard.test.ts`
- `tests/config/env.test.ts`

## Manual Telegram QA

1. Confirm level 7 still opens legacy Barrel.
2. Confirm level 8+ opens legacy Barrel when the Big Barrel Brother flag is disabled.
3. Enable the flag locally and confirm level 8+ opens Big Barrel Brother recruiting.
4. Join the same Big Barrel Brother party by deep link and nearby invite.
5. Start the raid and verify private cards show only the viewer's HP/mana.
6. Submit and replay actions from multiple participants.
7. Force/allow timeout and verify missing actions defend.
8. Continue past rounds 7 and 13 in a controlled high-HP scenario and verify no automatic loss by round number.
9. Finish a win; replay result/actions and confirm rewards/success do not duplicate.
10. Use `/dev_raid_reset` locally when the same account needs another Barrel/Big Barrel Brother attempt in the same raid period without waiting for the next `:23` period boundary.
11. Finish a loss and confirm no Barrel success/beer gate is written.
12. Remort/invalid participant smoke on disposable characters.

## Release surfaces

- `package.json` / `package-lock.json`
- `CHANGELOG.md`
- `news.md`
- `docs/tasks/README.md`
- `docs/ROADMAP.md`
- `docs/PLAYTESTING.md`
- `docs/SECURITY_AND_FAIR_PLAY.md`
- `docs/ai/context.md`
- `docs/design/BIG_BARREL_BROTHER_BALANCE.md`
