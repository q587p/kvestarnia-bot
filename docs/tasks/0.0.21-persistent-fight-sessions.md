# 0.0.21 — Persistent Fight Sessions

Status: Shipped
Date: 12026-06-15
Source: `CHANGELOG.md`

This is a compact historical task record generated from the shipped changelog entry. It is not an active task unless a human explicitly reopens a follow-up.

## Goal

Ship `Persistent Fight Sessions` as version `0.0.21`.

## Scope summary

- **Added:** Added persistent solo combat sessions for level 3+ `/fight`, backed by a new `solo_combat_sessions` table that stores serializable combat state JSON, monster id, status, and lazy expiry.
- **Added:** Added FightService session flow that starts or resumes exactly one active solo fight per character and resolves attack, class skill, and flee turns through the pure combat domain engine.
- **Added:** Added versioned persistent fight callbacks shaped as `v1:fight:turn:{sessionId}:{turn}:{action}`, with turn validation so stale buttons show current state instead of applying damage twice.
- **Added:** Added Telegram fight screens for persistent combat with hero HP/mana, monster HP, current turn, last-turn summary, terminal states, and class-shaped skill labels.
- **Added:** Added the first tiny persistent fight quest wrapper, `Тринадцять дрібних проблем`, tracking 13 won level 3+ solo fights and granting one fixed completion reward once.
- **Added:** Added `item.badge-of-thirteen-small-problems` as a non-power cosmetic/junk-style proof that the корчмар can count to thirteen under pressure.
- **Added:** Added tests for schema/migration shape, callback parsing, service start/resume/expiry, stale-turn safety, no-mana non-mutation, presenter escaping, command output, keyboard callbacks, and pending-raid presence protection.
- **Changed:** `/fight` keeps the starter Mimic Shawarma probe for levels 1-2, then switches to persistent solo combat from level 3 onward.
- See `CHANGELOG.md` for 3 more scope detail(s).

## Guardrails / non-goals

- Per-fight XP/gold/items for persistent fight victories, loot rolls, equipment stat effects, persistent HP/mana outside the session, group combat, PvP, shops, trading, crafting, Redis/jobs, or bestiary collection progression.

## Verification notes

- Detailed tests and implementation notes live in the original PR/history and relevant test files.
- Use focused tests around changed modules if reopening this area.
- Keep player-facing copy Ukrainian.

## If reopening a follow-up

```text
Use $kvestarnia-version-task.

Create a new task doc for the follow-up instead of editing this shipped record.
Start from this historical record and CHANGELOG.md entry for 0.0.21.
Context: docs/ai/context.md
Final output: changed files, behavior changed, tests run, risks, completion status. No tutorial.
```
