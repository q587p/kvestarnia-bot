# 0.1.3 — Durable Barrel Notifications & Chest Cleanup

Status: Shipped
Date: 12026-06-17
Source: `CHANGELOG.md`

This is a compact historical task record generated from the shipped changelog entry. It is not an active task unless a human explicitly reopens a follow-up.

## Goal

Ship `Durable Barrel Notifications & Chest Cleanup` as version `0.1.3`.

## Scope summary

- **Added:** Added `barrel_raid_notifications` as a durable notification ledger for pending `Бочка Пінного Міражу` raid completion messages.
- **Added:** Added startup resume for pending Barrel completion notifications: future rows are rescheduled and due rows run through the existing idempotent Barrel reward path.
- **Added:** Added processing lease recovery and `reward_claimed_at` tracking so crashed or failed notification delivery can resume after the reward claim without suppressing the completion message.
- **Fixed:** Manual Barrel completion remains the fallback; later durable notifications now skip already-completed raids instead of sending a confusing duplicate completion message.
- **Fixed:** Telegram send failure after a notification-owned reward claim now leaves the row retryable and can resend the existing reward summary instead of turning into a skipped notification.
- **Fixed:** `Дружня Скриня` pending runs older than the cleanup TTL now expire safely and old confirm callbacks ask the player to reopen the chest instead of touching inventory.

## Guardrails / non-goals

- No reward changes, loot changes, combat formula changes, remort rule changes, Support Jar runtime, group raids, shops, trading, crafting, or Phase 2 runtime.

## Verification notes

- Detailed tests and implementation notes live in the original PR/history and relevant test files.
- Use focused tests around changed modules if reopening this area.
- Keep player-facing copy Ukrainian.

## If reopening a follow-up

```text
Use $kvestarnia-version-task.

Create a new task doc for the follow-up instead of editing this shipped record.
Start from this historical record and CHANGELOG.md entry for 0.1.3.
Context: docs/ai/context.md
Final output: changed files, behavior changed, tests run, risks, completion status. No tutorial.
```
