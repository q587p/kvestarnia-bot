# 0.0.3 — Friday Tavern Raid

Status: Shipped
Date: 12026-06-12
Source: `CHANGELOG.md`

This is a compact historical task record generated from the shipped changelog entry. It is not an active task unless a human explicitly reopens a follow-up.

## Goal

Ship `Friday Tavern Raid` as version `0.0.3`.

## Scope summary

- **Added:** `/tavern` and `/raid` now open the first tiny playable event: `П’ятничний рейд на Бочку Пінного Міражу`.
- **Added:** `/quest`, `/hunt`, `/inventory`, and `/guild` now return short Ukrainian placeholder responses instead of staying silent.
- **Added:** The tavern screen shows the current hero, short event scene, and buttons for the barrel raid, hero summary, and help.
- **Added:** `DailyAction` persistence records once-per-day local rewards with a unique character/key/date constraint.
- **Added:** The tavern raid grants `+7 XP` and `+5 gold` once per stored reward date, then returns an already-completed result on repeated taps.
- **Added:** Tests cover tavern callback parsing, presenter output, no-character handling, idempotent service rewards, and Prisma daily-action uniqueness.
- **Changed:** The existing `🍺 До корчми` menu button now opens the real korchma screen instead of a placeholder.
- **Changed:** Help text now lists `/tavern` and `/raid` as available local commands.

## Guardrails / non-goals

- Full combat, inventory, item loot, group raids, guilds, PvP, jobs, payments, and Mini App UI.

## Verification notes

- Detailed tests and implementation notes live in the original PR/history and relevant test files.
- Use focused tests around changed modules if reopening this area.
- Keep player-facing copy Ukrainian.

## If reopening a follow-up

```text
Use $kvestarnia-version-task.

Create a new task doc for the follow-up instead of editing this shipped record.
Start from this historical record and CHANGELOG.md entry for 0.0.3.
Context: docs/ai/context.md
Final output: changed files, behavior changed, tests run, risks, completion status. No tutorial.
```
