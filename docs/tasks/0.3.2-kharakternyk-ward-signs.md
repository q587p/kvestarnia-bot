# 0.3.2 - Kharakternyk Ward Signs

## Goal

Add a narrow Big Barrel Brother lobby support feature for existing `class.kharakternyk`: a level `3+` Kharakternyk can place one ward sign before the raid starts, joined participants can support it, and the sign mitigates the first typed boss special / area attack once during the resulting boss fight.

## Scope

- Existing Big Barrel Brother / party-session recruiting lobby and refresh cards.
- One active ward sign per raid lobby/session.
- Placement by eligible `class.kharakternyk` only, costing exactly `5` mana.
- One support per joined participant before start; the placing Kharakternyk does not also create a support row.
- Other Kharakternyks support for `0` mana and still count as one support.
- Non-Kharakternyk support costs deterministic `1..3` mana, biased cheaper by effective Intelligence.
- Final support count uses only the starting/final raid roster and caps at `7` supporters for an eight-person raid.
- Mitigation formula: `min(95, 25 + 10 * supportCount)`.
- The sign triggers once on the first typed boss special / area attack, affects only current participants hit by that attack, then persists as broken/consumed.
- Short Ukrainian lobby, button, blocker, trigger and journal/card copy.

## Non-goals

- No new race, class, location, universal noncombat engine or deprecated `race.kharakternyk` revival.
- No item crafting, one-use ward charms, shops, gifts, postal transfers, loot, gold, XP, quest progress, public feed row, paid advantage or permanent status buff.
- No mitigation on ordinary attacks.
- No multiple triggers.
- No exact hidden support-cost thresholds or formulas in player-facing pre-commit copy.
- No string-parsing Ukrainian combat log lines to detect boss specials; use typed boss action metadata or a narrow domain result flag.

## Acceptance criteria

- Eligible level `3+` Kharakternyk can place a sign for `5` mana in a valid recruiting Big Barrel lobby.
- Placement, duplicate placement callbacks and stale callbacks are replay-safe and cannot spend mana twice.
- Non-Kharakternyks cannot place signs.
- Joined participants can support exactly once before start; non-joined and stale supporters cannot mutate state.
- Support cost is resolved and stored durably before/inside the mutation boundary; duplicate callbacks replay the same cost/result.
- Insufficient mana support fails closed with Ukrainian blocker copy and no support row.
- Mana spends update mana regeneration anchors consistently with existing class noncombat mana spends.
- Lobby UI shows support count only, not supporter names.
- If the placing Kharakternyk leaves, remorts or is absent from the starting roster, the sign fails closed and does not carry into combat.
- If a supporter leaves before start, their support row may remain for replay/audit but does not increase final effective support count.
- Mitigation is frozen at raid start, applies only once, and cannot apply again on refresh, replay or concurrent resolution.
- Raid card/journal copy clearly shows the sign triggered and how much mitigation was applied.
- Achievement handling is explicit: add a small rewardless first-use/support hook if it fits the current catalog, or document why achievements are out of scope for this slice.
- Lore Board impact is reviewed and either updated or explicitly documented as unchanged.

## Relevant files / search terms

- `src/services/partySessionService.ts`
- `src/services/partyBossService.ts`
- `src/domain/partyBoss/`
- `src/bot/keyboards/partySessionKeyboard.ts`
- `src/bot/presenters/partySessionPresenter.ts`
- `src/bot/callbacks/partySessionCallbackData.ts`
- `src/services/classNoncombatService.ts`
- `src/domain/progression/effectiveStats.ts`
- `src/content/classes.ts`
- Search terms: `Big Barrel`, `party boss`, `party session`, `class.kharakternyk`, `mana`, `effective intelligence`, `boss special`, `area attack`.

## Focused tests

- Domain tests for mitigation formula: `0 => 25%`, `1 => 35%`, `7 => 95%`.
- Callback tests for compact sign placement/support callback data and Telegram length limits.
- Repository/service tests for placement, support, duplicate replay, stale session rejection, roster finalization, mana spend and remort/life mismatch.
- Presenter/keyboard tests for eligible placement, support buttons, count-only display, blocker copy and no hidden formula preview.
- Party-boss integration tests proving the sign triggers only on the first typed special / area attack, never on ordinary attacks, and never twice.
- Regression tests for concurrent trigger/replay safety and broken/consumed state persistence.
- Run focused tests first, then `npm run typecheck` and `npm run check`.

## Manual Telegram QA

1. Create or use a Big Barrel eligible `class.kharakternyk`, start a Big Barrel recruiting lobby and tap `🧿 Поставити знак`.
2. Confirm placement spends `5` mana once and duplicate taps replay safely.
3. Join with one non-Kharakternyk and one Kharakternyk supporter; verify support copy, costs, count-only display and duplicate support behavior.
4. Start the raid and verify the first boss special / area attack triggers the sign, writes the result on the active card and journal, and marks the sign broken.
5. Refresh/reopen/replay the raid result and confirm the mitigation does not apply again.
6. Verify ordinary boss attacks are unchanged.

## Release surfaces

- `CHANGELOG.md`
- `news.md`
- `docs/ai/context.md`
- `docs/operations/playtesting.md`
- `docs/balance/notes.md`
- `docs/architecture/security-and-fair-play.md`
- `docs/tasks/README.md`
- `docs/design/` or another focused design note if the implementation needs more detail.

## Implementation notes

- Shipped without a schema migration: lobby placement/support metadata lives in the existing `PartyParticipant.snapshotJson` and is revalidated against joined/remort-matching participants at Big Barrel Brother start.
- Placement spends exactly `5` mana and duplicate placement/support callbacks replay the stored state without spending again.
- Non-Kharakternyk support cost is deterministic from effective Intelligence: low-Int support spends more mana, Kharakternyk support costs `0`.
- The frozen fight state stores the effective support count and mitigation, then marks the sign broken after the first Big Barrel Brother broad retaliation.
- Achievement hooks are intentionally out of scope for this slice: placement/support are prep metadata with no separate durable event ledger or reward/feed boundary; the decision is documented here and can be revisited if raid-prep achievements get a shared ledger.
- Lore Board was updated for `class.kharakternyk` because the class now has a visible raid-prep trick beyond its combat action.
