# 0.0.24 — Level Cap 13 & Grownup Cellar Quest

Status: Shipped
Date: 12026-06-15
Source: `CHANGELOG.md`

This is a compact historical task record generated from the shipped changelog entry. It is not an active task unless a human explicitly reopens a follow-up.

## Goal

Ship `Level Cap 13 & Grownup Cellar Quest` as version `0.0.24`.

## Scope summary

- **Added:** Added the first level 4+ cellar follow-up, `Справа не до миші`, which replaces the retired mouse errand when an older hero tries the old cellar route.
- **Added:** Added a narrow `CellarGrownupQuestService` and repository path that use existing `daily_actions`, `character_items`, and `character_cooldowns` instead of introducing a broad quest engine.
- **Added:** Added `Сирна пломба Корчмаря`, `Кльовий шмат сиру`, and `Пляшка Пінного Міражу` content items as the first post-mouse cellar objects.
- **Added:** Added idempotent paths for buying the seal, trying a roleplay bypass, receiving the bottle once, and choosing the final ending.
- **Added:** Added the runtime MVP for `Дружня Скриня`: inventory entry point, auto-pick of 5 cheapest eligible манатки, confirmation, transactional recycling, idempotent replay, and one better-than-average output item.
- **Added:** Added a `mantok_chest_runs` ledger for pending/completed chest runs with stored input/output audit data.
- **Changed:** Raised the current alpha level cap from 10 to 13 with a steeper post-level-9 XP curve: `450`, `650`, `900`, and `1300` total XP for levels 10-13.
- **Changed:** Moved the level-cap celebration and `/restart` suggestion from level 10 to level 13.
- See `CHANGELOG.md` for 6 more scope detail(s).

## Guardrails / non-goals

- Epic-level abilities, level 14+ progression, broad quest engine, shops, trading, crafting, achievements runtime, manual chest selection, item-instance inventory, or combat rebalance.

## Verification notes

- Detailed tests and implementation notes live in the original PR/history and relevant test files.
- Use focused tests around changed modules if reopening this area.
- Keep player-facing copy Ukrainian.

## If reopening a follow-up

```text
Use $kvestarnia-version-task.

Create a new task doc for the follow-up instead of editing this shipped record.
Start from this historical record and CHANGELOG.md entry for 0.0.24.
Context: docs/ai/context.md
Final output: changed files, behavior changed, tests run, risks, completion status. No tutorial.
```
