# 0.0.23 — Loot Engine & Reward Replay

Status: Shipped
Date: 12026-06-15
Source: `CHANGELOG.md`

This is a compact historical task record generated from the shipped changelog entry. It is not an active task unless a human explicitly reopens a follow-up.

## Goal

Ship `Loot Engine & Reward Replay` as version `0.0.23`.

## Scope summary

- **Added:** Added a pure loot domain engine with rarity weights, deterministic/injected RNG, bounded LUCK influence, monster loot candidates from content, and safe no-loot fallback.
- **Added:** Added reward replay fields to `solo_combat_sessions` so won persistent fight rewards can be shown again without rerolling loot.
- **Added:** Added a small per-session reward path for won persistent solo fights: XP, gold, and at most one controlled monster loot item.
- **Added:** Added idempotent reward claiming for persistent fight victories through `daily_actions` using the solo combat session id as the reward bucket.
- **Added:** Added tests for loot selection, bounded LUCK, no eligible loot fallback, persistent fight reward claim/replay, no rewards for flee/expired outcomes, presenter copy, and schema shape.
- **Changed:** Persistent fight victory screens now show the fight reward and replay the same reward summary on repeated callbacks instead of implying a second payout.
- **Changed:** The `Тринадцять дрібних проблем` wrapper reward remains a separate one-time reward and still does not replace the per-session fight reward.

## Guardrails / non-goals

- Shops, selling, trading, crafting, item-to-level exchange, consumable item actions, achievements runtime, group/PvP combat, Redis/jobs, Mini App UI, broad quest engine, or bestiary collection progression.

## Verification notes

- Detailed tests and implementation notes live in the original PR/history and relevant test files.
- Use focused tests around changed modules if reopening this area.
- Keep player-facing copy Ukrainian.

## If reopening a follow-up

```text
Use $kvestarnia-version-task.

Create a new task doc for the follow-up instead of editing this shipped record.
Start from this historical record and CHANGELOG.md entry for 0.0.23.
Context: docs/ai/context.md
Final output: changed files, behavior changed, tests run, risks, completion status. No tutorial.
```
