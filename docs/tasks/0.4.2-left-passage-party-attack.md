# 0.4.2 — Left-Passage Party Attack

Status: implemented in repository behind a default-off production entry flag;
manual Telegram QA, production enablement, merge and deployment remain pending.

## Goal

Turn the exact pending or recoverable surviving `deep-left` encounter preview at
`PRESENCE_LOCATION_KORCHMA_DEEP_LEVEL1_LEFT` into the first real
production-capable GroupCombat consumer. The player keeps the solo
`⚔️ Атакувати` path and may instead choose `🤝 Покликати в атаку` for a
restart-safe 1–3-person fight against 1–6 enemies.

## Scope

- Atomically reserve the initiating character's exact pending preview or its
  same-life, one-enemy, terminal non-win survivor, current remort life,
  deterministic seed, normalized location and `PartySession` under typed
  origin `nyz-left-passage-party.v1`; do not overload `periodId`. Freeze the
  survivor's canonically regenerated HP in the reservation and production
  snapshot. Cancelling/expiring the gathering restores a consumed survivor to
  solo recovery instead of rerolling or healing it.
- Recruit 1–3 current-life participants for at most three minutes. The current
  leader may start early; the last readiness change starts immediately when
  every joined participant is ready, and the scheduler owns the deadline start.
  Both automatic paths revalidate the authoritative roster atomically under
  `PartySession.version`.
- Revalidate exact location, life, valid persistent resources, active combat,
  search, adventure and expiry at join/start. Never teleport a participant.
  The canonical `adventure.solo-fight` presence marker written by opening this
  preview is compatible with reservation/join/start while any other adventure,
  raid or owned combat lease remains an explicit blocker. Validate resources
  against canonical effective maxima, not raw base HP/mana columns.
- Keep the reserved preview monster as the primary enemy. Start with one enemy
  per participant, then add one deterministic authored backup for every frozen
  participant whose remort count, solo Низ pressure or three-level advantage
  marks them as strong; cap the encounter at six enemies. Freeze the complete
  roster, ability loadouts and difficulty snapshot under `group-combat.v3`.
- Derive each participant's bounded canonical solo Низ pressure with
  `decideThreatEscalation` from history belonging to the participant's frozen
  remort life; legacy history without life metadata applies only to base life.
  Choose the strongest source with roster-order tie-breaking and apply its
  repeated-pressure bonus only to the first backup, capped at level 23. Group
  results never mutate solo pressure history.
- Freeze every remort input; use the strongest remort source with roster-order
  tie-breaking and canonical multi-enemy remort pressure on backups only.
- Reuse the hardened class/race/gear/item actions, deterministic targets and
  timeout guards from `0.4.1`. Freeze the complete canonical 132-ability
  monster catalog in the immutable production-v1 resolver instead of
  maintaining a second short GroupCombat allowlist. Authored self/ally/
  all-player scopes, direct damage, healing, shields, buffs, debuffs,
  cooldowns and once-only markers execute through the common ability recipe.
  Every canonical authored ability is eligible for GroupCombat; an unknown or
  malformed persisted ability still fails strict validation.
  A separately delivered one-time production
  opening message follows the ordinary-fight title/participant/opponent/tip
  shape and names remort reinforcements or repeated Низ pressure when present;
  the canonical combat card uses the compact ordinary-fight heading,
  cooldown/effect and recap rhythm. One deterministic living monster may speak
  per turn. Player multi-target damage, healing and protection lines state
  their exact target scope and whether damage/healing is per target. Every
  newly defeated monster receives its own `🧾 Знешкоджено` paragraph after the
  complete participant/enemy action exchange instead of interrupting it.
  An enemy alive when the exchange begins still makes the ordinary final basic
  response after being defeated during participant actions; it cannot use a
  posthumous heal, shield, buff or special. Solo ability/item lines name the
  actor once with explicit `застосовує вміння` / `використовує манатку`, keep
  the action icon and omit quoted labels or repeated self-target names.
  `skill.strict-blessing` and `gear.asclepius-instruction` keep their authored
  lowest-HP ally healing and all-ally protection while their authored direct
  damage selects the first living enemy in frozen enemy order. These are the
  complete explicit GroupCombat support-scope/direct-damage exceptions.
  Restart delivery,
  already-active replay and ordinary refresh do not repeat the intro. Authored
  deterministic monster specials, player and enemy contribution totals and a
  rolling last-25-turn resource/cooldown/effect journal remain replay-safe.
  Production combat has no artificial turn-limit defeat while both sides live;
  the 25-turn terminal limit remains exclusive to the rewardless proof.
- Surface the live gathering through `👀 Хто поруч` at the exact location for
  members and non-members. Reuse one preparation presenter for readiness
  markers and the `✅ Готово` / `⏳ Зачекайте` actions; keep
  `⚔️ Почати атаку` available to the current leader.
- Keep exact combat items behind one `🎒 Одноразові манатки` button in both
  GroupCombat and ordinary persistent fights. Show the GroupCombat reply button
  only while at least one owned supported item is currently useful and legal.
  The submenu exposes only those items; opening it spends no item or turn.
- Make `/dev_reset_monster_rest` clear the same consumed-passage completion
  evidence used by left-passage rest. When every nearby candidate is already
  busy in combat, Bard performance copy says that Inspiration must be applied
  before the fight; live mid-combat buff injection remains explicitly deferred.
- Keep full contribution output behind `📊 Статистика` beside `📜 Журнал` on
  the terminal result only. Active combat cards expose neither detail surface;
  stale active journal/statistics callbacks return the canonical fight card.
  The active/terminal battle card remains compact, terminal cards omit
  `🔎 Оновити` after settlement, and stale action/refresh callbacks replace a
  superseded card at the bottom instead of mutating an older message. Live-card
  HP/effect/action text and target buttons use the ordinary-fight first-word
  monster labels, adding frozen-order numbers when short labels collide;
  intros, defeat notices, journals and statistics retain full authored names.
- Persist final same-life HP/mana and consumed supplies. Build one bounded
  encounter reward plan from the ordinary persistent-fight XP formula and
  character-level gold band for every frozen enemy; 1×1 therefore matches
  ordinary PvE, while larger groups add enemy budgets before a neutral split
   between participants with a committed manual action. Grant timeout-only
   passengers nothing. Every enemy receives one deterministic ordinary broad-
   loot roll and one ordinary bandage-slot roll; stronger enemy levels improve
   the broad-loot chance, more enemies add opportunities, and the rolls are
   distributed neutrally among eligible manual participants. Authored monster
   loot, eligible Loot Expansion manatky, bandages and the ordinary rare
   `Іскрокамінь` replacement are all possible. A winning result without loot
   says plainly that no manatka dropped. Active cosmetic titles remain
   presentation-only and never enter reward calculations. `lootVersion: 1`
   uses a code-owned immutable v1 catalog and resolver. The persisted snapshot
   contains only canonical rolled outputs; exact candidates, weights, selected
   item and quantity are rederived from frozen encounter/participant inputs
   rather than trusted selection ranges or adjacent public checksums. Current
   items, monster loot, monster metadata, ability profiles and the generic loot
   algorithm are not consulted on active restart or terminal settlement.
   `GroupCombatParticipant.snapshotJson` is independently and strictly parsed
   as the start-time authority for immutable identity/display/title/life/order,
   class/race/level, maxima, attack/defense/support, base stats, equipment,
   granted gear abilities and initial combat-item quantities. Runtime state may
   change only current HP/mana, cooldowns, threat, flee/fumble evidence and
   item quantities decreased by canonical committed item actions. The immutable
   v1 catalog stores released effective candidate rarity: every Loot Expansion
   `x025` base/enhancement variant is selectable as `epic`, not as its raw
   authoring rarity `legendary`.
   Legitimate rows therefore survive unrelated content/algorithm drift, while
   coherent output/evidence mutations, changed recipients or quantities,
   duplicate rows, enemy stats, abilities and reward inputs fail closed.
- After a production victory, persist the same deterministic 13–23-minute
  left-passage discovery window for every participant. During it, no new
  monster preview appears in the left passage and no other left-passage
  gathering may accept that participant through either an invitation or
  `👀 Хто поруч`; the join blocker names the previous group fight rather than
  coupling its explanation to the left passage and includes the canonical
  remaining wait. The card shows
  `↩️ Повернутися до Сутеренів` above `🪜 Ярус II`. The second tier is a non-mutating
  construction notice only and cannot be entered in `0.4.2`.
- Validate the complete turn-start action snapshot before start-of-turn effects
  or roster execution. Record every accepted manual action as eligible before
  execution order can end the fight; skipped later actions spend no
  mana/items, tick no cooldowns and apply no effects. Timeout guards remain
  ineligible.
- Settle each participant in one transaction: validate current life, canonical
  plan/receipt and the participant-owned lease; apply resources, XP, gold,
   deterministic per-enemy loot and one privacy-safe Chronicle event; commit the exact immutable
  receipt; then release only that participant's lease. Timeout-only
  participation receives no reward or activity. Settlement may advance a level
  from XP but never lowers a character level already raised after combat start.
  A successful flee uses the same transaction boundary immediately: persist
  exact final HP/mana and already-committed inventory, resume frozen timed
  statuses, write canonical fled-turn/attempt receipt evidence and release only
  the escapee's lease before any Telegram success is claimed. A durable
  pending/claimed/menu-delivered/completed/superseded exit-delivery state retries
  a failed menu after later party turns, records the returned Telegram message
  id, never resends after durable acknowledgement when old-card retirement
  fails, and finally clears the inert combat-card reference. Gameplay exit is
  exactly once, while Telegram notification is durable at-least-once: Bot API
  has no send idempotency key, so a crash after acceptance but before database
  acknowledgement leaves a bounded duplicate risk on stale claim recovery.
  A live claim retries acknowledgement without immediately resending; ordinary
  Telegram rejection returns to pending. Before menu restoration, delivery
  reads current authoritative navigation: free players receive current
  presence/quest markers, while a newer active combat durably supersedes the
  old delivery and keeps its battle keyboard. The
  remaining party validates with only its active leases. Later terminal
  settlement skips that completed exit entirely, including after the escapee
  changes resources or remorts; the shared passage discovery may touch the
  escapee only if the frozen remort life still matches. Failed, duplicate,
  concurrent and restarted attempts cannot partially release or reapply the
  gameplay exit. Chronicle related-character ids and participant counts use only the
  canonical eligible manual roster: successful escapees and timeout-only
  participants never appear.
- Order every active card/reply-keyboard publication through the same durable
  per-character claim used by terminal/flee navigation fencing. Renew the exact
  session/revision/token atomically before each Telegram send/edit/delete and
  abort each Bot API operation after 13 seconds, below the 23-second stale
  boundary. Loss of claim ownership prevents every later Telegram call in that
  replacement, so a terminal menu or newer combat that takes ownership remains
  last. Telegram can still accept a request immediately before a connection
  failure or abort and offers no idempotency key; that external at-least-once
  ambiguity is not claimed away. A failed database acknowledgement retains the
  claim for stale restart recovery. Record a reply-keyboard fingerprint and
  increment its generation only when that exact keyboard was attached to the
  delivered private card; no-keyboard supergroup/legacy delivery acknowledges
  only the card and leaves controls due for the next private publication. The
  one-time production intro is always a separate message, but the first
  authoritative keyboard-generation owner publishes it inside the same durable
  claim immediately before the canonical card. Both private messages carry the
  current reply keyboard, so whichever Telegram displays last still leaves an
  active combat actionable. It never edits or releases the canonical reference,
  and no starter/scheduler retry may publish the intro after an acknowledged
  battle keyboard. When the
  acknowledged controls remain unchanged, active-card and already-delivered
  compact-reference edits omit `reply_markup` entirely instead of applying an
  empty inline keyboard over the persistent reply keyboard; this preserves the
  visible controls without another participant send. Changed controls and
  explicit actor refresh/recovery still publish one fresh keyboard-card. The
  direct reply keyboard fills ordinary controls two per row, including a second
  available ability beside guard when that is the canonical order.
- Apply the same live-I/O bound to the separate exit-navigation claim. After
  current presence/quest markers are resolved, atomically renew the exact
  participant/session/token and navigation lease immediately before sending
  the main-menu reply keyboard. Abort that Bot API call after 13 seconds; lost
  ownership publishes nothing, ordinary rejection returns the exit to pending,
  and acknowledgement failure retains the claim for stale restart recovery.
  A reclaimed worker may acknowledge and release the navigation lease before a
  newer combat starts; the aborted old request cannot later make its menu last.
  The terminal result card remains reply-keyboard-free, so its retained
  at-least-once delivery cannot replace a newer combat keyboard.
  Treat `claimed` and claimed `menu-delivered` rows as canonical while that
  exact exit token is live: repair must not quarantine this normal send/ack/CAS
  interval. Generic combat-lock navigation recognizes the temporary
  `group-combat-exit-navigation` lease, resumes its exact participant exit and
  continues the requested route after completion instead of emitting a global
  mismatch warning that blocks quests and locations.
- Persist every validated queued/replaced manual action in its own transaction
  before attempting turn resolution or claiming terminal/flee navigation
  fences. A separate bounded resolver adopts the complete persisted action set;
  live UI ownership never rolls the action row back and never spends optimistic
  mutation-conflict retries. If the local adoption budget is exhausted, return
  the honest queued/replaced/duplicate result rather than `stale`; duplicate
  submission, timeout/scheduler work or another process can resume the same
  turn after restart. Consumables, combat effects, flee evidence, rewards and
  receipts remain exclusively in the exactly-once resolution/settlement
  transactions.
- Ship no new left-passage completion achievement or achievement event in
  `0.4.2`. Achievement progression is deferred and is not part of settlement,
  repair, scheduling or rollout. The unavailable Ярус II construction notice
  also grants no achievement because it performs no transition or gameplay
  action.
- Preserve existing generic progression: durable level milestones commit in the
  participant transaction, then fresh manual settlements may emit the existing
  level/item/combat achievement events and separate standard level/achievement
  Telegram notices. Replayed and timeout-only settlements emit none of these;
  no left-passage-specific event or achievement is introduced.
- Keep v1/v2 proof rows readable, repairable and rewardless. Service existing
  combats independently from default-off
  `LEFT_PASSAGE_PARTY_ATTACK_ENABLED`; flag-off blocks new invitations/starts
  while timeout, delivery, repair and settlement continue.
- Fail closed on malformed or semantically untrustworthy production v3 rows.
  Persist an explicit operator-repair state and concise durable reason, retain
  every lease and frozen artifact until a durable production-safe resource
  disposition exists, and exclude the row before every bounded runtime limit.
  Never load it as ordinary combat or downgrade v3 to v1/v2; explicit operator
  inspection remains available without parsing the malformed evidence. Initial
  reads, final reloads and every mutation CAS fence on `repairState = null`;
  quarantine increments the session version without canonicalizing actions,
  participant resources or leases.

## Non-goals

- Guilds, guild-only access or weekly guild progression.
- Big Barrel migration/redesign, public matchmaking, raid finder or group chat.
- Parties above three participants, encounters above six enemies, new authored
  abilities, rare items or a new economy.
- A generic quest engine, broad `FightService` rewrite or cross-mode
  contribution-card rollout.
- Production flag enablement, merge or deployment.

## Acceptance criteria

1. The party button exists only on the exact left-passage pending preview or
   its same-life, single-enemy terminal survivor; the solo button remains
   available until the preview is atomically reserved. Opening that preview
   must not make its own party action ineligible. A survivor reservation freezes
   canonical regenerated HP and cancellation restores consumed solo recovery.
2. Duplicate creation/start is replay-safe, solo/start races have one winner,
   start consumes the reservation once and cancel/undersized expiry releases or
   safely invalidates it without rerolling the primary.
3. Join/start freeze only the authoritative current 1–3 participant roster.
   Leader transfer, late join, leave and stale due snapshots cannot copy the
   reservation, affect a frozen fight or grant rewards to a departed player.
4. Strongest Низ and remort sources may be non-leaders; ties use frozen roster
   order. Previous-remort and non-base-life legacy wins do not contribute.
   Primary identity/level is unchanged, backups are deterministic from immutable
   reservation/party inputs and all adjustments are explicit in strict state.
5. Timeout auto-guards never count as committed manual actions. Every valid
   manual action accepted for the resolving turn counts even if an earlier
   actor or start-of-turn bleed defeats the final enemy; skipped abilities and
   items consume nothing. Terminal and
   replay cards use persisted damage/heal/guard/control/damage-taken/action
   evidence and do not rank or shame participants.
6. Per-participant settlement retries converge after restart. Pending rows have
   zero attempts/no receipt/no timestamp; completed rows have positive attempts,
   the canonical receipt and timestamp. A failed participant cannot duplicate
   resources, rewards, items or activity, unlock its lease or block another
   participant's settlement. Shape-valid receipt drift enters operator repair
   with the remaining lease and raw evidence retained.
   Exact persisted HP/mana are not truncated to raw base maxima after a valid
   level/equipment-scaled combat, and settlement never lowers a newer live
   character level to an older XP-derived value.
7. Reward totals are one encounter-wide budget made from ordinary persistent-
   fight XP and gold bands for each frozen enemy, then split neutrally across
   eligible manual participants. A 1×1 level-3 fight against a base-level-2,
   effective-level-5 enemy yields exactly 13 XP and 0–3 gold before loot, not
   the former 40 XP / 20 gold shortcut. Loot then uses one ordinary broad roll
   and bandage slot per enemy: effective enemy level scales the broad-roll
   multiplier from `0.75` to `1.5`, enemy count adds independent opportunities,
   and deterministic round-robin distribution can grant authored/Loot
   Expansion manatky, bandages or the ordinary rare `Іскрокамінь` replacement.
   Active cosmetic titles have no reward effect. The immutable code-owned v1
   catalog and resolver rederive canonical enemy evidence, candidates,
   weights, exact item ids and quantities from frozen inputs; no persisted
   selection range or adjacent digest is trusted. Terminal plans reject
   coherent evidence/output mutations, changed item ids or quantities, forged
   recipients and duplicate item rows. Current item, monster-loot, monster
   metadata/profile and generic-loot drift cannot change or invalidate a
   legitimate active or terminal v1 encounter.
8. Migration, strict parsers, 1×1 through 3×6 repository coverage, the retained
   2×2/3×3 proof simulator, focused/full checks and manual
   three-account QA are recorded honestly before any production enablement.
9. Readiness auto-start is covered for one, two and three participants. Every
   monster defeat is recorded in the resolving turn; multi-target player
   actions state their scope and per-target values; only one monster remark is
   selected per turn. A defeated enemy that was alive at exchange start makes
   one final basic response, while a start-of-turn defeat does not. Production
   combat continues past turn 25 while both sides live and keeps a rolling
   25-turn journal. Ordinary and group combat expose one reachable,
   non-consuming one-use-item submenu. `📜 Журнал` and `📊 Статистика` appear
   only after the fight is terminal. While combat is active, the persistent
   main menu is replaced by a compact battle reply keyboard and the active
   card has no inline action grid; every currently legal frozen class, race and
   equipment ability is visible directly, while exact target and item choices
   open only on demand. A dedicated durable per-participant claim orders
   active keyboard publication against flee/terminal navigation and later
   combat ownership across concurrent workers, restart and stale-claim recovery.
   The terminal/flee mutation atomically claims a navigation fence before
   committing, while the owned combat lease remains the authority for the
   current session. An acknowledged keyboard generation/fingerprint means
   unchanged controls are not routinely republished party-wide; changed
   cooldown, mana, target or knockout controls are attached to the one fresh
   canonical card instead of a separate readiness announcement. An explicitly
   fresh actor card requested by `/start`, `🔎 Оновити` or a handled reply
   action republishes the authoritative reply keyboard even when its fingerprint
   is unchanged, so the newest recovery card cannot strand the player without
   controls. Unavailable
   or absent one-use items remove `🎒 Разові` from that reply keyboard. Candidate
   adoption atomically records any keyboard already attached by the successful
   Telegram send, so a later acknowledgement failure or scheduler tick edits
   the adopted canonical card instead of publishing another countdown card. A
   sent candidate that loses the durable reference claim is deleted with
   bounded retry; if Telegram permanently refuses deletion, it is reduced to a
   compact inert superseded note. Every successful active or terminal
   replacement first compacts the previous canonical message and only then
   attempts deletion, so delete refusal cannot preserve a second full battle
   card or terminal result beside the winner. A failed fresh terminal send
   restores the previous result.
   `🔎 Оновити` first persists a participant-specific pending keyboard request.
   A busy claim, process restart or stale takeover therefore remains eligible
   for bounded scheduler delivery, and active delivery cannot be finalized
   while that reply-keyboard fingerprint is still pending.
   A completed participant exit is sufficient to retire terminal delivery work
   even when an older legacy card revision was never advanced; finished rows do
   not remain in the five-second scheduler queue.
   Unavailable abilities disappear and reappear with canonical validation, and a
   knocked-out participant receives only `🔎 Оновити`. Every reply-keyboard
   action redraws at most one acting-participant canonical live card as the
   newest message. A press from an older keyboard is recognized from the frozen
   loadout, reaches current canonical validation, commits no unavailable action
   and redraws without a separate error bubble. Delivery rechecks controls
   after card convergence so a concurrent older attempt cannot leave an older
   keyboard or card. When no matching GroupCombat exists, stale reply text
   delegates to authoritative current combat/navigation handling instead of
   restoring a generic menu over a newer fight. Terminal delivery restores the
   location-aware main menu before a fresh bottom-most card with Journal and
   Statistics and atomically records
   that result as the participant's canonical delivered reference. `🏃 Відступити` rolls
   independently for that participant with the ordinary escalating flee
   formula and a persisted personal attempt count. Success removes only that
   actor, atomically persists their exact resources/inventory/status release
   and canonical exit receipt and releases only their lease. Telegram menu
   delivery is durable at-least-once, records the returned message id and is
   deduplicated after durable acknowledgement; the unavoidable send/ack crash
   gap may yield a bounded duplicate on stale reclaim. A live active publication
   makes exit/terminal retry; an already fenced exit or later combat makes the
   stale active worker send nothing. Menu failure remains
   retryable after later turns, an acknowledged menu is not resent if card
   retirement retries, and a newer combat durably supersedes the stale exit
   menu instead of losing its battle keyboard. The participant forfeits encounter
   rewards/activity, the remaining party continues without that lease and
   later settlement cannot overwrite the escapee's newer life or resources;
   failure leaves them in combat without converting the action into guard.
10. Every production winner receives one shared deterministic 13–23-minute
    left-passage clear window. Its card exposes the unavailable `Ярус II`
    construction notice and a return to the Сутерени, while other passages
    remain available. Until the window ends, both direct invitations and
    exact-location nearby joins to another left-passage gathering fail with
    the canonical remaining wait. The dev monster-rest reset clears this
    window without changing group-combat history.

## Relevant files / search terms

- `PendingPassageEncounter`, `PartySession.originKind`,
  `GroupCombatSession`, `GroupCombatParticipant`, `group-combat.v3`.
- `deep-left`, `PRESENCE_LOCATION_KORCHMA_DEEP_LEVEL1_LEFT`,
  `LEFT_PASSAGE_PARTY_ATTACK_ENABLED`.
- `decideThreatEscalation`, `applyThreatSecondEnemyLevelBonus`,
  `deriveMonsterCombatStats`, settlement plan/receipt and combat leases.

## Focused tests

- Exact-surface button/flag/callback bounds; duplicate reservation and
  solo/start concurrency.
- One authoritative `activeCombatLease` read for generic combat-lock routing,
  exact `referenceId + characterId` owner dispatch without unrelated combat
  probes or a second solo ownership read. Missing, terminal, quarantined,
  wrong-participant and unknown-kind owners fail closed with one recovery
  notice and no downstream navigation, plus separate callback
  `interactiveMs` / `postPresentationMs` / `totalMs` evidence without new
  participant delivery.
- Join/start location/life/resource/search/combat/expiry gates; transfer,
  join/leave and due-start races; cancellation reservation release.
- Exact primary plus deterministic backups; non-leader pressure/remort source,
  tie-breaking, cap 23 and frozen restart state.
- Separate fresh-start intro versus canonical active card, both carrying the
  current reply keyboard under the first generation owner; no intro on
  already-active/restart delivery; manual-versus-timeout participation; player
  and enemy contribution fields; the complete frozen 132-ability monster
  catalog with a typed exactly-once consumer for every semantically active
  authored parameter, including typed target/damage modifiers, self-defense, self/ally
  heal, all-monster buff, all-player and single-player specials, mana drain,
  cleanse/removal/reapply, locks/pressure, counters/reflection,
  damage-over-time, accuracy/evasion, marks, cooldown changes and conditional
  follow-up damage; exact class/race/one-frozen-ability lock selection;
  ability-only base-plus-pressure mana spending with no basic-action surcharge;
  one canonical flee chance with the seventh-attempt guarantee; first-landed-hit
  mark consumption; exact effect source/target/trigger/duration/charge restart
  replay plus separately validated reapplication provenance;
  unsupported start-time recipe and strict unknown/corrupt persisted
  effect rejection;
  three-player early-kill/start-effect manual eligibility with no skipped
  resource use; exact `strict-blessing` / `asclepius-instruction` ally support
  plus canonical first-living-enemy damage, replay, cooldown and fumble;
  final basic enemy responses; no artificial production
  turn-limit loss; rolling turn snapshots and monster remarks; explicit
  actor/verb/icon solo ability and item lines; compact collision-safe live-card
  and target-button monster names; compact battle reply keyboard with every
  available ability visible directly, cooldown/mana/target-unavailable
  abilities hidden and restored through canonical action validation,
  frozen-label validation for an immediate duplicate press, target-only pickers
  when needed, active-delivery keyboard convergence for every participant,
  durable active-versus-flee/terminal/new-solo/new-GroupCombat lease barriers,
  per-call 13-second Telegram send/edit/delete bounds with atomic claim renewal,
  acknowledgement failure, restart/process-death and stale-claim takeover,
  concurrent/retried card-and-keyboard
  convergence with exact Telegram send/edit/recipient order, bottom-most canonical
  actor-card redraw after valid or unavailable reply actions, stale-text
  delegation that preserves newer solo/duel/raid/GroupCombat navigation,
  observer-only refresh for knocked-out participants and an on-demand item menu;
  independent deterministic flee attempts,
  atomic escapee resource/evidence/status/lease exit with rollback, retry,
  duplicate/concurrent/remort/new-combat coverage, eligible-only Chronicle
  roster, durable late-turn menu retry, acknowledgement retry without immediate
  resend, bounded ambiguous send/ack-gap reclaim, returned message-id evidence,
  current-location/quest menu rebuilding, newer-combat supersession,
  acknowledged-menu/card-failure replay, restart/concurrent delivery
  convergence, inert old card and terminal restore;
  remort/pressure intro notes; active-card journal/statistics absence
  with stale callback rejection; terminal journal/statistics availability;
  ordinary result/reward formatting;
  monotonic-level exact-once resource/reward/item/activity/level-milestone settlement, restart
  and duplicate invocation; standard post-settlement level/achievement notices;
  no left-passage completion achievement event or unlock.
- Live UI claims held beyond the old 13 × 23 ms retry window for a final win,
  successful flee and timeout; concurrent duplicate submission and restarted
  stale-claim recovery must preserve the action/effects exactly once and leave
  terminal/flee/newer navigation last. A no-keyboard supergroup redirect must
  not acknowledge controls; the following private delivery publishes the
  changed keyboard exactly once.
- Exit main-menu sends for both successful flee and terminal settlement paused
  beyond the stale boundary; exact pre-send renewal, 13-second abort, ordinary
  failure release, acknowledgement failure, stale takeover/restart and newer
  solo/GroupCombat publication must keep the authoritative newer keyboard last.
  After terminal menu acknowledgement, the exact claim/navigation lease remains
  owned through old-card compaction, fresh result send, canonical-reference
  adoption and losing-candidate cleanup; only the final exact-token/reference
  CAS releases it. Process death after adoption remains recoverable.
  A process interruption after a final win/flee action row commits but before
  resolution must leave that row canonical and adoptable by duplicate or
  timeout work without duplicating consumables, resources, rewards, receipts,
  flee effects or navigation.
- Strict production v3 location, cardinality, difficulty, reward, loot, terminal
  and settlement semantics; cosmetic-title loot invariance; active and pending
  terminal loot-v1 item/monster/ability-profile/monster-loot/generic-algorithm
  drift; active and terminal changed-item-id/quantity rejection; coherent
  selection/evidence/output forgery with every legacy public SHA-256 value
  recomputed; forged plan item, recipient and duplicate-item rejection;
  canonical enemy-stat/ability/reward evidence; independent relational actor
  snapshot rejection for coherent class/race/LUCK, level/reward,
  attack/defense/support, equipment/gear-ability and item-quantity mutations;
  legitimate committed item decrement across restart; exhaustive released
  direct/Loot Expansion candidate parity, including effective-epic selection
  for every `x025` variant;
  active/terminal operator inspection, queue
  quarantine, stale action/timeout/settlement/delivery race fences, 13-row
  quarantine starvation prevention, exact receipt-only
  character/reward/resource/activity corruption, lease/resource retention and
  no-repeat diagnostics; unchanged v1/v2 rewardless repair compatibility.
- Query/state/card/callback budgets; deterministic 1×1, 2×2, 3×3 and 3×6
  production restart, retained proof simulation and
  unchanged solo/Training/duel/Big Barrel suites.

Automated evidence on the final candidate tree:

- The final monster-semantics focused slice passed **11 files / 491 tests**,
  including repository JSON round-trip and delivery/lease regressions. It
  proves the typed authored-parameter coverage contract across all 132 frozen
  abilities, exact class/race/one-ability locks, effective ability mana cost,
  seventh-flee success, first-landed-hit marks and restart-canonical effect
  reapplication. Across 42 deterministic reachable 3×6 rosters, including the
  effect-heaviest 25-turn case with 10 active and 6 retained expired effects,
  the worst full-journal state is `64,030 / 65,536` bytes (seed 10), leaving
  `1,506` bytes of headroom. Repository query observations remain
  `35/35` manual start, `34/34` due start, `25/25` queue, `39/42` single
  resolve, `50` concurrent-pair aggregate, `29–30/38` settlement, `1/1`
  due/delivery/settlement scans and `6/6` idle repair.
- Focused GroupCombat domain/service/presenter/command/delivery/scheduler/lock
  plus PartySession recruitment and FightService preview coverage passed:
  13 unit files / 551 tests and
  2 repository integration files / 178 tests.
- The final authoritative-lease/performance follow-up passed 11 unit files /
  536 tests plus 2 exact-owner repository integration files / 103 tests. It
  proves one lease read, exact owner-only dispatch, no second solo ownership
  read and separate privacy-safe interactive/post-presentation/total timing.
- Direct presenter/delivery/command/scheduler/lock coverage passed at 5 files /
  124 tests. Initial production delivery serializes one separate intro send and
  one canonical combat-card send per participant under the same durable
  publication claim; both messages carry the current reply keyboard, so visible
  ordering cannot leave the active chat without controls. An unchanged later turn uses zero sends, one canonical edit
  per participant without `reply_markup`, and no keyboard-generation increment.
  Exact delivery assertions also cover one actor send for queued/replaced/duplicate
  and unavailable actions; a changed two-participant resolved generation uses
  two sends/four edits in actor-then-ally order, with no separate keyboard-ready
  message. A supergroup no-keyboard redirect followed by private recovery uses
  one supergroup notice, then one private canonical send/two private edits/one
  old-card delete and acknowledges the keyboard only on the private send. An
  adopted but not-yet-marked card retry uses zero sends and one edit; its
  keyboard generation remains unchanged. A refresh blocked by a live claim
  performs zero sends on that attempt, and the recovered delivery publishes
  exactly one keyboard-bearing candidate send. Successful active or terminal
  replacement makes the old reference one compact superseded edit before its
  delete attempt; a rejected delete never leaves two full combat cards/results,
  and a failed fresh terminal send restores the old result.
- Exact repository coverage passed at 121 tests, including wounded-survivor HP
  reservation/cancellation/restart and the terminal
  `menu-delivered` repair barrier: the owned final-CAS interval remains
  canonical, retains its participant lease and completes without quarantine.
  Combat-lock coverage proves the exact temporary exit-navigation owner resumes
  the menu and then lets the original navigation continue without mismatch
  spam.
- Ordinary persistent-fight one-use-menu routing passed at 4 files / 464 tests.
- Full repository coverage passed at 333 files / 4,261 unit tests and 27 files /
  684 integration tests.
- Prisma validation/generation, the full combat simulator and its 24-case
  2×2/3×3 GroupCombat matrix passed. Documentation checks passed for 419
  Markdown files / 248 context lines.
- Observed GroupCombat query events remained bounded at `35/35` manual start,
  `34/34` due start, `25/25` queue, `39/42` single resolve and `1/1` due scan;
  the concurrently resolving pair used 50 aggregate events and the measured
  production start used 41. Delivery and pending-settlement scans remained
  `1/1`; receipt-aware idle repair measured `6/6` and participant settlement
  `29–30/38`; the settlement
  cap covers the strict twelve-item per-enemy grant
  bound.
- Final state/card/callback and simulator byte observations are recorded in the
  QA document and owning draft PR.

## Manual Telegram QA

Use three accounts with `NODE_ENV=development`,
`LEFT_PASSAGE_PARTY_ATTACK_ENABLED=true` and
`DEV_GRANT_COMMANDS_ENABLED=true` in the untracked checkout `.env`. Run
`refresh-local-bot.cmd`, verify `status-local-bot.cmd` reports the exact
candidate SHA and confirm the copied runtime `.env` contains the enabled
left-passage flag before opening Telegram. The checkout `.env` alone does not
update a running snapshot.
Cover deep-link and nearby joins, 1/2/3-player early and automatic start,
leader transfer/join/leave races, non-leader strongest pressure/remort, cancel
and expiry, restart during combat/settlement, timeout-only participation,
partial retry, post-victory join rejection through both entry paths and flag-off
while an active fight finishes. In an ordinary persistent fight with an eligible
one-use item, confirm `🎒 Одноразові манатки` opens the concrete filtered picker
instead of the generic combat-lock card and spends neither the item nor the
turn. In GroupCombat, confirm `🎒 Разові` is absent with no currently useful
item, appears when a usable item exists and disappears again after use or while
  blocked. Leave one turn idle across several scheduler ticks: the same canonical
  card must be edited in place rather than sending countdown copies every five
  seconds. Press `/start` and `🔎 Оновити` during the active fight after the
  current controls were already acknowledged: each deliberate fresh card must
  carry the battle reply keyboard. Hold its UI claim across the immediate
  refresh attempt and confirm the bounded scheduler publishes exactly one fresh
  keyboard-card after release/stale reclaim rather than losing the request or
  sending five-second copies. Force one previous-card delete refusal and
  confirm the previous full combat card was already compacted before that
  delete, leaving only one full canonical card. Repeat at completion: the main
  menu must precede exactly one full bottom-most result, while a refused old
  delete leaves only the compact replacement note. A completed fight must stop appearing in
  later scheduler passes. Start a production fight while the command path and
  scheduler both attempt first delivery: on every account the separate intro
  and exactly one canonical card must both carry the current reply keyboard,
  and the persistent keyboard must remain visible regardless of their visible
  order. At terminal settlement, pause after
  the main menu is durably marked delivered and run one repair tick; it must
  retain the healthy exit claim, finish the navigation lease and allow the same
  queued quest/location input through without any `Бойовий запис не
  збігається` message. Confirm no location changes and record the exact SHA/runtime. Use
`/dev_group_combat_timeout <party-token>` to advance one active timeout boundary
without waiting; the helper is non-production only. Results remain pending until
run. Local two-account probing exposed and reproduced the knocked-out
participant repair deadlock; the isolated test DB was backed up, conditionally
unquarantined after the fix and remained active without a new repair marker.
That diagnosis is not a substitute for the final three-account matrix.

## Release surfaces

- Version lockstep: package, lockfile, changelog and spoiler-light player news.
- Roadmap/task index, architecture sequence, QA matrix, compact context,
  playtesting, release ledger, achievement catalog and environment docs.
- Migration `20260724233000_left_passage_party_attack`, including the durable
  production operator-repair marker/reason, party origin and passage reservation
  fields plus the per-participant flee exit-delivery state/claim and acknowledged
  Telegram message id, acknowledged reply-keyboard fingerprint/generation and
  the dedicated GroupCombat UI-publication claim table. Its unreleased
  achievement-only participant columns and indexes were removed in place
  without changing migration identity. Rollback keeps fresh entry disabled,
  drains healthy active rows, resolves every operator-required disposition
  explicitly, then restores the pre-migration DB; the unreleased migration must
  not be partially reversed in place.
