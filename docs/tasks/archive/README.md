# Archived Task Drafts

This directory keeps superseded, consumed, or conditional task drafts that are
not current implementation contracts. Use the active versioned files indexed by
[`docs/tasks/README.md`](../README.md) instead of launching these drafts
verbatim.
