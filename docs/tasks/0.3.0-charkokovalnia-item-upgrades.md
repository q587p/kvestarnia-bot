# 0.3.0 - Charkokovalnia / Item Upgrades MVP

Status: implemented in this task branch.

## Goal

Add a replay-safe, bounded item-upgrade loop for selected manatky without introducing item-instance identity or a broad market/crafting rewrite.

## Scope

- Use concrete item ids for upgrade variants: `base`, `base.plus-1`, ... `base.plus-5`.
- One successful upgrade moves one owned stack unit to the next concrete item id.
- Keep equipped rows aligned when an equipped stack unit is upgraded.
- Use deterministic cost/chance/pity/donor gates.
- Add Iskrokamin / material reward path only as required by the upgrade loop.
- Add Charkokovalnia Telegram callbacks, keyboards and presenters at `Задвірок корчми`, not as a `Манатки` command/shortcut.
- Add the Mage NPC unlock: level 5+, or level 3+ after remort, then consume one `Польова аптечка` and grant dynamic XP.
- Add dev QA helpers behind production-disabled dev gates.
- Add rewardless achievements for first upgrade success, first failed attempt and first `+5` upgrade.
- Add a rewardless first-Iskrokamin achievement now that Iskrokamin can drop from fights.
- Update item detail, inventory, equipment, hero/effective-stat and Mantok presentation.
- Treat `+1..+3` as weak magic and `+4..+5`, set pieces and ability-grant manatky as strong magic; magical equipment bonuses require attunement after equip.

## Non-goals

- No item-instance rewrite.
- No new Prisma migration unless a specific implementation proves unavoidable.
- No market, auction, player-to-player upgrade service or paid power.
- No broad loot/economy rebalance.
- No new combat actions.
- No tournaments.
- No broad plus-drop economy: generated Loot Expansion `-plus-N` rows may drop only as a very rare tail of existing generated loot and do not double-upgrade already generated ids.

## Acceptance criteria

- Preview does not spend resources.
- Stale direct attempts reject before spend when stack/level/pity snapshot changed.
- Successful upgrade mutates exactly one stack unit and replays safely.
- Equipped item alignment remains correct.
- Failed attempts apply pity/cost exactly once.
- Pity and donor bonuses are bounded and visible only qualitatively before commit; exact committed details may appear after an attempt.
- Plus drops remain very rare when generated Loot Expansion candidates are eligible.
- Positive post-fight bandage slots can rarely become Iskrokamin instead, never both from the same slot.
- Iskrokamin is transferable through Safe Gifting and postal delivery.
- Existing Mantok set bonuses and Mantok ability grants preserve base-item semantics for upgraded concrete ids.
- Equipping magical manatky asks for confirmation, starts `Налаштування на...`, withholds stats/set/ability bonuses until the timer is ready, and warns before replacing a still-tuning slot.
- Replaying equip for the same item already tuning in the same slot is a no-op that preserves the active attunement row.
- Successful upgrades are recorded in `📜 Хроніки Квестарні` under `Манатки`; `+5` successes are marked important.

## Implementation notes

- Upgrade variants for authored eligible equipment manatky are generated in content as `base.plus-1` through `base.plus-5`.
- Upgrade attempts use existing `CharacterItem`, `CharacterEquipment` and `DailyAction` rows; no Prisma migration was required.
- Charkokovalnia unlock state uses a persistent `DailyAction` key, consumes one field kit and grants bounded dynamic XP.
- Pity is stored under a persistent `DailyAction` key per item id and target level.
- `Іскрокамінь` is the only new material item, added only as a Charkokovalnia cost stack.
- Fight rewards can now very rarely replace a positive ordinary-bandage reward slot with bounded Iskrokamin, using compressed quantity mapping.
- Iskrokamin is not trade-blocked; gift and postal eligibility are intentionally consistent.
- `/dev_add_iskrokamin [amount]` is available only when the existing non-production dev-grant gate is enabled.
- Weak magic (`+1..+3`) keeps the old small stat scaling and uses a 13-minute equipment attunement. Strong magic (`+4..+5`, set pieces and ability-grant manatky) uses stronger upgrade bonuses, higher Iskrokamin costs and a 42-minute equipment attunement.
- Pending attunement is stored in existing `DailyAction` rows under the equipment attunement key. Tuning equipment stays visually equipped but is excluded from effective stats, set bonuses and Mantok ability grants until ready; the runtime scheduler sends the completion notice and marks it notified.
- `/dev_finish_attunements` is available only behind the same non-production dev-grant gate and moves pending attunement timers to ready for local QA.
- Lore decision: `📖 Перекази` was reviewed and the manatky entry now mentions the backyard Mage and careful Charkokovalnia strengthening without promising markets, crafting or hidden procs.

## Focused tests

- `tests/domain/itemUpgrades.test.ts`
- `tests/bot/itemUpgradeCallbackData.test.ts`
- `tests/db/prismaItemUpgradeRepository.integration.test.ts`
- `tests/content/lootExpansionV1.test.ts`
- `tests/domain/lootEngine.test.ts`
- item detail/equipment/Mantok presenter and semantics tests
- equipment attunement domain/service/repository/scheduler/presenter tests
- fight reward Iskrokamin replacement, transfer eligibility, first-Iskrokamin achievement and successful-upgrade Chronicles tests
- dev helper production-disabled tests
- `npm run check`

## Manual Telegram QA

- Enable local dev grants and seed `Іскрокамінь`, an upgradeable item and a field kit with `/dev_add_iskrokamin`, `/dev_add_item` and `/dev_add_field_kit`.
- Open `Задвірок корчми` -> `Чароковальня`, verify the Mage asks for the field kit, then unlocks after turn-in.
- Preview `+1` through the NPC path and the self path for a mage-class character.
- Add/remove a donor stack and verify preview copy changes qualitatively.
- Double-click a stale attempt after success and verify no second spend or upgrade.
- Upgrade equipped and unequipped copies.
- Equip `+1..+3` gear and verify the 13-minute tuning warning/status; equip `+4..+5`, set and ability-grant gear and verify the 42-minute path.
- During tuning, verify hero stats, combat/duel/noncombat checks, set bonuses and equipment actions ignore the item; equipment should show the struck-through effect and `Йде налаштування`.
- Replace a slot while tuning and verify the separate interruption confirmation.
- Use `/dev_finish_attunements` locally, then verify the completion notification and active bonuses.
- Verify inventory, item detail, equipment and hero copy.
- Verify gifting, postal delivery, Mantok Chest, remort/protected-item flows and Mantok set/ability-grant semantics around upgraded ids.
- Verify Iskrokamin appears in gift and postal choices when owned and not reserved/equipped.
- Verify achievement notifications unlock once for first success/failure, first Iskrokamin and `+5`.
- Verify successful upgrades appear in `/chronicles` under `Манатки`, and `+5` upgrades appear under `Важливе`.

## Release surfaces

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `news.md`
- `docs/ai/context.md`
- `docs/tasks/README.md`
- balance/content/QA docs as needed
