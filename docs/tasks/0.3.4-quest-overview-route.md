# 0.3.4 - Quest Overview Route

## Goal

Turn the persistent `🗺️ Квести` button and `/quest` into a compact quest overview/journal route that works from safe non-combat states, plus ship the first tiny route quest that teaches players how to reach `📋 Стіл зі справами`.

## Scope

- Render a mostly read-only `🗺️ Квести` overview from the current quest snapshot.
- Show compact rows only for active/taken/current-period progress or claimable quest work.
- Hide starter rows, retired rows, locked future quests, generic available untaken quests, completed/reward-claimed rows, and informational unlock hints.
- Each visible row should carry progress, what is already done when available, next-step guidance, and a location/turn-in hint in text.
- Show a compact empty state when no active/taken/claimable rows exist.
- Make `/quest` and the persistent `🗺️ Квести` button available outside Korchma for the overview, while keeping the physical `Стіл зі справами`, `v1:quest:list`, and Quest Hub archive behavior at the table.
- Keep the full Quest Hub table-gated: direct list/action callbacks outside Korchma still route through the existing `Квести видають усередині.` gate.
- Add the first current-life route quest, `Перший крок до столу`: enter Korchma, reach `Стіл зі справами`, then receive a replay-safe +1 XP completion and rewardless achievement.
- Keep overview buttons navigation-only: `📋 До Столу зі справами`, optional refresh, and hall/back navigation. The table button may move through the normal place route and complete the first route quest when the table is reached; do not add per-quest direct route buttons from overview rows.

## Non-goals

- No broad quest engine, journal archive, schema, migrations, public feed rows, combat rebalance, Mini App, or Yeger/Adventure/Daily reward changes.
- No new `/dev_*` helper: the slice adds no timer, cooldown, random offer, pending session, retry, once-per-period gate, or wait rule.
- No production flag: the slice changes only safe navigation/overview surfaces and a one-time current-life tutorial quest.

## Acceptance criteria

- `🗺️ Квести` and `/quest` show a useful active/taken/claimable overview from safe states inside or outside Korchma and do not stamp quest-table presence by themselves.
- Locked, retired, completed/reward-claimed and generic available untaken quest rows stay out of the overview.
- Empty overview states explain that new papers come from `Стіл зі справами`.
- No-character, active combat, turn-based duel, pending/active Big Barrel, active passage-search and stale callback blockers keep existing behavior.
- The first route quest appears as 0/2 before entering Korchma, 1/2 after entering, completes only when the character reaches `Стіл зі справами`, grants +1 XP once per current-life token, and resets after remort.
- Completing the first route quest unlocks `achievement.quest.first-korchma` once without gold, items, title power or combat power.
- Overview buttons are navigation-only; per-quest locations are mentioned in text rather than offered as direct route buttons.
- Quest Table and archive callbacks remain compatible.
- Overview text is Ukrainian, compact, HTML-escaped and under Telegram callback-size limits.

## Relevant files / search terms

- `src/bot/commands/questHubCommand.ts`
- `src/bot/modules/mainMenu.ts`
- `src/bot/modules/quest.ts`
- `src/bot/presenters/questOverviewPresenter.ts`
- `src/bot/keyboards/questOverviewKeyboard.ts`
- `questMarkerSnapshot`, `QuestHubSnapshot`, `mainMenuButtons.quest`, `v1:quest:overview`

## Focused tests

- `tests/bot/questOverviewPresenter.test.ts`
- `tests/bot/questOverviewKeyboard.test.ts`
- `tests/bot/questHubCommand.test.ts`
- `tests/bot/questHubKeyboard.test.ts`
- `tests/bot/questCallbackData.test.ts`
- `tests/bot/mainMenuKeyboard.test.ts`
- `tests/bot/sceneCallbackHtml.test.ts`
- `tests/bot/questButtonMarkers.test.ts`
- `tests/services/firstKorchmaQuestService.test.ts`
- `tests/services/achievementService.test.ts`
- `tests/content/achievements.test.ts`
- `npm run typecheck`
- `npm run check`

## Manual Telegram QA

1. New character outside Korchma: open `🗺️ Квести` or `/quest`; verify the overview opens instead of `Квести видають усередині.`, shows `Перший крок до столу` as 0/2, and does not mark table presence.
2. Press `🚪 Зайти в корчму` or route to the hall, then reopen `🗺️ Квести`; verify `Перший крок до столу` is 1/2 and points to `Стіл зі справами`.
3. Press `📋 До Столу зі справами`; verify normal place movement reaches the table, the first route quest completes once with +1 XP, and a duplicate refresh/table open does not pay again.
4. After remort/current-life reset, repeat the route and verify the first route quest can appear and complete again for the new life.
5. High-level character with completed starter quests: open `🗺️ Квести`; verify starter shawarma/fight rows are absent.
6. Character with no other active/taken quests after the first route quest is complete: verify the compact empty state points to `Стіл зі справами`.
7. Active Daily Korchma Round 1/2: verify the row appears with done step, next step and location/turn-in text, without a direct `До обходу` button.
8. Daily Korchma Round 2/2 turn-in-ready: verify the row appears as claimable and explains where to turn in.
9. Problem Quest in progress: verify the row appears with progress and turn-in guidance.
10. Problem Quest completed and reward claimed: verify the row disappears.
11. Yeger not started: verify no row.
12. Yeger in progress / claimable: verify the row appears with progress or turn-in guidance and no direct `До Єгеря` button.
13. Barrel Beer Tutorial completed, retired or merely available: verify no row; in progress / turn-in-ready still appears.
14. From the full Quest Hub, open the available Barrel Beer Tutorial paper; verify it shows a preview/confirmation card, does not grant the stipend or mark the quest accepted yet, and only `🛢️ Взяти записку` grants the accepted result with the 39-gold received line.
15. Verify the overview keyboard contains only `📋 До Столу зі справами`, optional `🔎 Оновити`, and `🍺 До зали` / back navigation.
16. Verify opening overview does not complete, claim, accept, start fights, start raids, move presence to `Стіл зі справами`, or expose per-quest route buttons such as `До Низу`, `До Єгеря`, `До бочки`, `До шинку`, `До льоху`, `До задвірка`, `До Корчмаря`, `До обходу`, or `До Трьох справ`.
17. Verify direct full Quest Hub list/action callbacks outside Korchma still use the existing outside gate.
18. Active solo combat, active turn-based duel, active/pending Big Barrel and active passage search: verify existing blockers win over overview.
19. Stale `v1:quest:overview` callback: verify it fails closed or routes to the current canonical overview without mutation.

Manual Telegram QA status for implementation: not run in Telegram.

## Release surfaces

- `package.json`
- `package-lock.json`
- `CHANGELOG.md`
- `news.md`
- `docs/tasks/README.md`
- `docs/tasks/0.3.4-quest-overview-route.md`
- `docs/ai/context.md`
- `docs/operations/playtesting.md`
