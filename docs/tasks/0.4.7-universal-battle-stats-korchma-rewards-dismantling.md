# 0.4.7 — Universal Battle Statistics, Richer Korchma Rounds, and Manatka Dismantling

Status: implementation complete in PR #196 from merged `0.4.6`
(`origin/main` at `6b5ab40a` when the task branch was created). Automated
validation is complete; Manual Telegram QA, merge and deployment remain
separate and unclaimed.

## Goal

Make truthful terminal battle statistics universal, turn the Daily Korchma
Round into a meaningful deterministic daily reward, and add a safe one-unit
forge dismantling sink for canonical equippable manatky.

## Scope

### Universal battle statistics

- Preserve GroupCombat as the golden eight-dimension implementation and extend
  the separate terminal-only `📊 Статистика` card to persistent solo PvE,
  Training Doppelgänger, PartyBoss/Big Barrel, turn duels, quick duels and the
  starter Mimic-Shawarma probe.
- Keep the canonical order: damage dealt, actual healing, actual prevention,
  successfully applied control, damage taken, committed actions, committed
  specials and committed defensive turns.
- Generalize the shared presenter for partial persisted evidence: a supported
  zero renders as `0`; an unprovable value renders as unavailable and is never
  inferred from prose, authored values, current state, hidden odds or truncated
  logs.
- Persist versioned authoritative accumulators for solo/Training and PartyBoss;
  derive duel facts only from immutable action ledgers; freeze the starter probe
  payload in `DailyAction.resultJson`; show only already-public quick-duel facts.
- Completed results, journals and statistics are unlisted capability-link
  artifacts: anyone holding the valid opaque battle/result token may read them
  from a private, group or supergroup card without being a participant or
  having a Character. These terminal reads are replay-stable, non-settling and
  strictly read-only. Active actions, inventories, hidden choices and mutable
  controls remain participant-authorized, and statistics remain unavailable
  until terminal state.
- Persistent solo, Training and starter Mimic produce compact versioned
  `ba1_{s|t|m}_<UUID>` Telegram URLs and consume them through the typed `/start`
  registry before onboarding or viewer authorization. Active, malformed,
  unknown, wrong-kind and unavailable tokens return one generic response.
- Every authoritative terminal result producer exposes that URL directly on the
  first/replayed card: interactive completion, command reopen, callback
  replacement, combat-lock recovery, timeout and passage-search schedulers, and
  adventure/Yeger delivery. The shared typed delivery-options contract emits
  exactly one share row only for a terminal valid UUID plus valid bot username;
  active cards and malformed inputs emit none.
- New solo/Training combat state and Mimic `DailyAction.resultJson` freeze a
  versioned minimum public identity (name, title, crest, level and race/class
  labels/IDs). Terminal rendering never joins the live mutable Character;
  pre-snapshot legacy rows use a generic stored-data-only fallback without
  read-time backfill.

### Daily Korchma Round rewards

- Preserve the Kyiv-calendar daily identity and XP formula
  `2 * level + deterministicRandom(1..level)`.
- Set gold to `13 + level + deterministicRandom(1..level)`, grant exactly
  `13 * item.iskrokamin` and `1 * item.dense-bandage`, and disable the old
  quest Iskrokamin bonus for this claim.
- Add a deterministic identity-manatka branch: `13%` at baseline LUCK, plus
  one percentage point per full LUCK above `6`, capped at `23%`. LUCK affects
  only whether the branch opens; canonical `tavern_event` candidate weights
  choose the item without a second LUCK modifier. Force a
  featured drop after six consecutive completed reward rows without one; missed
  calendar days do not affect the streak.
- Select only a base `+0` equippable candidate with positive current class,
  race, canonical-title affinity or an explicit matching hard requirement.
  Never grant an incompatible fallback; persist `no-candidate` evidence and
  still settle the base reward safely.
- Freeze rules version, roll/branch, pity decision, selected item, matched
  identity kind and actual applied grants in the stored result. Legacy rows
  replay without top-up; repository-applied grants remain authoritative.
- Preserve Daily Korchma offer/step/reward rows and bounded featured-drop
  history across remort so same-day and seven-completion guarantees cannot reset.
- Extend a non-production-only Korchma helper to exercise miss, hit and forced
  seventh-completion branches without production registration or mutation.

Expected gold ranges are `17..19` at level 3, `22..29` at level 8 and `27..39`
at level 13.

### Forge manatka dismantling

- Add `🔩 Розібрати манатку` inside `✨ Чароковальня`, reusing its current
  location, level/remort, field-kit, callback and return-navigation contracts.
- Present the action as `🔩 Розібрати манатку` in a dedicated first keyboard
  row before the sorting controls. Keep its icon distinct from the adjacent
  `♻️ До Дружньої Скрині` inventory action.
- Dismantle one owned canonical equippable weapon, armor or accessory per
  irreversible confirmation. Exclude resources, consumables, cosmetics, junk,
  unknown content, priceless/story/memory/sentimental/quest-critical items,
  equipped IDs, active reservations and protected last copies.
- Ordinary characters pay `5` gold. `class.mage`, `class.varenyk-mancer` and
  `class.bureaucramancer` pay `5` mana after canonical regeneration, with no
  gold fallback.
- Yield is
  `rarityBase + ceil(0.42 * nominalCumulativeIskrokaminUpgradeCost)`, where the
  rarity bases are common `1`, uncommon `2`, rare `3`, epic `5`, legendary `8`.
  Replay the real stepwise upgrade-cost function, including canonical set and
  legendary modifiers; item provenance does not affect yield.
- Paginate every eligible row with deterministic ordering. Preview exact item,
  level, quantity, yield, payment and resulting balance; preview reserves
  nothing.
- Confirmation uses one atomic transaction to recheck current life, access,
  regenerated resources, exact quantity, equipment/reservations, content/rules
  fingerprint, protection and yield, then writes one durable receipt, spends
  exactly once, removes one item and grants the frozen Iskrokamin output.
- Bind the compact opaque guard and receipt to remort life, item/base ID,
  upgrade, rarity/set fingerprint, expected quantity, yield, payment and rules
  version. Duplicate confirmation replays the receipt; concurrent last-unit
  confirmation has at most one success; old-life callbacks are stale.

Expected non-set yields:

| Base rarity | +0 | +1 | +2 | +3 | +4 | +5 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| common | 1 | 4 | 9 | 20 | 40 | 85 |
| uncommon | 2 | 5 | 11 | 21 | 42 | 86 |
| rare | 3 | 6 | 12 | 23 | 43 | 88 |
| epic | 5 | 8 | 15 | 27 | 49 | 97 |
| legendary | 8 | 13 | 24 | 43 | 77 | 153 |

## Integrity and balance decisions

- Statistics never affect rewards, settlement, quests, achievements, analytics
  or public activity. Existing GroupCombat and Big Barrel eligibility/economics
  remain unchanged.
- The featured-drop branch is `13%` at LUCK `6` or below and rises by one
  percentage point per full LUCK above `6`, capped at `23%`; the canonical
  candidate weights themselves remain LUCK-independent. The seven-completion
  guarantee is bounded by six prior rows and overrides the chance roll.
- Daily base grants and dismantling are exact-once under duplicate, concurrent,
  stale, restart and remort paths.
- Dismantling is a deterministic inventory/currency sink with no failure chance,
  bulk action, daily cap, buyback, provenance reconstruction or resale coupling.
- Every producer recognized by the shared active-reservation predicate was
  audited. Item-use preview/confirmation, gift creation/acceptance, postal draft
  confirmation, equipment writes, Mantok Chest creation/refresh/confirmation,
  Level Barter settlement and Shynok sale creation/refresh/confirmation use the
  same item-stack serialization seam with bounded retry and stable exhausted-
  contention outcomes. Restore-to-full is restricted to its canonical bandage,
  so it is domain-disjoint from dismantle-eligible gear, but it still uses the
  same protocol and stable `reserved` fallback.
- No new Chronicle event or achievement is added for statistics or dismantling:
  statistics are a read-only detail surface and dismantling is an irreversible
  inventory service whose repeated use should not create public spam. Existing
  shared inventory achievement behavior is preserved when applicable.
- The QA follow-up also closes an existing guild-safety gap: hub leave and
  disband buttons open version-bound yes/no confirmation cards, and neither the
  intent nor the negative path mutates membership. Literal legacy `l`/`z`
  callbacks are treated only as old intents; distinct `ly`/`zy` affirmative
  actions are the sole mutation paths. A repository-wide durable rule now
  requires the same fail-safe shape for difficult-to-reverse actions.
- Registered exclusive player-facing action icons use one semantic-owner
  registry. The scope test rejects reuse of those registered icons outside
  their named consumers and consults only validated admin/dev exceptions; no
  ordinary player-facing exception is present. The broader semantic-ownership
  rule remains normative, while automated completeness is intentionally not
  claimed for every Unicode icon in the repository.
- Review `📖 Перекази`; update lore only if the visible forge contract makes the
  current in-world text inaccurate. No quest-overview change is expected because
  no quest state or route changes.

## Non-goals

- Leaderboards, lifetime combat analytics, ranking/shaming, hidden-odds
  disclosure, FightService rewrite or item-instance provenance.
- New currency, resale integration, Korchmar recycling, bulk dismantling or
  unrelated Old Altar/guild work.
- Retroactive reward top-ups or reinterpretation of legacy combat evidence.

## Acceptance criteria

1. Every shipped terminal combat surface exposes truthful replay-stable
   statistics with missing-versus-zero semantics through an opaque capability
   token, without participant, Character or private-chat authorization. Every
   capability-link claim has a tested URL producer and matching `/start`
   consumer, and every authoritative terminal card producer exposes exactly one
   link directly without requiring a journal/statistics detour or forwarding.
2. Win/loss/flee/surrender/timeout/expiry/restart/stale/duplicate and legacy
   evidence paths do not mutate combat or rewards.
3. Daily level 3/8/13 rewards, exact item quantities, bounded 13..23% LUCK
   branch boundaries, hit/miss, forced seventh completion, reset after an
   actually applied featured grant, missed-day behavior and remort history are
   deterministic and exact-once.
4. All live class/race/title profiles have at least one identity-relevant
   candidate; corrupt no-candidate profiles settle safely and are observable.
5. Dismantling matches every formula row and canonical modifier path; payment,
   inventory decrement and Iskrokamin grant are atomic and replay-safe.
6. Every eligibility, equipped/reserved/protected-last-copy, stale fingerprint,
   quantity, life, access and resource branch is rechecked at confirmation.
7. Eligible rows are fully reachable through deterministic pagination; maximal
   names are HTML-safe and every callback/card remains within Telegram limits.
8. Selling, upgrading, equipment, chest runs, transfers, remort, rewards,
   public events and all existing combat settlement remain regression-covered.

## Relevant files / search terms

- `battleContributionPresenter`, GroupCombat terminal details and contribution
  state; persistent `CombatState`, Fight/Training repositories and callbacks.
- `PartyBoss`, Big Barrel contribution/settlement/presenter; duel action ledgers,
  quick-duel result details and starter Mimic `DailyAction.resultJson`.
- Korchma daily offer/step/reward claim, deterministic daily action repository,
  identity loot expansion, remort cleanup and dev reset handlers.
- Forge/field-kit routing, item self-upgrade service, canonical upgrade cost,
  equipment/inventory/reservation predicates and durable JSON receipt patterns.

## Focused tests

- Domain/presenter/repository/callback tests for all eight dimensions, partial
  evidence, terminal capability access, active-state denial, replay, legacy JSON
  and byte caps.
- Typed capability registry tests cover URL/payload round trips, the Telegram
  64-byte limit, strict UUID/kind validation and private/group/supergroup
  routing. Snapshot regressions mutate the current owner profile after combat
  and compare result, journal and statistics byte-for-byte while asserting zero
  public-read writes and no Character join.
- Production-shaped delivery tests cover completed/replayed Mimic, interactive
  and command-reopened solo/Training, both timeout completions, combat-lock
  recovery, passage-search scheduling, adventure complications and Yeger
  terminal delivery. Each emitted share URL round-trips through the typed `ba1`
  parser; active cards, absent usernames and malformed tokens expose no link.
- Daily reward golden ranges/quantities, candidate coverage, pity/remort/missed
  day behavior, legacy replay, duplicate/concurrent claims and production guard.
- Dismantling formula tables, modifiers, class payment matrix, transaction
  mutations, replay/concurrency/stale/protection/reservation/access branches,
  pagination/order/escaping and adjacent inventory-flow regressions.
- Production-shaped contention tests use two independent Prisma clients and the
  actual dismantling, equipment, item-use, transfer, Mantok Chest, Level Barter
  and Shynok repositories; core races do not mock the serialization helper or
  insert the competing reservation directly.
- Production-shaped guild callback routing proves that literal deployed `l`/`z`
  callbacks only open current confirmations, while version-bound `ly`/`zy`
  affirmatives perform the single leave/disband mutation. Icon scope tests pin
  registered exclusive owners, allowed consumers and real admin/dev exceptions.
- Canonical focused suites while iterating, then `npm run check`,
  `git diff --check` and merge-tree validation against refreshed `origin/main`.

Automated evidence on `12026-08-22`: the final cross-flow follow-up focused set
passed 245 tests across fourteen equipment, item-use, transfer, Mantok Chest,
Level Barter, Shynok and dismantling repository/service files. The complete
`npm run check` passed static/docs/build, 4704 unit tests and 768 integration
tests. On `12026-08-23`, the Forge-layout, icon-ownership, GroupCombat notice
and guild-confirmation follow-up passed 281 focused tests across twelve unit
and production-shaped integration files. The terminal-capability, literal
legacy-guild-callback and registered-icon re-review passed 816 focused tests
across thirteen unit and production-shaped integration files; the complete
canonical gate passed static/docs/build, 4716 unit tests and 770 integration
tests. Exact-head canonical check,
`git diff --check` and refreshed merge-tree evidence are recorded at PR handoff.
The capability-URL and frozen-identity follow-up passed 732 focused tests across
fifteen parser, routing, presenter, service, repository and state-contract files;
the complete canonical gate passed static/docs/build, 4732 unit tests and 770
integration tests. The terminal-delivery follow-up passed 574 focused tests
across twelve parser, command, scheduler, callback, runtime and read-only service
files; the complete canonical gate passed docs/static/build, 4749 unit tests and
770 integration tests. Manual Telegram QA remains separate and pending.

## Manual Telegram QA

Status: pending — not run in this implementation pass.

Use four isolated QA accounts. Account A starts at level 13 as a
non-magical class with Forge access, at least 100 gold, an eligible base `+0`
item, one upgraded item and one protected/equipped example. Account B starts at
level 13 as `class.mage` with Forge access, an eligible item and mana below full
with an old regeneration marker. Account C is the opponent/party participant;
Account D is a non-participant without a Character. A–C begin outside combat
with no stale active lease.

Set these exact non-secret values in the untracked root `.env` without changing
deploy defaults or secrets:

```env
NODE_ENV=development
DEV_GRANT_COMMANDS_ENABLED=true
PARTY_SESSION_FOUNDATION_ENABLED=true
BIG_BARREL_BROTHER_RAID_ENABLED=true
GROUP_COMBAT_PROOF_ENABLED=true
LEFT_PASSAGE_PARTY_ATTACK_ENABLED=true
GUILD_FOUNDATION_ENABLED=true
```

Run `refresh-local-bot.cmd`, then `status-local-bot.cmd`; verify the reported
snapshot SHA and the seven runtime values above before testing.

1. Finish and replay representative solo, Training, turn duel, quick duel,
   starter Mimic, PartyBoss and GroupCombat outcomes. Use each result card's
   shareable opaque Telegram URL with account D and open the result, `📜 Журнал` and
   `📊 Статистика` from private, group and supergroup contexts where Telegram
   permits callbacks. No Character or participant membership is required; all
   authoritative recorded combatant rows are visible, unavailable legacy fields
   remain `—`, and repeated reads change no rewards, HP, resources or state.
   Rename the owner and change title/crest/profile after completing solo,
   Training and Mimic; verify all three historical surfaces remain unchanged.
   Verify the share row appears immediately on first completion, command reopen,
   timeout/recovery replacement and adventure/Yeger handoff cards, exactly once
   per card and never on an active card.
   Verify that active cards still reject terminal statistics and that their
   actions, inventories and hidden choices remain participant-authorized.
2. On account A run `/dev_reset_korchma_round miss`, complete the round, then
   repeat with `hit` and `forced-pity`. Verify the planned/applied item, pity
   behavior and same-day replay; remort between a completed miss and the next
   round and verify the Korchma offer/step/reward history survives.
3. On account A open the Forge and verify `🔩 Розібрати манатку` is the first
   row, above both sorting controls, while inventory still shows the distinct
   `♻️ До Дружньої Скрині`. List, preview and confirm ordinary gold
   dismantling; retry the same confirmation and verify one durable result with
   no second spend or item removal. Reopen an old preview after
   equipping/reserving the item and verify rejection without payment.
4. On account B preview and confirm mana dismantling after passive regeneration;
   verify exactly 5 regenerated mana is spent, no gold fallback occurs and the
   preview/result balances agree.
5. Put account C in an active guild led by account A. Tap `🚪 Вийти`, verify the
   `✅ Так, вийти` / `❌ Ні, лишитися` confirmation, choose `Ні` and confirm the
   membership remains. Open it again, choose `Так` and confirm C leaves once.
   For a one-member guild, verify the leader's disband button likewise requires
   `✅ Так, розпустити` before deletion.

Keep this pending result separate from automated checks, merge, deployment and
production availability.

## Release surfaces

- Shift every still-unimplemented accepted task from current `0.4.7..0.4.14`
  to `0.4.8..0.4.15`, preserving order and live links while leaving shipped
  records/history unchanged.
- Package/lockfile, changelog, spoiler-light Ukrainian news, roadmap/task index,
  compact context and playtesting/developer notes are release surfaces for this
  PR. `📖 Перекази` now names confirmed Forge dismantling; the quest overview is
  unchanged because no quest route/state changed. Statistics and dismantling add
  no achievement or Chronicle event by the explicit anti-spam/read-only decision
  above. The PR body carries the same pending Manual Telegram QA contract.
