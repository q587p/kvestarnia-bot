# 0.3.13 — Location-Safe Quest and Combat Bugfixes

## Goal

Ship a patch-first release that preserves world location across Bard and quest-combat flows, makes every valid duel completion advance the Fighting Corner quest exactly once, and repairs the related encounter scaling and Telegram copy defects.

## Scope

- Keep Bard Performance at its starting location while retaining Shynok-only house behavior for performances already in Shynok.
- Carry normalized `User.lastSeenLocationId` through the Prisma daily-action claim into the Adventure result, then store and reuse it through combat start, redraw and terminal navigation. One shared Adventure callback presence policy treats `active-fight`, `combat-blocked` and `already-completed` as non-authoritative for `problem`, `problem-help` and final `approach`, so none can replace a durable combat location.
- Scale Adventure quest monsters to the current character level, then apply the canonical remort-aware combat derivation once in durable battle state.
- Count Quick Duel resolution at or after the persisted quest-acceptance timestamp while preserving shared replay-safe duel progress. Prisma coverage verifies the exact persisted equality boundary; the cause of a normal human-flow miss remains subject to Telegram QA.
- Render Doppelganger training comments as escaped HTML block quotes.
- State the full barrel-beer requirement consistently in the tutorial, clue and quest overview.

## Non-goals

- No new gameplay action, reward, economy formula, monster family, schema, migration, dependency, rollout flag or broad combat refactor.
- No change to ordinary Niz fight location or non-quest monster selection.
- No new achievement, lore entry or `/dev_*` helper: all changed surfaces and actions already exist, and the fixes add no new timed or mutable QA gate.

## Acceptance criteria

- Bard start and response callbacks never assign Shynok when the player began elsewhere.
- Quick and turn-based duel completions use the existing normalized quest-progress handler and remain exact-once on replay.
- Quest fights preserve front, yard and Quest Table origins through start, redraw, victory and flee navigation.
- Concurrent Adventure completion callbacks leave the winning Front or Yard fight presence authoritative when the loser reports `already-completed`, including when it discovers the committed claim during preflight before the winner starts the fight.
- Delayed or historical `already-completed` Adventure problem, help and approach callbacks preserve current presence; explicit quest navigation, ordinary selected problem/help navigation and newly completed non-fight Adventure results retain their existing Quest Table movement.
- `active-fight` and non-solo `combat-blocked` Adventure problem, help and approach callbacks perform no presence write.
- Adventure monsters use the current level; remort pressure is frozen once, does not stack, and its notice appears once at battle opening only when applicable.
- Doppelganger payloads contain a valid escaped `<blockquote>` under HTML parse mode.
- Before the Barrel round is offered, active instructions say to serve/buy beer for everyone and drink the player's own beer.
- After the Barrel round is offered but the player's mug remains undrunk, active instructions ask only to drink that mug.

## Relevant files / search terms

- `src/bot/presence/presenceRouting.ts`, `src/bot/modules/quest.ts`, `src/bot/keyboards/fightKeyboard.ts`
- `src/services/fightService.ts`, `src/services/fightingCornerQuestService.ts`
- `src/bot/presenters/*Presenter.ts`
- `v1:sh:bp`, `originLocationId`, `recordResolvedDuelSafely`, `deriveMonsterCombatStats`

## Focused tests

- Presence routing and Bard repository integration, including stored legacy location aliases.
- Prisma daily-action Adventure handoff, the shared callback presence-state matrix, delayed problem/help callbacks, both approach concurrency interleavings, persistent-fight presenter/keyboard and Fight service state.
- Real `DuelChallengeService` plus Prisma duel/daily-action repository wiring, including normal Quick Duel, pre-acceptance exclusion, Rogue retaliation and replay.
- Doppelganger command/presenter payloads and barrel/quest presenters.
- Standard `npm run check` plus focused combat simulation.

## Manual Telegram QA

Use [the compact 0.3.13 checklist](../qa/0.3.13-bugfix-release-qa.md). Refresh the isolated snapshot with `refresh-local-bot.cmd` only at the user's manual-test checkpoint.

## Release surfaces

- Bump `package.json` and `package-lock.json` to `0.3.13`.
- Update `CHANGELOG.md`, `news.md`, this task record, QA checklist and `docs/ai/context.md`.
- Achievements: unchanged because the patch adds no player-visible action or milestone.
- Quest overview: updated because the existing barrel quest instruction changes.
- Lore: reviewed; no update required because no location, concept or item behavior changes in canon.
