# 0.1.5 — Pre-duel Training Doppelganger Prep

Status: Shipped
Date: 12026-06-17
Source: `CHANGELOG.md`

This is a compact historical task record generated from the shipped changelog entry. It is not an active task unless a human explicitly reopens a follow-up.

## Goal

Ship `Pre-duel Training Doppelganger Prep` as version `0.1.5`.

## Scope summary

- **Added:** Added `/spar` and a Quest Hub `🥊 Бійцівський куток` entry for a level 3+ turn-based training fight against the `Сумлінний Допельґанґер`.
- **Added:** Added a training combat path that mirrors the current hero summary/equipment into a doppelganger enemy, uses `solo_combat_sessions` for turns and keeps PvP state out of scope.
- **Added:** Added `v1:spar:open` and `v1:spar:turn:{sessionId}:{turn}:{action}` callback parsing, presenter/keyboards and neutral-before-handler presence routing; successful training writes `location.korchma.quest_table` / `adventure.training-doppelganger` only after Barrel, interior and level gates pass.
- **Changed:** Mantok Chest auto-pick remains conservative, but manual selection can now explicitly include protected/priceless/story manatky with a visible `ручне переконання` warning and final confirmation; equipped stacks remain protected.

## Guardrails / non-goals

- Pending Barrel raids block the training surface before any training fight starts.
- Level 1-2 heroes see a friendly `/spar` gate and cannot create or continue training sessions; level 3 is the shared minimum planned for future duel prep unless a later PR changes it explicitly.
- Training can grant XP only: `1 XP` on loss, level-scaled win XP at roughly half of a similar-level monster reward with small luck/random upside, and no gold, items, manatky or quest progress.
- Repeat training is gated by a doppelganger recovery cooldown derived from the copy's remaining HP after the fight, not by a once-per-day card.
- No real duel invites, target player selection, wagers, gold/items/manatky rewards, new loot tables, group raids, trading, shops, crafting, Mini App work, schema changes or durable PvP ledger.

## Verification notes

- Detailed tests and implementation notes live in the original PR/history and relevant test files.
- Use focused tests around changed modules if reopening this area.
- Keep player-facing copy Ukrainian.

## If reopening a follow-up

```text
Use $kvestarnia-version-task.

Create a new task doc for the follow-up instead of editing this shipped record.
Start from this historical record and CHANGELOG.md entry for 0.1.5.
Context: docs/ai/context.md
Final output: changed files, behavior changed, tests run, risks, completion status. No tutorial.
```
