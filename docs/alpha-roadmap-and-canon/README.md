# 0.3.x Alpha Roadmap and Canon Audit

The latest imported snapshot lives in [`12026-07-10/`](12026-07-10/).
It audits `main` at `0.3.5` / `3c2c5945`, the open `0.3.6` release candidate, documentation canon, closed-alpha readiness, performance evidence, operations gates and the next planning horizon.

This package is planning and audit evidence, not implementation permission. Its task board, task drafts, checklists and Codex prompts stay package-local until a maintainer explicitly activates one bounded task against current `origin/main`.

## Current drift note

The snapshot predates the naming follow-up in the active `0.3.6` branch. References to `Особистий протокол 13-Б` describe the source snapshot; the current player-facing split is `📄 Форма 13-А` for filing and `Протокол 13-З` for protection. The canonical runtime contract remains [`docs/tasks/0.3.6-bureaucramancer-personal-protocol-13b.md`](../tasks/0.3.6-bureaucramancer-personal-protocol-13b.md).

## Package contents

- `00-README-UA.md` and `MANIFEST.json` describe the source snapshot and evidence limits.
- `analysis/` contains the executive, documentation, technical and risk audits.
- `roadmap/` contains the proposed `0.3.x` closed-alpha readiness line and later horizon.
- `task-board.md` and `task-board.json` hold the human- and machine-readable proposed queue.
- `tasks/`, `prompts/` and `checklists/` are inactive handoff artifacts.
- `docs/` contains the source-of-truth matrix and proposed repository change waves.
