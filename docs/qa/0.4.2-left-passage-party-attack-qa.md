# 0.4.2 Left-Passage Party Attack QA

Status: automated coverage passed on the final candidate tree. Manual Telegram
QA on the final exact head remains pending.

## Automated matrix

| Area | Required evidence |
| --- | --- |
| Entry | The party button appears only for the exact reserved `deep-left` preview and only when `LEFT_PASSAGE_PARTY_ATTACK_ENABLED=true`; solo attack remains until reservation. |
| Reservation | Duplicate create is idempotent; solo/create/start races have one winner; cancel and undersized expiry release the same primary without reroll. |
| Recruitment | 1–3 participants, exact three-minute due time, current-leader early start, current roster at due start, transfer/join/leave races and version-guarded invalid closure. The exact-location nearby view shows the live gathering to members and non-members; shared readiness actions use `✅ Готово` / `⏳ Зачекайте`. |
| Eligibility | Opening the exact preview writes the canonical `adventure.solo-fight` presence marker and must still allow reserve/join/start. Reject any other adventure, raid, active lease/combat, active search, wrong location, stale life, dead, truly invalid effective resources or expiry; never write a participant location. Level/equipment-scaled HP/mana above raw base columns remain valid. |
| Difficulty | Preserve exact primary; deterministic backups from reservation/party identity across restart; strongest non-leader pressure/remort from the frozen life only; roster-order ties; first-backup pressure cap 23; no solo-history writes; frozen replay. |
| Combat | Deterministic 1×1 through 3×6 scaling from immutable roster/reservation power, strict v3 callbacks/state, authored frozen monster specials/cooldowns, manual versus timeout guard, player and enemy contributions, opening roster/levels/tip, complete per-turn HP/mana/cooldown/effect journal, terminal/stale card parity and existing v1/v2 rewardless compatibility. |
| Settlement | Validate life/plan/receipt/owned lease before one-transaction HP/mana, consumable, XP/gold, item and activity effects; commit the exact receipt before releasing only that lease. Persist canonical frozen effective HP/mana exactly instead of truncating them to raw base maxima. Timeout-only participants receive no reward or activity. Injected failure at every stage remains retryable and cannot block or duplicate another participant. |
| Achievement decision | `0.4.2` defines and emits no left-passage completion achievement/event. GroupCombat settlement, repair and scheduling have no effect fields, selector, projector or ownership reconciliation; Chronicle activity remains independent. |
| Integrity repair | Exact v3 location, 1–3 participant / 1–6 enemy cardinality and power formula, ability loadouts, pressure/remort source, backup adjustment, cap, reward, common-loot, terminal, plan and receipt semantics fail closed. Active and terminal untrustworthy v3 rows retain resources, raw actions/receipts and leases under operator repair; initial reads, final reloads and every action/timeout/settlement/delivery CAS exclude them. Barrier races prove quarantine-first operations are inert, while an ordinary winner remains inspectable only until later quarantine. Thirteen older quarantined rows cannot starve healthy work, repeated ticks are mutation/log silent, explicit inspection remains available and v1/v2 rewardless repair is unchanged. |
| Rollout | Flag-off rejects new create/start while active timeout, repair, delivery and unfinished settlement continue. |
| Regression | Ordinary PvE, solo left passage, Training Doppelgänger, duels and Big Barrel suites remain unchanged; state/card/callback/query budgets stay bounded. |

Automated result:

- Focused GroupCombat, party-session and achievement-catalog regression
  coverage passed: **17 unit/service/bot/scheduler/catalog files / 277 tests**
  and **2 repository integration files / 134 tests**. Cross-combat regression
  coverage passed at **8 unit files / 310 tests** and **2 duel/Big Barrel
  integration files / 103 tests**.
- Full repository coverage passed at **331 files / 4,108 unit tests** and
  **27 files / 641 integration tests**.
- `npm run db:validate`, `npm run db:generate`, `npm run simulate:combat`,
  `npm run check` and `npm run check:docs` passed; docs reported **416 / 249**.
- The retained proof simulator still covers **24/24** deterministic 2×2/3×3
  cases. Production repository/domain coverage separately proves 1×1, 2×2,
  3×3 and 3×6 restart plus a complete 25-turn 3×6 journal.
- Production start measured **39** query events. Due, delivery and
  pending-settlement scans measured **1/1** each, receipt-aware idle repair
  **6/6**, participant settlement **18/20**, and manual start, due start,
  queue and single-resolve observations measured **33/33**, **32/32**,
  **21/21** and **23/35**.
- A complete 25-turn 3×6 production journal measured **45,700/65,536** state
  bytes. Initial 3×6 state/card measured **6,738/65,536** and
  **1,568/4,096**; the final 2×2 repository run measured **3,824/65,536**
  initial-state and **1,322/4,096** terminal-card bytes. The retained proof
  simulator maximum was **19,578/65,536** and the longest rendered action
  callback was **46/64** bytes.

## Manual three-account QA

Use the isolated local runtime only. Before opening Telegram:

1. In the untracked checkout `.env`, set `NODE_ENV=development`,
   `LEFT_PASSAGE_PARTY_ATTACK_ENABLED=true` and
   `DEV_GRANT_COMMANDS_ENABLED=true`.
2. Run `refresh-local-bot.cmd`, then `status-local-bot.cmd`.
3. Confirm the runtime reports the exact candidate SHA and its copied `.env`
   contains `LEFT_PASSAGE_PARTY_ATTACK_ENABLED=true`. A checkout-only flag
   change does not update an already-running snapshot.
4. Prepare three local test accounts at the left-passage prerequisites; use the
   promoted `/dev_add_level` shortcut from `/dev_help` where useful.

Record the exact git SHA, runtime snapshot and three test account IDs; do not
reuse production data.

1. Produce the hard left-passage preview. Confirm `⚔️ Атакувати` and
   `🤝 Покликати в атаку` are both visible and no other preview/menu gets the
   party action. Press `🤝 Покликати в атаку` first, without using
   `⚔️ Атакувати`; reservation must succeed despite the preview's canonical
   solo-fight presence marker.
2. Create a gathering, join once by deep link and once through nearby players.
   Confirm all accounts stay at the exact left-passage location.
3. Run a one-account automatic start, a two-account early start and a
   three-account automatic start. Confirm the current leader controls early
   start, `✅ Готово` / `⏳ Зачекайте` update every recruiting card and the due
   start uses the roster visible at the boundary.
4. Repeat around the deadline with leader transfer, a late join and a leave.
   Confirm a departed initiator cannot solo the reserved preview and receives no
   combat card, resource use or reward.
5. Arrange 1×1, ordinary 2×2/3×3 and strong 3×6 starts, including strongest
   recent Низ pressure and strongest remort on non-leaders. Record the frozen
   difficulty/ability snapshot and confirm restart does not change primary,
   backups or monster specials.
6. Exercise cancellation, undersized expiry, a participant timeout, restart
   during combat and restart while one participant settlement is unfinished.
   Confirm the opening card lists both rosters/levels and one tip, live cards
   expose cooldowns and effect durations, monsters use named specials, terminal
   cards include monster contributions and every journal page retains that
   turn's HP/mana, cooldowns and effects.
   Confirm exactly-once resources/rewards and lease release only after each
   durable receipt. Confirm Chronicle activity appears only for the eligible
   manual victory and that no left-passage completion achievement unlocks or
   progresses. Use `/dev_group_combat_timeout <party-token>` to resolve one
   active timeout boundary through the normal service path.
7. Disable the rollout flag during an active fight. Confirm new invitations and
   starts fail while the active fight, delivery and settlement finish.

Manual result: **pending**. Production enablement, merge and deployment are
outside this QA run.
