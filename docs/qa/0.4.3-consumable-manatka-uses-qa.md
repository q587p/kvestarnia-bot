# 0.4.3 Consumable Manatka Uses QA

Status: automated coverage passed on the candidate tree. Manual Telegram QA on
the final exact head remains pending. This document records the required live
matrix; it is not production rollout evidence.

## Setup

- Isolated non-production bot refreshed with `refresh-local-bot.cmd`; record
  the snapshot SHA from `status-local-bot.cmd`.
- Set `CONSUMABLE_MANATKA_USES_ENABLED=true` and
  `DEV_GRANT_COMMANDS_ENABLED=true`, plus the existing non-secret flags needed
  for the selected Party Boss and GroupCombat entry surfaces.
- Use one account for inventory and ordinary single-/multi-enemy combat, and
  three accounts for party surfaces.
- Grant two of each exact catalog id with
  `/dev_add_item 2 itemId=<exact-id>`. Use `/dev_heal` and
  `/dev_restore_mana` to prepare partial and full resources.

## Required Telegram matrix

| Area | Steps and expected visible result |
| --- | --- |
| Catalog and reachability | Inspect every one of the twenty Ukrainian item cards. Amounts, locations and icons match the task catalog. Every eligible GroupCombat row is reachable across five-row pages; no item disappears through truncation. |
| Exact common heals | Use `c001`, `c003` and `c009` outside combat and in ordinary single-/multi-enemy combat. They restore exactly `7/8/9 HP` up to the effective maximum. Full HP consumes no item and no combat turn. |
| Resource and random effects | `c014` changes each missing resource independently. `c010` is unavailable above `50% HP`. Bottle, `c007`, `c011` and `c015` retain their one resolved useful outcome on replay; cancel/reopen at unchanged stack/life cannot reroll it. |
| Solo utility | With a live positive cooldown and surviving enemy, verify `c005` reduces owner cooldowns by one, `c006` reduces only the nearest response by `42%`, `c008` deals fixed `13` damage without critical scaling, and `c013` evades only that response. The journal repeats the exact committed result. |
| Pair and party | In two-/three-account Party Boss and GroupCombat, `c002` heals the owner and most injured living ally by up to `8`; `c012` heals each living participant by up to `13`; `c004` appears only in GroupCombat with a removable harmful owner status and removes one. |
| Exact-once races | Double-tap inventory confirm and combat actions; overlap an item action with timeout/restart and another queued action. Exactly one effect and decrement land, and terminal/replayed cards do not repeat either. |
| Reservations and remort | Race preview/use against gift, mail, chest, barter, Shynok sale and a second preview for the same stack. Only one destructive owner wins. Remort invalidates an old preview and carries only explicitly selected item ids under the existing policy. |
| Quest bottle | Before the cellar grown-up ending, the current-life bottle is absent/rejected on every use surface and remains available to `turn-in`. After `keep`, it becomes usable. If practical, carry the kept bottle through remort and verify the new life is not permanently locked. |
| Rollout boundary | With `CONSUMABLE_MANATKA_USES_ENABLED=false`, all seventeen new nonmedical use controls disappear/reject while ordinary bandage, dense bandage and Field Kit still work. Turn-based duels expose no item action and no take-away purchase shelf exists. |

## Evidence to retain

- Final commit SHA, local snapshot SHA and effective non-secret flags.
- One screenshot/card pair for each location family and the exact `7/8/9`
  deltas; pagination first/last page; flag-off medical/nonmedical contrast.
- Before/after inventory/resource state for duplicate, reservation, quest-bottle
  and remort cases.
- Account count, starting states, pass/fail result and any blocker.

Manual Telegram QA status: pending. Production enablement, deployment and merge
remain outside this task.
