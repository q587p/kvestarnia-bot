# 0.3.13 Bugfix Release — Telegram QA

Automated tests cover the deterministic service, routing, presenter and callback contracts. The checks below remain manual until run against the isolated Telegram bot.

## Preconditions

- Refresh the isolated snapshot with `refresh-local-bot.cmd` only when manual testing begins.
- Use one Bard outside Shynok, two level 3+ characters with the Fighting Corner quest, and quest-capable characters at remort `0` and above the class's free remort ranks.
- Record the character's location before each Bard or quest-fight scenario.

## Compact matrix

| Flow | Action | Expected Telegram response | Expected state / replay result |
| --- | --- | --- | --- |
| Bard outside Shynok | Start a performance, refresh/respond, then finish or decline | Performance cards and callbacks work normally | Stored location never changes before, during or after the flow |
| Bard in Shynok | Complete a performance | Existing Shynok-specific house behavior remains visible where applicable | Location remains Shynok |
| Normal Quick Duel after quest acceptance | Accept the quest for both eligible characters, resolve one ordinary Quick Duel, then reopen/replay its terminal result | Both eligible participants receive Quick Duel progress | Exactly one progress row per participant; terminal/result replay adds none |
| Quick Duel exclusions | Complete a duel before quest acceptance; separately use a Rogue-retaliation duel | Neither result advances the quest | No progress row appears, including after result replay |
| Turn duel quest | Complete a valid turn duel and replay its terminal callback | Existing quest progress remains intact | One progress record only |
| Adventure fight — Korchma Front | Start a complication at the Front, including rapid duplicate completion callbacks; redraw, act, flee, then repeat and win | Whether the loser discovers the existing claim during preflight or inside the atomic claim, its `already-completed` result cannot move presence; every active/result card remains at the Front and terminal navigation returns there | `User.lastSeenLocationId` and durable `originLocationId` remain the Front throughout |
| Adventure fight — Yard | Start a complication in the Yard, including rapid duplicate completion callbacks; redraw, act, flee, then repeat and win | Whether the loser discovers the existing claim during preflight or inside the atomic claim, its `already-completed` result cannot move presence; every active/result card remains in the Yard and terminal navigation returns there | `User.lastSeenLocationId` and durable `originLocationId` remain the Yard throughout |
| Adventure callback under non-solo combat | While a turn duel, raid or another non-solo combat lease is active, press old problem, problem-help and approach callbacks | Each callback shows the existing combat blocker | No callback writes location, current raid or current adventure presence |
| Delayed or historical completed Adventure callback | After another callback starts a Front or Yard fight, let an old problem, problem-help or approach callback return `already-completed` | The existing completed-state card may be shown | Current fight presence remains unchanged; explicit `/adventure` or Quest Table navigation still moves through its ordinary route |
| Ordinary Adventure navigation | Select a current problem, open its help, then complete a non-fight approach | Current problem/help/result cards render normally | Problem, help and the new non-fight result move to the Quest Table as before |
| Ordinary Niz fight | Start and finish a normal Niz fight | Existing Niz cards and navigation remain unchanged | Origin remains the Niz layer |
| Quest level scaling | Start equivalent quest encounters at low and higher levels | Ordinary cards do not expose hidden formulas | Stored monster level follows the current character level |
| Quest remort pressure | Compare remort `0` with a remort count above the free ranks; retry/redraw the opening and a turn callback | Only the adjusted battle opens with `🧿 Відплата за минулі пригоди: монстр бʼється з поправкою на ремортну памʼять.` | Stored stats remain identical on replay; the line is sent once and does not recur on cards |
| Doppelganger quote | Train with a dynamic name containing `<`, `&` or `>` | The comment is visibly a block quote, with valid italics if retained | Telegram payload uses HTML parse mode, one `<blockquote>`, and escaped dynamic text |
| Barrel quest — before the round | Open the active quest, clue/tutorial and `🗺️ Квести` after the raid but before offering beer | Each surface says to serve/buy beer for everyone and drink the player's own beer | Neither required action is left implicit behind «пінна формальність» |
| Barrel quest — after the round | Offer/buy beer for everyone without drinking the player's mug, then reopen the active quest and `🗺️ Квести` | Both active rows instruct only to drink the player's own beer | The completed serve/buy-everyone step is not requested again |
| Quest checklist — Fighting Corner | Open `🗺️ Квести` at 0/3, after each objective in a mixed order, and at 3/3 | The row always shows training, Quick Duel and turn duel; each completed objective is crossed out and each unfinished objective remains plain | At 3/3 all three are crossed out and the separate Quest Table claim guidance remains visible |
| Quest checklist — Korchma round | Accept today's round, open `🗺️ Квести` before a scene, after one scene, and after the second scene | The row says to settle two of three and lists the three actual offered locations; completed locations are crossed out one by one | The uncompleted/omitted third location remains plain, and 2/2 still points turn-in to the Quest Table |

## Regression smoke

- Open Shynok, the Niz, Training, Quick Duel and turn-duel surfaces after the focused cases.
- Confirm stale or duplicate callbacks answer without duplicate quest progress, remort scaling, notices or presence writes from non-authoritative Adventure callback states.
- Inspect bot logs for Telegram parse errors and database errors; inspect the relevant character location and quest action rows if any visible state differs.

## Result record

- Automated: recorded in the PR and release handoff.
- Manual isolated Telegram run: pending; do not mark it passed without executing the matrix.
