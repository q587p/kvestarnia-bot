# Varenyk-mancer Sated Support — Telegram QA

Status: in progress. The caller-owned local dev-reset path, maximum-rank self-preview/activation mechanics and binding, full-HP/no-immediate-mana behavior and expired-preview rejection passed on checkpoint `28a3120`; the newly approved `або кожен хід в бою (це забирає хвилину дії)` preview and success/status copy passed on checkpoint `3a6e3dd6`. Checkpoint `35fa35bc` exposed wall-clock double consumption during Fight. Checkpoint `74d39a5c` passed the corrected dev reset, preview and fresh activation, but exposed an initial-turn freeze defect: an incomplete outside minute reduced `13` available combat turns to `12`. Checkpoint `5b252afb` exposed that Big Barrel and stored turn-duel journals did not own a per-round Sated snapshot. Automated follow-up now covers exact release duration rebasing, missing/malformed solo and duel cleanup, terminal expiry, Training journal heading order, same-named raid participants and resolved-duel journal persistence. This is not Telegram PASS evidence: the cursor-based freeze, release and stored-journal corrections still require a refreshed Telegram retest; initial `13 → 12`, target-list/combat/recovery compaction, duplicate/minute-late confirmation, other-recipient isolation and replacement/rollback/pair-wait/replay behavior remain pending.

Automated presentation coverage now also pins the outside-combat Sated duration and recovery explanation to one em-dash line. This is not fresh Telegram PASS evidence; the latest-head card still needs the pending Telegram retest.

## Execution log

- `12026-07-15`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `1d69cef3a57a268a91793387799576cb38ca1ceb`: Kyjivan BooksDragon completed a fresh internal-rank-1 self-feed for 8 mana at `20:51:27 Europe/Kyiv`. The durable receipt recorded the then-candidate immediate `+1 mana`, one activation and the canonical 13/93-minute timestamps, while the visible result compressed status/recovery/cost into one block and exposed internal rank. Product review clarified that rank must never be player-facing, mana must not return immediately, the buff row must explain its minute/own-turn effect, and lazy Hero recovery must arrive as a separate message. Result: **failed — presentation/mechanics; corrected checkpoint retest pending**.
- `12026-07-15`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `a1e1fbb8eef6339c8b307b8ccbb8e7ccd0da5f00`: a fresh feed was followed by a real `Спуск` Fight against `Павук дедлайнів`. Telegram showed the stored recovery effect on resources but omitted active `😋 Ситий` from the fight card and journal. Isolated SQLite evidence showed that the pending-passage session `e0e32a43-a48e-4e08-8d1e-e357f22a1b02` was created without `varenykSated`, while the following Training session froze the same activation and stored two distinct pulse ids. Root cause: pending-passage first/restart session creation bypassed the shared solo Sated freeze. Product review also approved one minute of Sated duration consumed by every fresh combat pulse. Result: **failed — combat start/presentation; corrected checkpoint retest pending**.
- `12026-07-15`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `7e2dc2a4546b9fd7d14e4944bdde16e7aa555bb1`: `/dev_reset_varenyk_sated` removed the status/wait, but Hero and `Хто поруч` hid feeding behind an alleged active adventure. SQLite showed no Sated row, no combat lease and only won Fight/Training sessions, while presence still held `adventure.training-doppelganger`. Result: **failed — stale terminal presence marker; corrected checkpoint retest pending**.
- `12026-07-16`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `88953763604a5bbd782e88f1aa893b3e90d78c9a`: other-player feeding exposed inconsistent spacing, unbolded names/values and `Баф` wording; combat cards/journals showed minutes instead of remaining combat turns, and Big Barrel recovery did not name its recipient. Clearing the feeder's wait still left an active target non-actionable because the implementation treated 93 minutes as recipient-global. Product review clarified an actor-recipient pair wait: another Varenyk-mancer may replace an active status, while concurrent previews of the same observed activation still produce one winner. Result: **failed — presentation/personal-wait contract; corrected checkpoint retest pending**.
- `12026-07-16`, automated review of checkpoint `b22a49704b0c6dd8d62d5393851519ab400c78dc`: a minute-late stale confirmation could commit the winner's pending Sated settlement before returning `stale`; Hero tied the viewer's self wait to the current status caster; replay kept the immutable original wait after an explicit pair-row shortening/clear; copy said an arbitrary combat turn and called all resources full when only immediate HP was checked. Result: **failed — transaction/pair/replay/copy contract; corrected checkpoint retest pending**.
- `12026-07-16 13:26:28 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `aaa38867bca20165e98849b3904a2954cd6f2977`: prepared Shannar de Kassal as a level 13 Varenyk-mancer with no equipment, full `68/68` effective HP, effective `INT 18 / CHA 9`, and enough mana for the maximum internal plan, then opened `/hero` → `🍽️ Нагодувати себе` and stopped before confirmation. Telegram rendered separate target, effect/timing and closing paragraphs; bold target/numeric values; exact `23`-mana price; immediate cap `+7 HP`; then-current delayed `+1 HP / +1 mana`; own-turn wording; the one-minute combat duration cost; and `13 / 93` minute timers without exposing rank. The isolated preview row stored `statRank: 5`, exact plan `{ rank: 5, manaCost: 23, immediateHp: 7, immediateMana: 0 }`, effective `INT 18 / CHA 9 / level 13`, full authoritative `68 HP / 34 mana` planning snapshots and no attuned rows. No recipient status, pair wait or Sated achievement existed, and the managed runtime log remained error-free. Product review then rejected fixed `+1/+1` sustain and approved rank-scaled portions `1/1`, `2/1`, `2/2`, `3/2`, `3/3`. Result: **historical presentation evidence only — superseded by the approved recovery scaling and not a PASS for the next checkpoint**.
- `12026-07-16 14:06:44 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `5665d453f5927f812e790a92913a432951b1cadc`: Shannar de Kassal reopened `/hero` → `🍽️ Нагодувати себе` at full effective `68/68 HP` and `34/34 mana`. Telegram showed the exact `23`-mana price, immediate cap `+7 HP`, rank-scaled delayed `+3 HP / +3 mana`, own-turn wording, one-minute combat duration cost and `13 / 93` minute timers without exposing rank. The preview row stored `statRank: 5`, exact plan `{ rank: 5, manaCost: 23, immediateHp: 7, immediateMana: 0 }`, effective `INT 18 / CHA 9 / level 13`, no attuned rows, and no recipient status or pair wait. The user rejected both the label punctuation `Одразу:` / `Далі:` and the long parenthetical, asking to restore the earlier approved compact `(кожен забирає хвилину дії)`. Result: **failed — mechanics and hidden-rank values correct; compact preview/status copy correction and retest pending**.
- `12026-07-16 14:08:12–14:09:58 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `5665d453f5927f812e790a92913a432951b1cadc`: after Shannar de Kassal fed Kyjivan BooksDragon and reopened `🍽️ Нагодувати`, Telegram rendered the self status/wait and target status/wait as four consecutive repetitive rows with no recipient grouping; the target button said `Kyjivan BooksDragon — оновити Ситого`. The screenshot showed the self `+3 HP / +3 mana` effect and target `+3 HP / +2 mana` effect, but ownership was hard to scan. SQLite stored Kyjivan's fresh activation at `14:08:12.815`, internal rank `4`, cost `20`, expiry `14:21:12.815`, cursor `14:09:12.815` and no immediate mana recovery; the later status settlement updated at `14:09:58.239`. Result: **failed — recipient grouping, line separation and `оновити стан` copy retest pending**.
- `12026-07-16 14:10:19–14:13:13 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `5665d453f5927f812e790a92913a432951b1cadc`: Shannar de Kassal activated internal rank `2` Sated, entered a persistent Fight against `Мідний полоз скарбової вентиляції`, committed turn 2 and opened journal entry 2. Telegram showed `60/68 HP`, `3/34 mana`, `11 ходів`, then only `+1 мани` in the recovery line; the journal's `Кулдауни та ефекти` omitted the historical Sated state. SQLite proved turn 1 began the pulse at full `68/68 HP`, so it stored `0 HP / +1 mana`; on turn 2 the pulse again stored `0 HP / +1 mana` before the monster's `8` damage, ending at `60 HP / 3 mana`. The bounded turn-2 log had recovery and resource evidence but no Sated snapshot, while the final recipient row recorded rank `2`, cost `12`, activation start `14:10:19.413` and lease-shortened expiry/cursor `14:13:13.641`. Product review rejected the pre-response ordering as inconsistent with `після власного ходу в бою`, required recovery after the complete hostile response without reviving `0 HP`, and requested `😋 <name>: ситість відновлює …`. Result: **failed — post-response ordering, historical journal status and recovery-copy retest pending**.

- `12026-07-16 14:50:59–14:51:30 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `28a3120aa0ba85f82affad9aad0f13ca3a506eaa`, snapshot `508dc40de4360bebdc4c1fbf370b238cf7691c21a7b2050b60b1fee6babd8cf4`: before the action, Shannar de Kassal had the expired current-life recipient row plus the actor-owned self-pair wait from activation `573f7ea6-3cb8-459e-9454-24206d5f3c85`, and no active combat lease. The user sent `/dev_reset_varenyk_sated`; Telegram replied `🍽️ Ситість скинуто` and `Поточний стан і пауза цієї життєвої спроби прибрані.` Follow-up isolated SQLite inspection found zero caller cooldown rows matching the Sated recipient key or pair prefix and still no combat lease. No other recipient status row existed at this checkpoint, so the guarantee that reset preserves another recipient's active status was not exercised. Result: **PASS — caller status and actor-owned pair waits cleared; other-recipient isolation still pending**.

- `12026-07-16 14:54:37 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `28a3120aa0ba85f82affad9aad0f13ca3a506eaa`, snapshot `508dc40de4360bebdc4c1fbf370b238cf7691c21a7b2050b60b1fee6babd8cf4`: after the dev reset, Shannar de Kassal had full effective `68/68 HP` and `34/34 mana`, then opened `/hero` → `🍽️ Нагодувати себе` and stopped before confirmation. Telegram rendered the self target, bold dynamic values, exact `23`-mana price, immediate cap `+7 HP`, delayed `+3 HP / +3 mana`, `13 / 93` minute timers, compact `(кожен забирає хвилину дії)` wording and recovery after `завершення бойового обміну`; it exposed no internal rank and no stale colons after `Одразу` or `Далі`. Isolated SQLite stored only preview token `tzw3loig57` with internal `statRank: 5`, exact plan `{ rank: 5, manaCost: 23, immediateHp: 7, immediateMana: 0 }`, effective `INT 18 / CHA 9 / level 13`, full authoritative `68 HP / 34 mana` planning snapshots and no equipment, attunement or recovery windows. The preview did not mutate Character resources or create a recipient status/pair wait. Result: **PASS — maximum-rank self-preview presentation, hidden-rank contract and fresh server-owned binding; confirmation still pending**.

- `12026-07-16 15:27–15:28 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `28a3120aa0ba85f82affad9aad0f13ca3a506eaa`, snapshot `508dc40de4360bebdc4c1fbf370b238cf7691c21a7b2050b60b1fee6babd8cf4`: the user pressed `🍽️ Нагодувати` on the earlier self-preview card after its server-owned preview had expired at `15:07:37.503 Europe/Kyiv`. Telegram replaced the card with `🍽️ Годування не склалося` and `Кнопка застаріла. Оновіть «Хто поруч» і спробуйте з нового рядка.` Isolated SQLite inspection at `15:28:31.241` found the expired preview token unchanged, Character still at effective `68/68 HP` and `34/34 mana` with no Character update since the dev reset, and no recipient status or pair-wait row. Result: **PASS — expired stale confirmation is rejected without spend, recovery or status mutation; duplicate and one-minute-late confirmation remain pending**.

- `12026-07-16 15:30:45–15:31:13 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `28a3120aa0ba85f82affad9aad0f13ca3a506eaa`, snapshot `508dc40de4360bebdc4c1fbf370b238cf7691c21a7b2050b60b1fee6babd8cf4`: Shannar de Kassal opened a fresh self preview and confirmed it immediately. Telegram rendered `😋 Ситий`, the full-HP branch `HP уже повне, зате стан акуратно загорнутий.`, delayed `+3 HP / +3 мани`, completed-combat-exchange timing, compact duration-cost wording, `13 хв`, the actor-owned `93 хвилини` wait and `Мани витрачено: 23`, without exposing rank or claiming immediate mana recovery. Isolated SQLite stored Character at `68 HP / 11 mana`, deleted the preview, and created exactly one rank-`5` recipient status plus one self pair-wait for activation `88781f09-6b14-436e-8ded-76ca3c7b1804`: start/cursor `15:30:45.344`, status expiry `15:43:45.344` and pair availability `17:03:45.344 Europe/Kyiv`. The receipt recorded cost `23`, `immediateHpRestored: 0`, `immediateManaRestored: 0`, `actorManaAfter: 11`, `targetHpAfter: 68` and `targetManaAfter: 11`; no HP-recovery notification or duplicate achievement grant was created. Result: **PASS — fresh maximum-rank self activation, full-HP/partial-mana copy, exact spend, no immediate mana recovery, hidden rank and one status/pair row**.

- `12026-07-16 15:33 Europe/Kyiv`, checkpoint `28a3120aa0ba85f82affad9aad0f13ca3a506eaa`: product review rejected the combined `або після завершення бойового обміну (кожен забирає хвилину дії)` player copy as awkward and approved the shorter `або кожен хід в бою (це забирає хвилину дії)`. The runtime ordering remains recovery after the complete hostile response/exchange; only its player-facing explanation changes. Result: **copy correction required — the preceding preview/activation mechanics remain observed, but presentation must be retested on the refreshed checkpoint**.

- `12026-07-16 15:55:42–15:56:06 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `3a6e3dd6ace2fcb1de06ee6ab9e1bfe3e547ef34`, snapshot `9898fe0ec46d5fa512c7b3a879328335de33f807a752adee8379a0f394556e14`: after the requested local dev reset, Shannar de Kassal opened `/hero` → `🍽️ Нагодувати себе` and stopped before confirmation. Telegram rendered the approved `Далі +3 HP і +3 мани щохвилини поза боєм або кожен хід в бою (це забирає хвилину дії).` copy with natural mobile wrapping, bold dynamic values, exact `23`-mana price, immediate cap `+7 HP`, `13 / 93` minute timers and no visible rank. Isolated SQLite inspection found Character at effective `68/68 HP` and `34/34 mana`, no recipient status or pair-wait row, and one fresh preview token `wvuehulz22` with internal `statRank: 5`, exact plan `{ rank: 5, manaCost: 23, immediateHp: 7, immediateMana: 0 }`, full authoritative planning snapshots and expiry `16:08:46.096 Europe/Kyiv`. Result: **PASS — corrected maximum-rank preview copy and fresh server-owned binding; success/status rendering on this checkpoint still pending**.

- `12026-07-16 16:06:03–16:06:23 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `3a6e3dd6ace2fcb1de06ee6ab9e1bfe3e547ef34`, snapshot `9898fe0ec46d5fa512c7b3a879328335de33f807a752adee8379a0f394556e14`: the user confirmed preview `wvuehulz22` before its expiry; the edited Telegram card retained its original `15:55` message timestamp. Telegram rendered the full-HP branch, approved `щохвилини поза боєм або кожен хід в бою (це забирає хвилину дії)` status copy, exact `+3 HP / +3 мани`, `13 / 93` timers and `Мани витрачено: 23` without visible rank or an immediate mana-recovery claim. SQLite stored one rank-`5` recipient status and one self pair-wait for activation `6892f8eb-6df0-4a9d-af17-f57f91b8c73a`, with start/cursor `16:06:03.930`, status expiry `16:19:03.930` and pair availability `17:39:03.930 Europe/Kyiv`. The receipt recorded `immediateHpRestored: 0`, `immediateManaRestored: 0` and post-cost mana `11`; a separate ordinary passive regeneration advanced mana to `12` at `16:06:15.842` while the Sated cursor stayed at activation, proving it was not an immediate Sated pulse. No HP-recovery notification or duplicate Sated achievement was created. Result: **PASS — corrected success/status presentation, exact cost, no immediate Sated mana recovery, hidden rank and one current status/pair row**.

- `12026-07-16 16:06:15–16:12 Europe/Kyiv`, checkpoint `3a6e3dd6ace2fcb1de06ee6ab9e1bfe3e547ef34`: after the user pressed `🔄 Оновити`, Telegram rendered the self target as four separate status/wait lines despite only one recipient: heading, full effect explanation and repeat wait. SQLite showed no Sated pulse yet (`cursorAt` remained `16:06:03.930`); the visible `12/34` mana came from ordinary passive regeneration. Product review rejected the repeated target-list explanation, the similarly long combat status, and bold combat-recovery names/values. Approved compact forms are one target-list line `Ви/ім’я, ситий ще N хв (+H HP / +M мани). Знову через O хвилин(и).`, one combat line `😋 Стан: Ситий ще N ходів (+H HP / +M мани)`, and plain `😋 <name>: <i>ситість</i> відновлює +H HP і +M мани.` with zero components omitted. Result: **failed presentation — compact target-list, combat-status and recovery-line retest pending on the next checkpoint**.

- `12026-07-16 16:08:55–16:10:27 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `35fa35bc6fb7ef960963d34b70f5d3958be13ccb`, snapshot `82c3245eedc58255236306b5878fdb1006794742b0379455ede2a6efaed886c2`: Shannar de Kassal entered persistent Fight session `4c0dc692-d715-4d2a-8851-7850127d47bf` with maximum-rank activation `6892f8eb-6df0-4a9d-af17-f57f91b8c73a`. The turn-4 journal showed `😋 Ситий` with 6 turns and recovery `+3 HP / +1 мани`; after one further committed turn and about 28 seconds of real waiting, turn 5 showed 4 turns and `+3 HP`. SQLite proved exactly one new durable pulse id and exactly one stored-minute expiry reduction (`16:15:03.930` to `16:14:03.930 Europe/Kyiv`), while the presenter also subtracted the elapsed wall clock from `expiresAt - cursorAt`. The same absolute-expiry check could end the state during a long combat pause without a committed turn. Result: **FAIL — combat wall-clock time was consumed in addition to the fresh turn; fix and Telegram retest required, with 6 → 5 expected**.

- `12026-07-16 16:40:12 Europe/Kyiv`, managed isolated runtime refresh for checkpoint `74d39a5cc13cd078125dbf46a6c8767ed2f0af02`, snapshot `7e525e9881111f6ff787769bf2c4b076f447d4d57b73018d2b5ed4ab9c144778`: the repo-native refresh wrapper timed out while waiting for the background process, then managed status confirmed host PID `59276`, bot PID `17944`, version `0.3.12`, `Started from: 74d39a5c`, and no source changes since start. Local isolated `/health` and `/ready` both returned HTTP 200. Result: **runtime checkpoint prepared — no Telegram behavior is marked PASS until the user performs the retest**.

- `12026-07-16 16:50:09 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `74d39a5cc13cd078125dbf46a6c8767ed2f0af02`, snapshot `7e525e9881111f6ff787769bf2c4b076f447d4d57b73018d2b5ed4ab9c144778`: before the action, Shannar de Kassal had the expired current-life recipient row and actor-owned self-pair wait from activation `6892f8eb-6df0-4a9d-af17-f57f91b8c73a`, with no active combat lease. The user sent `/dev_reset_varenyk_sated`; Telegram replied `🍽️ Ситість скинуто` and `Поточний стан і пауза цієї життєвої спроби прибрані.` Follow-up isolated SQLite inspection found zero caller cooldown rows matching the Sated recipient key or pair prefix and no active combat lease. Result: **PASS — caller status and actor-owned pair wait cleared on the corrected checkpoint; other-recipient isolation remains pending**.

- `12026-07-16 16:56:04 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `74d39a5cc13cd078125dbf46a6c8767ed2f0af02`, snapshot `7e525e9881111f6ff787769bf2c4b076f447d4d57b73018d2b5ed4ab9c144778`: after the reset, Shannar de Kassal opened `/hero` at full effective `68/68 HP` and `34/34 mana`, then opened `🍽️ Нагодувати себе` without confirming. Telegram rendered the exact `23`-mana price, immediate cap `+7 HP`, delayed `+3 HP / +3 mana`, approved combat-turn wording, `13 / 93` timers and no visible internal rank. Isolated SQLite stored only preview token `f9euf97lhx`, internal `statRank: 5`, plan `{ rank: 5, manaCost: 23, immediateHp: 7, immediateMana: 0 }`, full authoritative planning snapshots, no equipment/attunement/recovery windows and no recipient status row. Result: **PASS — corrected-checkpoint maximum-rank preview presentation, hidden-rank contract and fresh server-owned binding; confirmation remains pending**.

- `12026-07-16 17:03:32 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `74d39a5cc13cd078125dbf46a6c8767ed2f0af02`, snapshot `7e525e9881111f6ff787769bf2c4b076f447d4d57b73018d2b5ed4ab9c144778`: Shannar de Kassal confirmed a fresh maximum-rank self-feed at full HP. Telegram rendered the full-HP branch, `+3 HP / +3 mana`, approved combat-turn wording, `13 / 93` timers, exact `23`-mana spend and no visible rank or immediate mana-recovery claim. Isolated SQLite stored Character at `68 HP / 11 mana`, removed the preview and created exactly one rank-`5` recipient status plus one self pair-wait for activation `92c5e246-9d8e-4539-9ead-aa4b695c6084`, with start/cursor `17:03:32.009`, status expiry `17:16:32.009` and pair availability `18:36:32.009 Europe/Kyiv`. Receipt token `ot6wlzquf9` records `immediateHpRestored: 0`, `immediateManaRestored: 0`, post-cost mana `11` and post-action HP `68`. Result: **PASS — corrected-checkpoint fresh activation, full-HP/partial-mana copy, exact spend, no immediate mana recovery, hidden rank and one status/pair row**.

- `12026-07-16 17:34:25–17:34:42 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `74d39a5cc13cd078125dbf46a6c8767ed2f0af02`, snapshot `7e525e9881111f6ff787769bf2c4b076f447d4d57b73018d2b5ed4ab9c144778`: Shannar de Kassal entered Fight session `35781442-8998-4362-9a7d-aa2e4e0823e3` with fresh activation `ea67a822-b06e-486f-abb3-382f27085481`. Telegram's initial turn-1 card showed `12 ходів`, although the combat lease started only `8.178` seconds after activation and no complete outside minute or combat pulse had occurred. Isolated SQLite preserved `outsideRemainderMs: 8178`; after the first committed turn it held one durable pulse id and the remaining frozen duration `11m51.822s`, proving the initial freeze had used absolute `expiresAt - leaseStart` and silently discarded the uncompleted outside minute from the visible combat budget. Result: **FAIL — initial card must show 13 turns and the first fresh pulse must change it to 12; cursor-based freeze and Telegram retest required**.

- `12026-07-16 17:43:15 Europe/Kyiv`, managed isolated runtime refresh for checkpoint `5b252afb359ca20504f5a487347c95ce54fc7726`, snapshot `07bff06ce9d063ed267b67ac164db67a0798cc9f3662fbb7898920e884c43280`: the repo-native refresh wrapper timed out while waiting for the background process, then managed status confirmed host PID `81668`, bot PID `1540`, version `0.3.12`, `Started from: 5b252afb`, and no source changes since start. Local isolated `/health` and `/ready` both returned HTTP 200. Result: **runtime checkpoint prepared — no corrected Telegram behavior is marked PASS until the user performs the retest**.

- `12026-07-16 18:00:09–18:00:14 Europe/Kyiv`, isolated development runtime `@kvestarnia_test_bot`, checkpoint `5b252afb359ca20504f5a487347c95ce54fc7726`, snapshot `07bff06ce9d063ed267b67ac164db67a0798cc9f3662fbb7898920e884c43280`: Shannar de Kassal entered Big Barrel session `20e7a7a5-2fe4-4a98-8ce9-01e63c20bb3c` with rank-5 activation `fedc8226-db13-4d82-9332-5bddad7663bb`, used `🥟 Кипляча начинка`, dealt `19` damage, received `37` damage from the boss and received the stored `+3 HP / +3 mana` pulse. Telegram journal page 1 showed that named recovery and the skill cooldown, but no Sated row under `Кулдауни та ефекти`. Isolated SQLite stored the pulse id and current participant Sated payload, but `roundLog[0].participantsAfter` contained only resources/cooldowns and no Sated snapshot. This particular late-duration pulse left only `47.138` seconds, so it correctly had no further complete combat pulse; independently, the missing round-owned snapshot meant a genuinely active historical effect could disappear after later turns or lease release. Inspection also found the resolved-duel journal parser dropped stored `satedRecovery`. Result: **FAIL — Big Barrel and turn-duel journals need the same round-owned Sated snapshot/shared effect presenter as Fight/Training; corrected Telegram retest pending**.

- `12026-07-16 18:18:21 Europe/Kyiv`, managed isolated runtime refresh for code checkpoint `1c37b892a4605d3020ee661ebce301d440459040`, snapshot `8cbf8986e07d5d8c22676a9e0d6c7bca6b8b464fea3c0f445570c6e1d3d089ef`: the repo-native refresh wrapper timed out while waiting for the background process, then managed status confirmed host PID `94792`, bot PID `69396`, version `0.3.12`, `Started from: 1c37b892`, and no source changes since start. Local isolated `/health` and `/ready` both returned HTTP 200. Result: **runtime checkpoint prepared — the corrected Big Barrel/turn-duel stored-journal behavior and earlier timing cases are not marked PASS until performed in Telegram**.

## Setup

- Refresh the isolated local bot with `refresh-local-bot.cmd` when ready for manual testing.
- Prepare a living level 3+ Varenyk-mancer, an active same-location recipient and a second Varenyk-mancer.
- Use `/dev_reset_varenyk_sated` only between local cases that need the caller's status/wait cleared.

## Manual Telegram checks

| Scenario | Preconditions and action | Expected bot response and state | Logs / database evidence |
| --- | --- | --- | --- |
| Discovery and preview | Living level 3+ Varenyk-mancer; set raw mana to 7 with exactly one passive point earned, open hero and `Хто поруч`, then preview self and an active exact-location recipient. Repeat immediately before and exactly at a relevant manatka's `readyAt`, using stats that cross internal rank 1/cost 8 to rank 2/cost 12. Replace the equipment row or blessing between list open and preview persistence. | `🍽️ Нагодувати` is reachable without a special location. Open shows exactly 8 mana rather than double-regenerating to 9. A successful preview shows target, exact price, immediate HP, the exact delayed rank-scaled effect (`1/1` at rank 1, `2/1` at rank 2) and timers without naming rank; all displayed resources/effects come from the persisted transaction's authoritative planning snapshots. | Open alone does not mutate Character. Preview proof retains both planning snapshots, internal rank/cost and exact attuned identity; no recipient status row or achievement exists yet. |
| Downgrade and self ordering | Put mana between two internal rank costs, note the visible exact price, confirm self-feed, then repeat below 8 mana after reset. | The highest affordable internal rank is committed without being named to the player. The exact displayed cost leaves in full and no mana returns immediately. Below 8 mana shows a blocker with no activation. | Fresh receipt records the applied internal rank/cost, `immediateManaRestored: 0` and post-action resources; blocked attempt creates no recipient activation. |
| Exact preview binding | Preview an automatically downgraded plan, then separately change mana through passive/Shynok recovery, spend mana elsewhere, equip/unequip a manatka, delete/recreate an equivalent attuned equipment row and start/finish attunement before confirmation. | More mana never silently changes the displayed price/effects; lost exact affordability blocks without spend; equipment row identity, attunement or Shynok-window drift asks for a fresh preview. Rank is absent throughout. Copy says recovery happens `щохвилини поза боєм або кожен хід в бою (це забирає хвилину дії)`; combat settlement still follows the complete hostile response/exchange. | Preview payload retains exact internal stat/applied rank, cost, effective stats, attuned row/slot/version identity, Shynok windows, target lives and expiry; rejected confirms leave actor/recipient resources, anchors, cursor, pair rows and notifications untouched. |
| Caps, full resources and cards | Feed at partial HP/mana, then at full HP with partial mana and at full resources after reset; reopen Hero and supported combat cards. After one complete outside-combat minute, open Hero in reply and edit modes. | Immediate `2 + internal rank HP` caps at max and mana does not change until an eligible minute/turn pulse. Full resources still activate. Zero immediate HP says HP is already full without claiming mana is full. Preview/result blocks use separate paragraphs and bold names/values; Hero keeps the full outside-combat explanation. Feed-list recipients use one compact status/effect/repeat-wait line, while combat cards show one compact `😋 Стан: Ситий ще **N ходів** (**+H HP / +M мани**)` line. Actual lazy recovery arrives once as a separate message after Hero; it is never embedded in the card and omits zero components. | One recipient row contains activation/lives/timestamps/cursor; actor-owned rows contain personal pair waits. No over-max value is stored. Hero card and separate recovery message use the same authoritative post-settlement resources; duplicate opens create no second recovery message. |
| Other recipient and notification | Feed another eligible player, then move, become inactive, lose HP to `0` or enter an incompatible flow before pressing the same confirmation again. | Recipient receives one concise private notification only for the fresh commit; each current-life replay returns the original result despite later mutable gates and does not spend, heal, notify or unlock again. | One receipt/activation and one achievement source event at most; duplicate callback produces no second mutation. |
| Invalid and stale targets | Try inactive, remote, different normalized location, defeated, remorted, combat-active and incompatible-flow targets; alter token/target/life callback fields. After a won Fight, Mimic fight and Training session, reopen feeding with the terminal presence marker still stored but no lease. | Real active leases and incompatible adventures fail closed with concise copy and no effect. A terminal combat presence marker without its authoritative lease does not hide feeding. | Actor mana and recipient cooldown/resources stay unchanged on blockers; terminal Fight/Training clears its adventure marker and the stale-marker fallback performs no mutation. |
| Two-caster race and replacement | Create two previews against the same observed target activation. Let B confirm at t0, then let A confirm the stale preview after one complete minute. Settle through Hero only afterward. Then let another caster open a fresh preview for the winner's active target. Finally shorten the first caster's actor-recipient pair wait and reopen. | B wins. A receives `stale` without consuming the pending winner-owned minute; the following Hero open applies the winner's committed rank-scaled HP/mana portion exactly once. A later fresh preview by another caster offers `оновити стан` and replaces the status with one new activation/rank/13-minute timer. The first caster remains blocked by only their own pair wait until it is shortened/ended, then can replace again. | After A loses, Character resources/anchors, status JSON/cursor, pair rows, notification and achievements are byte-for-byte unchanged. One recipient status row is CAS-replaced; each successful caster retains a separate actor-owned pair row. Old payload settlement completes before a winning replacement. |
| Active/wait UI boundaries | A self-feeds at t0; B replaces A at t1. Reopen Hero and `Хто поруч` for A at t15, exactly at A's t93 pair boundary and before B's t94 boundary. Shorten and then clear a matching pair row while the receipt remains current and replay it. | The effect-bearing status row appears only before expiry. At t15 Hero and `Хто поруч` both show A's own t93 wait, never B's t94. At t93 A can preview again inclusively. Replay shows the shortened wait, then availability after clear, while preserving the receipt and `created: false`. | Displayed status derives from recipient `expiresAt`; displayed wait derives from the validated current-life actor-recipient row `availableAt`. A newer/mixed activation row cannot rewrite another receipt's presentation. |
| Lazy time, restart and remort | Reopen after complete minutes at ranks 1-5, while full, after restart and after expiry; remort recipient and separately remort an actor who fed someone else. | Each eligible minute recovers the committed rank's capped portion (`1/1`, `2/1`, `2/2`, `3/2`, `3/3`); cursor advances while full. Expiry stops recovery while wait may remain. Recipient remort clears old-life state/wait; actor remort does not cancel the other recipient. | Cursor is monotonic and capped at expiry; recipient remort removes its row, while the other recipient's life-bound payload remains. |
| Persistent PvE and Doppelganger | Enter direct and pending-passage first/restarted single-/multi-enemy persistent PvE plus Training Doppelganger with active Sated; first enter before one complete outside minute, then commit actions and timeout/default defence, including terminal turns. Deliberately wait across a wall-clock minute between two actions. Start once at low living HP that survives the hostile response and once where the response reduces HP to `0`. | Entering before the first complete outside minute shows 13 turns; the first fresh surviving pulse changes it to 12. At most one pulse follows the complete hostile response. Each later fresh surviving pulse removes exactly one minute from the visible status; waiting between actions removes none. Active card and the exact stored journal turn show the same whole-turn remainder even after combat ends. It cannot fund that action or revive `0 HP`. Recovery copy follows the hostile response as `😋 <name>: ситість відновлює …`. A foe alive at exchange start still gets its restricted final response when defeated; mutual `0 HP` against the final foe is a win, while hero `0 HP` with another foe alive is a loss. Rejected/stale callbacks do not pulse or shorten duration. | Every start path freezes the same activation and paused remaining duration from the settled cursor while preserving the outside sub-minute remainder separately. Combat state, `lastTurn`, bounded log entry and replay carry identical post-response resources/recovery, the post-turn Sated snapshot and one durable activation/kind/session/turn/recipient id. Lease release rebases the post-pulse remainder once. No-Sated exchanges remain unchanged. |
| Turn duel, Big Barrel and quick duel | Start an asymmetric-tier turn-based duel with stored base maxima below effective maxima. Repeat immediately before and exactly at tuning max-resource, DEX/LUCK, set and ability gear's `readyAt`; inspect the target warning and confirmation, then accept twice and run duel/Big Barrel rounds plus a quick duel. | Before `readyAt`, tuning gear contributes nothing to passive values, warnings/confirmation, stats, maxima, sets/grants or initiative. Warning/confirmation preflight does not write Character resources. Exactly at the boundary all become active; only the winning leased repository transaction persists resources/anchors and chooses the first actor from canonical DEX/LUCK, while duplicate acceptance preserves both. Eligible turn-duel participants pulse after both queued actions resolve; eligible Big Barrel participants pulse after boss retaliation. Wall-clock waiting during the stored lease consumes nothing; every fresh supported surviving pulse consumes exactly one complete frozen minute and appears after the response in card/journal under `Кулдауни та ефекти`. `0 HP` is not revived and quick duel gets none. | Character resources/anchors never exceed the active natural maxima, stored base max columns never change, and the final snapshot matches the confirmation boundary. One acceptance creates exact-time leases, performs one resource preparation and consumes one initiative roll pair; replay consumes none. Each stored duel/raid round owns the post-turn Sated snapshot and nonzero recovery even after later turns or lease release. Matching lease release persists the pulse-shortened expiry once. |
| Solo pre-lease persistence | For ordinary Fight and Training Doppelganger, start with `2m30s` unsettled rank-3 Sated time; make one resource four points below full and the other partial, swapping them between cases. After start, simulate combat damage, mana spend and a pulse in stored state; corrupt the ordinary state and remove the Training session, then run observed-time cleanup. | Start persists exactly `+4 HP/+4 mana`, resets the full resource's anchor and preserves the partial resource's anchor while freezing the 30-second remainder. Cleanup preserves that canonical recovery; no combat-only damage, spend or pulse leaks out, and the next rank-3 `+2/+2` portion appears only after the remaining 30 seconds. | Both shared-path leases start at the exact combat clock. Cooldown cursor, Character and session/lease changes are atomic; a forced Character CAS loss rolls all creation changes back. |
| Combat lease exclusion | Enter solo combat with a 30-second outside remainder. Release once with valid no-pulse state and once after one pulse; then remove or corrupt the session and clean it up. Repeat with malformed and orphan turn-duel state, exact terminal expiry, duplicate release and parallel orphan repair. | Hero reads and wall-clock waiting under the lease do not move the durable cooldown cursor, consume the frozen remainder or change the visible combat-turn count. Every fresh supported surviving pulse consumes exactly one complete frozen minute. Early malformed cleanup resumes recovery from the observed cleanup clock; missing/malformed state preserves the complete legitimate no-state budget without fabricating combat resource or pulse data. | Exact solo freeze time and paused duration are stored without timestamp repair. After valid release, durable `expiresAt - cursorAt` equals combat `expiresAt - cursorAt`; the 30-second remainder shifts cursor but is not added to duration. Missing/malformed cleanup reconstructs only the matching activation active at lease start. Zero duration stores `cursorAt === expiresAt`; duplicate/newer-activation/current-life/CAS/orphan guards remain intact. |
| Big Barrel remort cancellation | Start a real Big Barrel with one participant holding `2m30s` unsettled rank-3 Sated time and resources four points below effective full. Remort another participant once before any round and once after a committed round; repeat the completed remort callback. | Start persists exactly `+4 HP/+4 mana` plus full-resource anchors. Cancellation removes every lease; the survivor keeps that canonical recovery, resumes only after the preserved 30 seconds outside combat, and receives no raid-time catch-up. Replay changes nothing. | Character never receives raid damage/spend/heal/pulses. Survivor cooldown advances exactly once from its matching frozen activation; lease start equals party combat start and remorting-character cleanup remains isolated. |
| Hero authoritative/cheap path | Open Hero when settlement completes HP/mana and ordinary passive HP recovery; replay the feed after status expiry and after the wait ends; then reopen after status, wait and cursor are fully finished. Start a Varenyk adventure and compare Priest/Rogue gates. | Hero shows authoritative resources; ordinary full-HP notice remains once in the card, while Sated recovery follows as one separate message in reply/edit modes. Expired replay shows only the real wait, then says feeding is available again. Historical finished rows keep receipt replay but later opens do no heavyweight fan-out. Varenyk guidance mentions the active adventure; Priest/Rogue behavior is unchanged. | First card and separate notice match committed Character resources. Repeated finished-row opens perform the indexed cooldown guard plus ordinary Hero reads only. |
| Concurrent public reads | With complete Sated minutes pending, race two Hero opens, then Hero against Varenyk open/preview. Force a winner settlement between fallback Character and cooldown reads; replace a completed row with fresh resource-neutral/resource-changing activation between historical cooldown and Character reads; complete a real remort between Hero's preliminary Character and the missing-row guard. Finally force all three fallback pairs unstable. | Every expected-race call returns a normal canonical card or documented blocker; no generic Telegram error appears. Each card uses one stable Character/status observation: never winner cursor with stale resources, old payload/equipment with a new life, or a hidden fresh activation. Missing status with changed Hero fingerprint returns the canonical new life; exhausted optimistic reads use one strong read-only snapshot. | Winner advances Character resources/anchors, cooldown cursor and HP notification once. Retry sequence is bounded at three pair attempts plus one strong snapshot; losers add no recovery/notification, old-life payloads stay hidden, and newer activation identity is not overwritten. |
| Regressions and dev safety | Smoke Priest heal/blessing, Shynok recovery, passive regeneration, rewards and stored journals, including HP-only, mana-only and zero Sated recovery in the Training journal. Verify drink durations/modifiers are bold. Run `/dev_reset_varenyk_sated` locally, then inspect production configuration. | Existing mechanics stay unchanged. Every combat journal shows only persisted nonzero recovery components as plain `😋 <name>: <i>ситість</i> відновлює …`, with only `ситість` italicized and no `+0`. Local command clears the caller's recipient status plus actor-owned pair waits but never another recipient's status; production neither registers, shows nor mutates through it. | Journal matches stored recovery without recalculation or duplicate notice. Only caller-owned cooldown rows are deleted locally; production invocation creates no DB write even with dev-grant configuration set. |

## Smoke after deploy

No deploy is authorized in this task. After a future authorized deploy, repeat discovery, one self-feed, one other-feed, one durable combat turn and the production dev-command absence check.
