# Varenyk-mancer Sated Support — Telegram QA

Status: pending. This checklist documents intended manual verification; it does not claim completed results.

## Setup

- Refresh the isolated local bot with `refresh-local-bot.cmd` when ready for manual testing.
- Prepare a living level 3+ Varenyk-mancer, an active same-location recipient and a second Varenyk-mancer.
- Use `/dev_reset_varenyk_sated` only between local cases that need the caller's status/wait cleared.

## Manual Telegram checks

| Scenario | Preconditions and action | Expected bot response and state | Logs / database evidence |
| --- | --- | --- | --- |
| Discovery and preview | Living level 3+ Varenyk-mancer; set raw mana to 7 with exactly one passive point earned, open hero and `Хто поруч`, then preview self and an active exact-location recipient. Repeat immediately before and exactly at a relevant manatka's `readyAt`. | `🍽️ Нагодувати` is reachable without a special location. Open shows exactly 8 mana, rank 1 and cost 8 rather than double-regenerating to 9. Preview names the target and uses the same resource/stat/maxima/attunement boundary for deterministic and affordable rank, cost/recovery, 13-minute duration and 93-minute wait. | Open alone does not mutate Character. Preview-only row stores the same active-equipment identity/rank/cost; no recipient status row or achievement exists yet. |
| Downgrade and self ordering | Put mana between two rank costs, confirm self-feed, then repeat below 8 mana after reset. | Highest affordable rank is committed; full mana cost leaves before capped `+1 mana` returns. Below 8 mana shows a blocker with no activation. | Fresh receipt records the applied rank/cost and post-action resources; blocked attempt creates no recipient activation. |
| Exact preview binding | Preview a downgraded rank, then separately change mana through passive/Shynok recovery, spend mana elsewhere, equip/unequip a manatka, delete/recreate an equivalent attuned equipment row and start/finish attunement before confirmation. | More mana never silently upgrades the shown plan; lost exact affordability blocks without spend; equipment row identity, attunement or Shynok-window drift asks for a fresh preview. | Preview payload retains exact stat/applied rank, cost, effective stats, attuned row/slot/version identity, Shynok windows, target lives and expiry; rejected confirms leave recipient state untouched. |
| Caps, full resources and cards | Feed at partial HP/mana, then at full resources after reset; reopen hero/combat cards. | Immediate `2 + rank HP` and `1 mana` cap independently. Full resources still activate. `😋 Ситий` shows canonical remaining time; recovery text appears only after a real change. | One recipient row contains activation/lives/timestamps/cursor; no over-max resource value is stored. |
| Other recipient and notification | Feed another eligible player, then move, become inactive, lose HP to `0` or enter an incompatible flow before pressing the same confirmation again. | Recipient receives one concise private notification only for the fresh commit; each current-life replay returns the original result despite later mutable gates and does not spend, heal, notify or unlock again. | One receipt/activation and one achievement source event at most; duplicate callback produces no second mutation. |
| Invalid and stale targets | Try inactive, remote, different normalized location, defeated, remorted, combat-active and incompatible-flow targets; alter token/target/life callback fields. | Every attempt fails closed with concise blocker copy and no support effect. | Actor mana and recipient cooldown/resources stay unchanged; no notification or achievement row. |
| Two-caster race and active protection | Confirm two casters concurrently for one recipient. While `Ситий` is active, manually clear/shorten only the row wait and try again at minute 14; then repeat after both 13-minute status and the original payload's 93-minute wait end. | Race yields exactly one fresh winner. Active status rejects refresh/extension/rank replacement, and shortening only the row cannot bypass the payload wait. A new activation is accepted only after both canonical gates end. | Exactly one recipient cooldown row; blocked attempts preserve activation id/cursor/receipt/original `availableAt`; the later valid activation gets a new id. |
| Active/wait UI boundaries | Reopen hero and `Хто поруч` immediately before, exactly at and after 13 minutes, then at the 93-minute boundary. | `😋 Ситий — N` appears only before expiry. Afterwards the card says `🍽️ Нагодувати знову через N`; the self-feed row remains visible as a wait. At 93 minutes a fresh preview becomes available. | Displayed durations derive from stored `expiresAt`/`availableAt`, not local guessed timers. |
| Lazy time, restart and remort | Reopen after complete minutes, while full, after restart and after expiry; remort recipient and separately remort an actor who fed someone else. | Eligible minutes recover capped `+1 HP/+1 mana`; cursor advances while full. Expiry stops recovery while wait may remain. Recipient remort clears old-life state/wait; actor remort does not cancel the other recipient. | Cursor is monotonic and capped at expiry; recipient remort removes its row, while the other recipient's life-bound payload remains. |
| Persistent PvE and Doppelganger | Enter single-/multi-enemy persistent PvE and Training Doppelganger with active Sated; commit actions and timeout/default defence, including terminal turns. Start at low living HP where the pulse can preserve 1 HP through the response. | At most one pulse follows action/resource spend and precedes the hostile response. It cannot fund that action or revive 0 HP. A foe alive at the start of an attack/class-skill exchange still gets its restricted final response when defeated; mutual `0 HP` against the final foe is a win, while hero `0 HP` with another foe alive is a loss. Rejected/stale callbacks do not pulse. | Combat state, `lastTurn`, bounded log and replay all carry identical post-pulse resources/recovery and one durable activation/kind/session/turn/recipient id. No-Sated exchanges keep the same canonical outcome. |
| Turn duel, Big Barrel and quick duel | Start an asymmetric-tier turn-based duel with stored base maxima below effective maxima. Repeat immediately before and exactly at tuning max-resource, DEX/LUCK, set and ability gear's `readyAt`; inspect the target warning and confirmation, then accept twice and run duel/Big Barrel rounds plus a quick duel. | Before `readyAt`, tuning gear contributes nothing to passive values, warnings/confirmation, stats, maxima, sets/grants or initiative. Warning/confirmation preflight does not write Character resources. Exactly at the boundary all become active; only the winning leased repository transaction persists resources/anchors and chooses the first actor from canonical DEX/LUCK, while duplicate acceptance preserves both. Each eligible durable participant pulses after their own action; quick duel gets none. | Character resources/anchors never exceed the active natural maxima, stored base max columns never change, and the final snapshot matches the confirmation boundary. One acceptance creates exact-time leases, performs one resource preparation and consumes one initiative roll pair; replay consumes none. |
| Solo pre-lease persistence | For ordinary Fight and Training Doppelganger, start with `2m30s` unsettled Sated time; make one resource two points below full and the other partial, swapping them between cases. After start, simulate combat damage, mana spend and a pulse in stored state; corrupt the ordinary state and remove the Training session, then run observed-time cleanup. | Start persists exactly `+2 HP/+2 mana`, resets the full resource's anchor and preserves the partial resource's anchor while freezing the 30-second remainder. Cleanup preserves that canonical recovery; no combat-only damage, spend or pulse leaks out, and the next Sated point appears only after the remaining 30 seconds. | Both shared-path leases start at the exact combat clock. Cooldown cursor, Character and session/lease changes are atomic; a forced Character CAS loss rolls all creation changes back. |
| Combat lease exclusion | Enter solo combat with a 30-second outside remainder. Open Hero just before release and again near/after Sated expiry while the lease exists; resolve at least one stored turn, then remove or corrupt the session and clean it up well before hard/session and Sated expiry. Repeat with a turn-duel orphan after a round. Race two orphan repairs/releases and repeat cleanup after legitimate post-release minutes. | Hero reads and stored progress do not move the durable cooldown cursor or consume the frozen remainder. Early malformed cleanup resumes recovery from the observed cleanup clock, not future hard expiry. Only the actual matching lease interval is discarded; one release wins and duplicate cleanup cannot consume later recovery. | Exact solo freeze time is stored without timestamp repair. Cursor/lease deletion commit atomically through logical release time, preserve `outsideRemainderMs`, never overwrite a newer activation, and an already-released orphan loser is benign while a real cooldown conflict still fails closed. |
| Big Barrel remort cancellation | Start a real Big Barrel with one participant holding 2m30s unsettled Sated time and resources two points below effective full. Remort another participant once before any round and once after a committed round; repeat the completed remort callback. | Start persists exactly two pre-lease HP/mana points and full-resource anchors. Cancellation removes every lease; the survivor keeps that canonical recovery, resumes only after the preserved 30 seconds outside combat, and receives no raid-time catch-up. Replay changes nothing. | Character never receives raid damage/spend/heal/pulses. Survivor cooldown advances exactly once from its matching frozen activation; lease start equals party combat start and remorting-character cleanup remains isolated. |
| Hero authoritative/cheap path | Open Hero when settlement completes HP/mana and ordinary passive HP recovery; replay the feed after status expiry and after the wait ends; then reopen after status, wait and cursor are fully finished. Start a Varenyk adventure and compare Priest/Rogue gates. | The first Hero card shows authoritative resources and the ordinary full-HP notice exactly once. Expired replay shows only the real wait, then says feeding is available again. Historical finished rows keep receipt replay but later opens do no heavyweight fan-out. Varenyk guidance mentions the active adventure; Priest/Rogue behavior is unchanged. | First response matches committed Character resources. Repeated finished-row opens perform the indexed cooldown guard plus ordinary Hero reads only. |
| Concurrent public reads | With at least one complete Sated minute pending, race two Hero opens, then race Hero against Varenyk open/preview. | Every call returns a normal card or documented blocker; no generic Telegram error appears. All cards converge on the same canonical resources. | Character resources/anchors and cooldown cursor advance once; HP recovery notification state has one generation/effect and no newer activation is overwritten. |
| Regressions and dev safety | Smoke Priest heal/blessing, Shynok recovery, passive regeneration, rewards and stored journals, including HP-only, mana-only and zero Sated recovery in the Training journal. Run `/dev_reset_varenyk_sated` locally, then inspect production configuration. | Existing mechanics stay unchanged. Training journal shows only persisted nonzero recovery components and never `+0`. Local command clears only caller current-life status/wait; production neither registers, shows nor mutates through it. | Journal matches stored `entry.summary.satedRecovery` without recalculation or duplicate notice. Only the caller's Sated row is deleted locally; production command invocation creates no DB write even with dev-grant configuration set. |

## Smoke after deploy

No deploy is authorized in this task. After a future authorized deploy, repeat discovery, one self-feed, one other-feed, one durable combat turn and the production dev-command absence check.
