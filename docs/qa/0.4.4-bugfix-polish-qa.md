# 0.4.4 Bugfix & Polish — Telegram QA

Status: pending. Run one exact scenario at a time against the final branch snapshot; automated tests are not manual PASS evidence.

## Setup

- Refresh the isolated local bot only at the manual checkpoint.
- Use three current-life accounts and record checkpoint/time/action/Telegram observation plus DB/log corroboration.
- Keep PR `#190` and all ґільдія flags/data out of the run.

## Matrix

| Scenario | Telegram expectation | State evidence |
| --- | --- | --- |
| GroupCombat inline attack with 2 enemies, then 1 | Main card has one `🗡️ Вдарити`; two enemies open target-only names and Back is `↩️ До дій`; one remaining enemy commits directly without a picker | Opening/Back does not queue an action, advance turn or move timeout; sole-target attack queues once |
| Class/race/gear abilities | One main-card button per available ability; explicit enemy or ally/self actions open a picker only for multiple valid targets; one valid target and auto-target actions commit directly; self is marked `(ви)` | Final selection or sole-target action queues once through the canonical action atom |
| Enemy focus | Start with the leader, then make another participant deal the most actual damage in a resolved turn; after combat open both journal pages; separately resume an active pre-marker v2/v3 fixture with nonzero legacy threat | Active card and each new journal page mark that turn's `🎯 ціль ворогів`; recap announces a change; the next enemy phase targets that participant; the pre-marker session first shows and targets the living leader, then follows actual turn damage; legacy pages without stored focus do not invent one |
| Mixed support damage and monster shields | Leave a later enemy at 1 HP, then use `✨ Суворе благословення` while the first frozen-order enemy is alive; let a monster shield absorb damage and finish the fight | The recap names the enemy that actually receives the blessing's damage; the 1-HP enemy survives only when it was not hit. `📊 Статистика` records only damage actually absorbed under the shielding monster's `🛡️ відвернена шкода`, not its displayed remaining shield points |
| Reply-menu picker | Reply attack/targeted ability creates a temporary picker; selection and Back remove it and restore the canonical card | No duplicate persistent references; stale target/turn is rejected |
| Duplicate/stale/timeout overlap | Double-tap a final target, retain an old picker across a turn, and overlap a timeout | At most one canonical action wins; fresh card/result is shown |
| Three-account last-ready and turn delivery | With all rollout flags enabled, let the third account press `✅ Готово`, then complete at least two turns with actions from different participants | The third account receives the first live card without hanging or returning to preparation; every account receives the same current turn in actor-first/roster order; ordinary taps do not repeatedly show `Це стара картка`; logs contain no GroupCombat-path `P1008`/`P2028` during the run |
| Left link from another ordinary location | Open `nyz_left_attack_…` as an eligible account | Player is moved to the exact left passage and joined in one transaction |
| Left join blockers | Repeat with wrong token/origin, expired/full party, 0 HP, stale life, invalid resources, combat, search, rest, adventure, raid and another live party | No blocked case changes location or membership; remaining wait is visible where applicable |
| Legacy left `party_` link | Open an older valid left-passage link from elsewhere | Same safe relocation/join behavior; unrelated party links do not relocate |
| Active/terminal fight link | Participant reopens the link during combat and after completion | Canonical GroupCombat card/result appears; no second join attempt |
| Invite sharing | Every live member opens the invite card and rotates copy; test a runtime without bot username; inspect a one-minute and multi-minute deadline | Visible `nyz_left_attack_…` URL, leader/count/readiness/deadline, `Ватагу відкрито.` on creation, correct `1 хвилина` / `2 хвилини` / `5 хвилин`, final `⚔️ Почати атаку` row, no reward amount or trace sentence; missing username stays bounded |
| Inventory locks | Compare gated level/class/race/title/Loot Expansion equipment, equipped gated equipment, normal gear and junk across all/sorted/slot views | `🔎 🔒` only for unequipped gated equipment; `✅` wins; ordering/filtering/pagination unchanged |
| Locked detail | Open locked detail from all-inventory, sorted and slot-filtered paths | Same requirement reasons; no Equip action; Back returns to the same surface |

## Result record

- Automated: record exact commands and counts in the PR/handoff.
- Manual isolated Telegram run: pending.
- Deployment/production observation: merged `0.4.4` head was observed, but the hotfix observation remains pending.
