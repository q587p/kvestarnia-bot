# Kvestarnia Codex Context

Keep this file compact. Target: under 250 lines.

## Identity

- Project: Kvestarnia, a humorous fantasy Telegram RPG.
- Player-facing name: `Квестарня`.
- Technical slug/package/repo prefix: `kvestarnia`.
- Bot username target: `@kvestarnia_bot`.
- Current package version in this repository snapshot: `0.1.23`.

## Language split

- Codex-facing workflow docs, task docs, prompts, skills, PR text, commits, and test names should be English when practical.
- Player-facing Telegram copy, lore, item/monster names, news, and release flavor stay Ukrainian.
- Ukrainian copy uses `«»`, Holocene visible dates (`12026`; release/news/changelog headings use `1YYYY-MM-DD`, e.g. `12026-06-20`, and should be rechecked against the current Kyiv release day before PR-ready handoff), `міт*` with `т`, and `соціяльн*` with `я` where applicable.

## Token-economy rules

- One versioned task per Codex thread.
- Prompts should be short and skill-based: `Use $skill` plus the task doc path.
- Do not paste long repeated instructions into prompts.
- Inspect changed/relevant files before broad scans.
- Second Codex review defaults to changed files only.
- Final Codex responses should be short: changed files, behavior changed, tests run, risks, completion status. No tutorial.
- After a versioned task closes, write a compact handoff and start the next task in a new Codex thread.

## Active skills

Repo-specific skills in `.agents/skills/`:

- `$kvestarnia-version-task` — main implementation for one scoped versioned task.
- `$kvestarnia-second-codex-readonly` — second Codex read-only review, changed-files default.
- `$kvestarnia-telegram-qa` — compact/full Telegram QA plans.
- `$kvestarnia-release-checklist` — release readiness and handoff.
- `$balance-review` — combat, loot, progression, and economy risks.
- `$ukrainian-rpg-content` — Ukrainian player-facing battle/tip/location/news/content copy.

Compatibility/reference copies in `skills/` may exist, but default workflow should rely on `.agents/skills/` so `$skill` activation is predictable.

Use one main skill by default. Add another skill only when it materially helps.

## Important docs

- `AGENTS.md` — hard project/Codex rules.
- `README.md` — public-facing project window.
- `docs/README.md` — docs index.
- `docs/CODEX_WORKFLOW.md` — workflow and token-economy rules.
- `docs/tasks/README.md` — version task doc convention.
- `docs/BRAND.md` — naming and public voice.
- `docs/PRODUCT_BRIEF.md` — positioning and MVP scope.
- `docs/GAME_DESIGN.md` — core game loop.
- `docs/CONTENT_STYLE_GUIDE.md` — Ukrainian copy style.
- `docs/TECHNICAL_PLAN.md` — architecture and data model.
- `docs/ROADMAP.md` — phase plan.
- `docs/BALANCE_NOTES.md` — formulas and economy guardrails.
- `docs/QUEST_RESOLUTION_VARIETY.md` — authored quest method contract, grade consequences and ledger rules.
- `docs/QUEST_SKILLS_AND_CHECKS.md` — deterministic quest check math and technique vocabulary.
- `docs/QUEST_RESOLUTION_CONTENT_SEEDS.md` — authored scene/method content seeds.
- `docs/SECURITY_AND_FAIR_PLAY.md` — privacy, anti-abuse, idempotency.
- `docs/DEVELOPER_SETUP.md` — local setup and deployment notes.
- `docs/PLAYTESTING.md` — manual smoke tests.
- `CHANGELOG.md` — detailed release history.
- `news.md` — player-facing spoiler-light release news.

## Code map

- `src/bot.ts` — app entry point.
- `src/bot/createBot.ts` — grammY bot assembly and middleware wiring.
- `src/bot/commands/` — command handlers.
- `src/bot/callbacks/` — callback data parsers/serializers.
- `src/bot/keyboards/` — Telegram keyboards.
- `src/bot/presenters/` — Telegram text rendering.
- `src/bot/presence/presenceRouting.ts` — presence/location routing helper.
- `src/services/` — application services connecting bot/domain/db.
- `src/domain/` — pure game logic; no Telegram imports.
- `src/content/` — validated content tables and flavor data.
- `src/db/repositories/` — repository interfaces and Prisma implementations.
- `src/jobs/` — background/scheduled jobs.
- `src/shared/` — shared helpers.
- `tests/` — tests mirror source layers.

## Package scripts

From `package.json`:

- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm test`
- `npm run check` = lint + typecheck + build + tests
- `npm run db:generate`
- `npm run db:validate`
- `npm run db:migrate`
- `npm run db:deploy`

Prefer focused tests first, then broader checks.

## Runtime safety focus

For Telegram/gameplay changes, always consider:

- duplicate callback presses
- stale callback data
- idempotent rewards and quest progress
- active fight/session conflicts
- player state/session consistency
- presence/location routing
- DB transactions and replay safety
- restart/redeploy behavior
- short Ukrainian Telegram messages
- distinct icons for distinct rows/actions in the same message; do not reuse a location/surface icon for a separate quest row.
- starter/onboarding paths when replacing older flows; level 1-2 starter mimic-shawarma must remain reachable before level 3 adventure choice.
- `0.1.14` adventure choice loop: level 3+, deterministic offers from a broad general + race + class + current-title problem pool with at least one personalized candidate when available, declined personalized race/class names in generated copy, stable per-problem offer/button icons, qualitative pre-choice risk/reward copy, readable choice spacing, daily-action idempotency, active-fight guard, preserved level 1-2 starter shawarma that hides from Quest Hub after completion, completed starter lookups do not stamp actionable starter presence, no direct Fighting Corner promo/button from the quest table, complication-to-persistent-fight path that does not spend the claim if combat cannot start, remort resets Yeger once-per-life quest keys, and `/dev_adventure_reset` for local QA that rerolls the same-period offer seed.
- `0.1.15` combat lock and battle flow polish: unfinished persistent, training, or active starter fights centrally redirect normal commands/callbacks and recognized reply-keyboard main-menu text back to battle until terminal state, while real persistent/training/starter combat callbacks, Help, `/help`, and `/version` remain allowed; combat guards run before destination presence writes and blocked routes refresh combat presence instead of stamping hall/news/Yeger destinations; terminal/expired persistent restores show the canonical terminal/reward screen; active training keyboards show only combat actions; fight keyboards recompute action availability every render; ordinary level 3+ problem fights route first to `Спуск до Низу` with `⬆️ Повернутися до зали` above `⬇️ Спуститися`, then `⬇️ Спуститися` enters the separate first-tier surface/location `Ярус I: Сутерени Корчми` (`location.korchma.deep.level1`) with three passage buttons (`⬅️ Лівий прохід` hard, `🚪 Прямий прохід` normal, `➡️ Правий прохід` easy); adventure three-choice lists show only title plus italic short line before selection; outside direct activity gates show only `Зайти в корчму`; three consecutive eligible ordinary `Низ` fights trigger monster rest from the third fight's stored terminal completion time, not later reward/replay writes; new battle intros reuse existing `Порада дня`; lazy HP recovery can show a one-shot full-health notice during `/hero` or `/fight`; Yeger quest selection/help/turn-in stay at the Barrel-side corner, while active trail taking/checking happens outside via `Надвір` / final-row `До полювання`, and progress still matches eligible monster type/tag source-agnostically by completion time.
- `0.1.16` character stats growth rework: Human-ish race budget is `+1 STR/+1 DEX/+1 CHA` with mysterious practical flavor instead of an exact preview; hidden paths add derived effective stat bonuses (`sun` STR+DEX, `moon` INT+DEX, `boundary` LUCK+CHA) without persisting to `statsJson` or previewing exact mechanics; level stats still total `level - 1` but use deterministic distributed growth biased by class profile plus race/path; `/hero` and post-reward level-up celebrations show the same race/path-aware next-level delta with `Зміна:` instead of cumulative `Ріст:`; active combat still locks adventure/navigation routes but allows safe side surfaces such as inventory/item/equipment callbacks, hero/profile, nearby-player views, restart/remort and support, with real `v1:rm:*` remort callbacks bypassing the combat redirect before normal remort/pending-raid validation; `/help` future-facing copy points to shops/crafting/guilds instead of saying loot/combat bookkeeping are missing; remort memory keeps 23% of previous actual race/path-biased distributed level growth per stat, excluding old fixed identity bonuses; combat simulation tooling also accepts the hidden path for live-math parity; `Низ` passage baseline XP and broad loot profile use effective monster level, while XP anti-farm compression checks stored pre-intervention base monster level so an easy-passage drop is not farming but genuinely weak base content remains compressed; front-door outside no longer shows a completed/inactive Yeger teleport or Barrel-side Yeger explainer, while active trails still show outdoor `До полювання`; selected Adventure Choice problem messages hide the safe/medium/risky reward ladder from body copy; local `/dev_raid_stop` can finish an active pending Barrel raid through normal completion for QA.
- `0.1.17` instant duel polish: the shipped quick duel is now `⚡ Миттєва дуель`; forwardable invites have 13 deterministic variants plus owner-only `🎲 Інший текст` rotation that edits only invite copy and preserves token/URL/expiry/state; duel create/rematch/accept uses canonical lazy HP/mana resource sync with persistence and optimistic conflict fallback; accept syncs both participants at the same logical time and recipient warnings come from the recipient snapshot; instant scoring temporarily normalizes only progression-derived level/remort budget while keeping race/class/title/path/equipped manatky/equipment effects personal; current HP/mana matter through a capped hidden readiness penalty; new result JSON stores participant snapshots, balance version and audit fields while old rows remain readable; no XP/gold/items/quest progress/item loss/wagers/tournaments/rating power/turn-based PvP runtime ships in this slice.
- `0.1.18` turn-based player duels: Fighting Corner now offers `⚡ Миттєва дуель` and `♟️ Покрокова дуель`; `DuelChallenge.mode` is stored and server-authoritative (`duel_<token>` quick, `duel_turnbased_<token>` turn-based); `👀 Хто поруч` can page active same-location players, choose a target and create a targeted quick or turn-based in-game invite, targeted recipients get a best-effort cancellation notice only on the actual cancel transition, quick duel participants get a best-effort result card only on the actual accept/resolve transition, and decline/refresh re-renders keep configured invite-link state instead of showing false missing-bot-username warnings; turn-based accept freezes both synced/normalized participant snapshots, creates one `duel_combat_sessions` row plus active combat leases, stores initiative/turn/version/turnExpiresAt and renders recoverable private participant cards plus spectator-safe group cards; active private duel cards show duel actions plus refresh only, hide unavailable class actions using shared combat availability, show viewer skill cooldowns, and omit Fighting Corner navigation while the combat lock would block it; result cards return to the Fighting Corner instead of the quest table or hall; each round queues participant choices privately, then reveals and resolves the round only after both choices or the durable 23-second timeout auto-attacks missing choices; player-action CAS requires `turnExpiresAt > now` and timeout CAS requires `turnExpiresAt <= now`; same-round older-version callbacks may merge once before deadline if the actor has not chosen; resolved actions use shared pure `resolveActorCombatAction(...)` for attack/class skill/mana/cooldown/armor/resist instead of duel-only formulas, with defensive skill mitigation applied to same-round incoming PvP damage; PvP damage uses normalized effective combat level while visible levels stay real; malformed active sessions/orphan leases repair to non-rewarding expired state; surrender/timeout/defeat terminal paths store explicit reasons, resolve the parent challenge as `resolved`, release leases and replay result cards with rematch/share controls; terminal turn-based results grant small replay-safe XP (`1` loss, `2-5` draw, `4-8` win with bounded luck influence) while quick duels remain XP-free; combat lock redirects active duel participants, including restart/remort routes, while ordinary-combat side-surface policy stays unchanged; duel HP/mana stay ephemeral and no gold, items, wagers, item loss, tournaments, broad discovery or rating power ship.
- `0.1.19` Nyz passage balance polish: ordinary/problem fight passage balance keeps the center route as baseline, changes the right/easy route to prefer available monsters `3-5` levels below the character with safe fallback, gives easy XP as a downward-rounded `0.5x-0.75x` character-level roll with a small bounded LUCK bias, and gives left/hard XP as a `1.25x-1.5x` character-level roll with a floor of center-route baseline XP for the same base monster plus one; passage loot endpoints are easy `0.65` drop multiplier / `-1` loot power, normal `1` / `0`, hard `1.35` / `+1`; normal, Yeger and adventure persistent fight wins use variable `0..character level` gold with `0` gold boosting item drop chance to `93%` before interpolating back to `getItemDropChance(luck) * passage.dropChanceMultiplier` at max gold; quest turn-ins do not use variable gold, first Yeger turn-in XP is level-scaled with an `80 XP` high-level cap while its gold/item stay fixed, and stored completed fight/quest rewards replay from session/daily-action data instead of being recomputed.
- `0.1.20` authored quest resolutions: level 3+ Adventure Choice no longer uses active `safe/flair/risky` method buttons; selected problems render 5-7 stable authored scene-action methods, with race/class/signature/title shaping checks, selected affordances and outcomes without exposing mechanic-source labels in buttons, and with qualitative hints and no exact future reward/odds copy. Adventure Choice uses compact stable problem and method callback keys under Telegram's 64-byte limit, while service resolution stores canonical ids. Hint rendering suppresses duplicate reliability wording and keeps chance phrases sentence-cased; generated race/class/title families use scene-native анкета/кухоль/портрет/підручник/форма/іспит/титул methods instead of one universal generated template, and outcome copy avoids singular/plural agreement traps. Race/class/signature influence is bound to concrete scene affordances rather than blind noun substitution, while visible labels/buttons name the scene action; the deterministic visible-method resolver is authoritative for both button rendering and completion, so hidden authored method ids stale out before mutation unless they are explicit legacy starter/cellar actions. Starter mimic-shawarma and level 2-3 cellar mouse use the same resolution contract while preserving level gates, item grants, idempotency and legacy starter/cellar callback replay; the cellar paid bribe stays reachable without dropping race/class/signature slots. Starter shawarma and starter combat each grant `75%` of the level 1-to-2 XP gap rounded up on the remort-adjusted curve, so doing both reaches level 2, and the quest archive shows completed starter shawarma beside the completed starter combat probe. Old Adventure Choice `v1` safe/flair/risky callbacks show stale-refresh/current offer rather than mapping to new methods, and unknown current-version method ids stale out before claim/cost/cooldown/reward. Quest checks use the effective stat snapshot including equipped manatky/item effects, are deterministic and bounded, produce `strong-success`, `success`, `mixed-success` or `complication`, and store method/grade/consequence/check/cost/reward/items in `daily_actions.resultJson` with `spent_gold` for guarded atomic paid methods. Level 3+ authored rewards apply small stored XP/gold variance and a low LUCK-influenced manatka chance after resolution; paid Adventure methods grant no gold reward and instead get a slightly higher manatka chance; replay never rerolls. Cellar mouse rewards stay deliberately tiny and paid mouse methods cannot become a farm. Player-facing result copy avoids internal method-source labels such as race/class/signature signatures and uses a separate `Винагорода за справу` block. Complications are varied: full/reduced/XP-only/paid/cosmetic outcomes and existing persistent-fight handoff where authored. Korchma hall prose still mentions `Бійцівський куток` and `спуск до Низу`, but the hall hides both action buttons before level 3, and stale direct corner/duel-board callbacks return a short level-gate card instead of moving presence into the corner. `🎒 Манчкін-скупник` follows a Kyiv local `21:00-07:00` night schedule: day paragraph/button stays at the korchma front door, night paragraph/button moves to `Спуск до Низу`; level barter now requires at least `587` gold value from eligible manatky before wallet gold can fill the rest. Training and duel result screens use explicit `↩️ Повернутися до кутка` return buttons for Fighting Corner navigation. Local `/dev_help` lists enabled dev commands, `/dev_raid_stop` sends the ordinary separate level-up celebration when its raid reward crosses a level threshold, and `/dev_add_level` accepts an optional positive amount parameter.
- `0.1.21` combat action foundation: combat state now supports basic attack, basic defend, class skill and flee through a server-authoritative action/availability contract; class skill cooldowns are ability-keyed while legacy `cooldowns.skill` states remain readable; current class actions become available after one subsequent own committed action. Unavailable skill attempts because of missing mana or cooldown are no-ops: no mana spend, no turn advance, no cooldown tick, no monster response and no RNG advancement. `🛡 Захищатися` appears in persistent solo fights, training doppelganger fights and turn-based duels; PvE defend reduces current-round incoming damage and can rarely counter with fatigue on repeated defense, while hidden-choice duels apply deterministic same-round incoming damage reduction at reveal. New solo/training turns store `turnExpiresAt` in `CombatState` for about `23` seconds; active cards store their Telegram message reference, scheduled overdue turns repeat the canonical basic auto-attack until combat ends or hard session expiry is reached, and skip-mode lazy fallbacks repeat the skipped hero action; auto-resolved victories can grant normal rewards. Persistent fight state stores an origin location: ordinary `Низ` fights keep post-fight `Новий бій`, while Adventure Choice and Yeger handoff fights return to the surface where they started instead of always pointing to `Низ`. Persistent monster fights also freeze a Kyiv-local context snapshot plus deterministic bark state in `CombatState`; the content roster now has 93 monsters, each with a context profile and five authored barks, and these affect fight texture only, not XP/gold/loot/Yeger matching or monster eligibility. Opt-in combat balance analytics adds Prisma tables via `20260621100000_add_combat_balance_analytics`, keeps collection disabled by default, separates manual vs timeout action origins in reports, and now claims training timeout terminal rewards through the same idempotent path as manual terminal turns. No race/signature/monster/item ability catalog, reward formula/economy change or broad combat coefficient rewrite ships in this slice.

- `0.1.21` combat UI follow-up: newly started persistent monster fights send a separate static intro card and then one active button card that updates each turn, including scheduled timeout edits when the active card reference is still deliverable. The active card shows HP/mana, turn number and the visible `23` second auto-action rule; stored monster barks render as Telegram `<blockquote>` speech; generic `Наступний крок` prompts were removed from fight/hunt result templates.
- `0.1.21` combat UI hardening: persistent active fight cards separate the timeout note with a blank line, prompt by character name (`<b>Name</b>, що робимо?`) and persist `lastTurn.debugTrace.timeoutMode` so the next card can say whether an overdue turn auto-attacked or skipped the hero action.

- `0.1.21` combat hardening follow-up: Prisma solo-combat state mapping preserves current runtime JSON fields such as origin, guard streaks, skipped-turn summaries, monster debug/equipment traces, ability cooldowns, context and bark state while keeping legacy `cooldowns.skill` readable; turn-based duel defend uses the next consecutive-defend tier and clears on non-defend actions; failed flee monster responses advance deterministic bark state, while successful flee does not count as a monster action.
- `0.1.21` combat scheduler hardening: solo/training timeout state preserves the active Telegram card reference through real resolver/service transitions; terminal `/fight` renders and scheduler edit-fallback replacement cards record the new canonical message reference, including terminal outcomes; training combat-lock redirects after expiry render the terminal training result instead of generic active-training copy; scheduler overdue handling repeats the canonical basic auto-attack, skip-mode lazy handling repeats the skipped hero action, hard-expired sessions close without auto-attacking or refreshing expiry, duplicate due ticks stay CAS-safe, and due discovery paginates active rows with kind filters so legacy/future/other-kind prefixes do not starve due fights. Proactive HP-full notifications are deferred; lazy `/hero` and `/fight` recovery notices remain.
- `0.1.21` combat balance analytics follow-up: an opt-in `COMBAT_BALANCE_ANALYTICS_ENABLED` collector stores one idempotent compact battle row per completed PvE solo/training combat plus ability usage rows for class/remort balance reports; combat state accumulates analytics totals from a frozen start snapshot, reports exclude Telegram ids/usernames/display names, failures never block combat completion, and old in-flight combats without snapshots are skipped rather than guessed.

- `0.1.22` monster abilities and AI: ordinary non-training monster fights now freeze a `monsterRuntime` block at encounter start with selected ability ids, AI profile, cooldowns, once-per-fight usage, telegraphs, shields and short-lived effects; old active fights without the block stay on legacy basic-attack behavior. The typed content catalogs cover 132 monster ability definitions and 93 current monster profiles. Explicit authored ability ids fill identity slots before deterministic tag fallback fills missing legal slots from effective-level scaling. Ordinary monsters owe a basic/defend after any ability, bosses may chain at most two different abilities, and telegraphed heavy actions spend one monster activation warning before impact. Failed flee responses and timeout auto-defend recovery call the same monster AI once. Training doppelganger keeps copied class-skill behavior. No player race/signature/title abilities, item actions, party/multi-enemy runtime, Yeger rule change, reward/economy/loot rebalance or new analytics migration ships in this slice.
- `0.1.22` title/skill identity follow-up: all 54 current active race/class pairs have authored pronoun titles, so active onboarding must not use the generic `Пригодник/Пригодниця/Пригодники місцевого значення` fallback. All nine current classes have distinct skill IDs and labels: Mage keeps `skill.hot-spell` / `🪄 Гаряче закляття`, Varenyk-mancer uses `skill.boiling-filling` / `🥟 Кипляча начинка`, Ranger keeps `skill.trick-shot` / `🎯 Хитрий постріл`, and Rogue uses `skill.shadow-cut` / `🌘 Тіньовий різ`. Varenyk-mancer and Rogue preserve their old numeric profiles; old in-flight cooldowns under `skill.hot-spell` or `skill.trick-shot` block the renamed skills until normal cooldown ticking removes them, then future use stores only new IDs.
- `0.1.22` battle-card follow-up: persistent PvE overdue turns now commit `defend` / `timeout-auto-defend` instead of basic auto-attack, while legacy timeout-auto-attack JSON remains readable. Successful persistent turns append durable `turnLog` entries in combat state JSON; terminal/result fight keyboards can open `📜 Журнал бою` with first/previous/next/last paging, include the final terminal `lastTurn` page when needed, and return with `↩️ До результатів`; active fight keyboards do not show the journal button. Active fight cards place the 23-second timeout rule directly below the named `що робимо?` prompt without an empty spacer, keep `Вдарити` and `Захищатися` in one first row for persistent/training fights, no longer show `Хід записано` or `Остання дія`, skill cooldown rows name the exact skill, monster ability summaries include damage/effect/no-direct-damage consequences, and monster context cues are covered against `????` mojibake. Timeout recovery refreshes the current combat row before committing an auto-turn so stale due records preserve newer active-card message references. The three Nyz passage buttons now enter separate presence locations (`Лівий прохід`, `Прямий прохід`, `Правий прохід`), show a spotted-monster preview with `Атакувати` before creating combat, and keep `👀 Хто поруч`, nearby duel validation, combat-lock redirects, fight turn callbacks, result replays, journal views and post-fight `Новий бій` scoped to the selected passage instead of merging everyone into `Сутерени Корчми`; legacy terminal fights without `originLocationId` are not backfilled and use the neutral straight-passage fallback. Duplicate persistent fight turn callbacks stale out without duplicate reward/level-up side effects. The nearby challenge button appears only when another active same-location target exists. Night Munchkin level-barter cards now return to `Спуск до Низу` instead of the front door, while daytime/default barter cards still return outside. Persistent reply keyboards can label the place button with the current presence location on normal reply-keyboard renders; location-changing inline callbacks no longer send standalone `📍 <місцина>` refresh messages, and recognized location labels reopen the current location surface/actions instead of always going to the korchma hall.
- `0.1.22` monster runtime hardening: content validation now compiles every authored monster ability parameter into a supported runtime consequence, rejects unsupported/effectless recipes, and filters fallback loadouts by recipe legality plus level/power safety. Runtime shields account absorbed/applied damage from pre-hit HP without healing the monster; equal-or-stronger shields, empty cleanses, missing cooldown targets, missing positive effects and missing expired effects do not produce misleading support/control consequences. Monster abilities now use explicit per-component execution plans for legality and resolution, so optional heals do not gate damage/shields/buffs, low-HP parameters are additive bonuses, mixed-scope buffs route to the authored actor, race-source locks do not lock class skills, and cycle/parity riders apply only selected branch effects. Runtime effects carry or derive target/polarity/removability/source metadata in combat-state JSON; cleanse removes only harmful removable monster effects, purge removes only real removable beneficial hero effects, and `statusResistancePp` uses an explicit non-removable `status-resistance` fallback instead of masquerading as damage reduction. Every component has an explicit trigger classification; harmful direct-attack riders such as burn, chill and outgoing-damage reductions wait for an actual landed direct hit, while self-support and support cycle branches stay independently applicable. Effect consumers use target plus polarity or documented magnitude semantics, so debuff bonuses count only active harmful hero effects and future beneficial hero accuracy/evasion/flee rows are not read as penalties. Deferred `nextAttackBonusIfShieldSurvives`, copied-potency next-hit bonuses and probabilistic `counterChance` use non-neutral persisted trigger state with injected RNG and exactly-once consumption; next-hit bonuses use source/trigger identity so copied-potency and shield-survival sources can coexist without duplicate same-source shield charges. Zero-damage support abilities, monster defend, telegraph and retained timeout-skip paths preserve resolver outcomes in summaries, durable logs, journal replay and analytics. Marks survive hero actions and spend charges only when a later direct monster hit uses them after defend evasion, including ordinary forced basics; reflect/counter/shield-break reactive damage resolves before win rewards, so hero-zero mutual KO is a loss. Turn riders, repeat prediction, expired-effect reapply, copied potency, shield-survival bonuses and counter chance use persisted runtime state and injected RNG. Runtime basic attacks use the shared defend stance for evasion, reduction, fatigue and bounded counters, while runtime abilities apply defend mitigation once. Target accuracy/evasion penalties affect later target rolls instead of the current monster cast; positive monster accuracy context raises current ability hit chance, and monster evasion affects hero hit probability only, not successful-hit damage. Terminal combat journal entries use stable `terminal:*` ids, synthesize legacy eventless final rows when needed, and are capped at 587 stored rows.
- `0.1.23` encounter preview memory: ordinary Nyz passage previews are durable pending encounters with server-owned opaque tokens, frozen monster ID/base/effective levels, 93-minute expiry and a consumed combat-session link. Reopening the same passage before expiry returns the same preview; attack consumes the pending row into at most one persistent solo combat session, while expired/stale/missing previews visibly refresh instead of silently rerolling. If the consumed fight ends without killing the monster, the same trail can show that same surviving monster again until the original 93-minute trail expires, even if the player dealt no damage; re-attacking creates a fresh combat session with the same monster/effective level and recovered monster HP. Killed monsters still send the next preview through normal fresh selection. Pending expiry/consume/re-attack paths use guarded status/version/current-time transitions and validate frozen encounter inputs inside the Prisma transaction. Legacy seed-shaped passage callbacks no longer select monsters. Passage attack callbacks are bound to the server-owned encounter and current passage, so old buttons from another location refresh the actual current place instead of moving presence or starting combat. Ordinary non-targeted Nyz selection reads bounded pages of recent history, skips active and non-ordinary fights, excludes the immediately previous monster when alternatives exist, excludes the last three distinct monsters when the candidate pool is large enough, then falls back deterministically for small pools. Passage preview copy stays monster-gender-neutral, includes the shown monster level and wounded HP when applicable, and avoids accusative-name contexts until a future monster grammar metadata slice adds gender/case forms; full-health survivor previews keep the same monster/token without a wounded HP line. Active persistent fight cards include the fight header, opponent name/level and first-turn start tip beside the combat buttons. Cellar start combo flavor keeps a blank line between the title-in-cellar sentence and the following race/class beat. Memorial-board remort detail buttons show all visible remort groups and treat `Реморт N` as the life after remort N, so detail rows do not reuse base-life milestones before that remort; post-remort level milestones use remort-specific daily-action keys with provenance and board backfill can restore current-life rows for already-remorted characters without duplicate same-character rows. Place-changing inline callbacks send a short movement line with the updated persistent keyboard before rendering the new place card, so the bottom place button matches the new location without a debug-like `📍 Тепер:` message. Successful remort during solo/training combat canonically expires the old session, releases the active solo lease, cancels live pending/consumed passage trails, preserves new-life starter HP/mana and grants no combat rewards; unsupported turn-based duel leases still block remort without mutation. Yeger targeted encounters, adventure handoffs, training doppelgangers, starter fights, monster abilities, timeout auto-defend and rewards remain unchanged.

- `0.1.23` terminal settlement follow-up: persistent ordinary/training combat JSON freezes a combat life remort count and durable settlement state (`pending`, `completed`, `forfeited-by-remort`). Terminal sessions keep the solo-combat lease until reward/resource/cooldown settlement completes or remort forfeits them; legacy no-life sessions derive expected life from `session.createdAt` versus the remort ledger so stale recovery cannot mutate a newer remort life. New ordinary/passage/Adventure/training starts first follow the active solo-combat lease, recover leased terminal pending sessions, clear exact supported stale completed/forfeited/missing leases, preserve unsupported lease kinds, complete/forfeit settlement through repository guards, and exclude forfeited won sessions from problem-chain/Yeger victory progress while counting completed and valid legacy wins.
- `0.1.23` combat settlement final hardening: ordinary/training settlement JSON also records durable `resources` and `training` substeps, so crash recovery can replay completion without double-spending HP/mana recovery anchors or extending a training cooldown. Training XP-committed sessions repair the exact missing cooldown before completion; cooldown-committed sessions replay without extension. Unsupported active combat leases block ordinary/training/Adventure starts instead of being ignored, terminal pending leases remain recoverable after wall-clock expiry, and duplicate session-id conflicts no longer masquerade as active-lease recovery.

## Current product direction

- `0.0.x` foundation is closed after `0.0.30`.
- `0.1.x` is stabilization, playtest polish, and careful Phase 2 preparation.
- Phase 2 direction: Social Combat & Interactions, not a group-raid-first roadmap.
- Real PvP, trading, party combat, raids, shops, crafting, guilds, and Mini App UI remain small future slices unless a task explicitly targets one.

## Do not promise as shipped

Do not present the following as current shipped features unless code/task proves it:

- guilds
- real group raids
- broad PvP
- player markets
- crafting economy
- Mini App UI
- paid power
- non-Telegram clients
