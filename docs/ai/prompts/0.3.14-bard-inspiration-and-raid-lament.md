Use $kvestarnia-version-task and $kvestarnia-telegram-qa.

Implement Kvestarnia version 0.3.14 from the attached `kvestarnia-0.3.14-bard-improvements` package.

Hard preflight gate:
1. Before copying, creating, or editing any file, check the current worktree, fetch `origin/main`, and verify live PR #174 state.
2. Verify by content/tree, not ancestry alone, that its 0.3.13 Bard actual-location and current-location cooldown fixes are already present on `origin/main`, and that the release surfaces are at 0.3.13.
3. If absent, stop with a blocker. Do not stack 0.3.14 on PR #174 or its head branch.
4. If present, create a fresh 0.3.14 branch from updated `origin/main`.

Only after the gate passes, bootstrap the archive:
- inspect `repo-files/`;
- copy its files to the matching repository paths before implementation;
- do not overwrite unrelated local edits; stop and report any path conflict;
- read `AGENTS.md`, `docs/ai/context.md`, and the copied docs below.

Task:
`docs/tasks/0.3.14-bard-inspiration-and-raid-lament.md`

Design and QA:
- `docs/design/bard-inspiration-and-raid-lament.md`
- `docs/qa/0.3.14-bard-inspiration-and-raid-lament-qa.md`
- `docs/references/bard-support-patterns.md`

Implementation priorities:
- preserve the current performance, reaction, payout, achievement, and location contracts;
- grant free Inspiration atomically to the frozen initial audience;
- reuse existing performance grades for `+1/+2/+3/+5` accuracy percentage points;
- generalize the shipped `Ситий` hybrid lease lifecycle so both statuses freeze, pulse, and release atomically across every durable combat surface;
- add the replay-safe, action-costing, once-per-session Big Barrel Lament with shared local Bard availability and the exact raid damage order from the task;
- keep remort, legacy JSON, restart, duplicate callback, and two-Bard concurrency paths safe;
- no Prisma schema/migration is expected. If an atomic implementation truly requires one or a broad architecture rewrite, stop and report before making that change.

Use focused repository inspection and a minimal reviewable diff. Do not create a general buff engine. Keep domain logic Telegram-free and player-facing text Ukrainian. Review the exact Bard tip, raid-role flavor note, Lore Board decision, playtesting/dev helper docs, task/docs indexes, compact context, and all release surfaces required by AGENTS.md.

Ship Inspiration as part of every eligible normal Bard performance. Keep Lament available only inside the existing `BIG_BARREL_BROTHER_RAID_ENABLED` Big Barrel surface; do not add a Bard-specific rollout flag. Add/extend a narrow non-production `/dev_*` helper and prove that production cannot register, show, or mutate through it. Document availability and pending manual Telegram QA honestly.

Verification:
- run focused unit/integration/presenter/callback tests first;
- run the existing Big Barrel 13-round simulations;
- run `npm run check` and `git diff --check` against the real `main` base;
- keep `docs/ai/context.md` under 250 logical lines;
- do not claim manual Telegram QA unless a human actually performs it; leave the full manual checklist pending in the draft PR.

Release workflow:
- bump package and lockfile to 0.3.14;
- update CHANGELOG/news with the actual current Kyiv Holocene date and recheck after later-day commits;
- update the task doc with implementation evidence;
- commit and push the branch;
- open a draft PR targeting `main`, verify its real base/head and mergeability, and do not merge or deploy it.

Final output:
- version task
- changed files
- behavior changed
- tests/checks run
- manual QA status
- risks / follow-ups
- draft PR link and completion status

No tutorial.
