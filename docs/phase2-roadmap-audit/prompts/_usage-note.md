# Prompt Usage Note

Use one Codex prompt per fresh Codex thread.

Do not paste the whole archive into Codex. Point Codex at one task doc plus `docs/ai/context.md`, and let AGENTS.md / skills carry reusable rules.

Filenames in this archive intentionally avoid Pull Request numbers. Use feature/problem names for artifacts and prompts; mention PR numbers only inside PR-specific discussion.
