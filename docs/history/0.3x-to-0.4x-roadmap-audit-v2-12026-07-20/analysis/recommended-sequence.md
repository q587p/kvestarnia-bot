# Рекомендована послідовність

## 0.3.15 — виправити поточний PR

Head `e223073a` уже додав CAS/generation-safe render/redaction acknowledgements,
same-life rejoin generation та permanent/transient/`429` transitions.

Залишковий merge gate:

- adaptive idle scheduling із query-count proof;
- async drain/stop до Prisma disconnect;
- callback acknowledgement поза message gate;
- 403 edit/redaction permanent transition і real grammY `HttpError` network retry;
- migration/restore і 2–3-account Telegram QA.

Не додавати сюди restart/remort або roadmap refactor: це інший scope.

## 0.3.16 — final closeout

В одному risk-led release:

- multi-actor restart/remort transactional policy;
- PartyBoss strict repair/orphan leases;
- join/action/timeout/settlement races;
- PartyBoss support parity або disabled rollout decision;
- release-state ledger;
- privacy-safe alpha funnel + feedback/admin decision;
- risk-based Telegram matrix, backup/restore, observation;
- canonical docs sync.

Після merge 0.3.16 production observation може тривати паралельно з підготовкою
docs/design для 0.4, але reward-bearing 0.4.3 не вмикається до прийнятого evidence.

## 0.4.x

| Версія | Результат | Production |
| --- | --- | --- |
| `0.4.0` | generic rewardless 2–3×2–3 proof | ні, default-off/dev |
| `0.4.1` | ability/target/AI/item/repair/settlement hardening | flagged testers only if approved |
| `0.4.2` | guild identity/roles/invites + ordinary party creation | staged flag |
| `0.4.3` | first capped reward-bearing party expedition | staged production |
| `0.4.4` | optional guild weekly goal | staged after expedition data |
| `0.4.5` | Old Altar gold-only MVP + blessing parity gate | staged flag |
| `0.4.6` | one-effect nearby greeting buff | after stacking decision |
| `0.4.7` | one-active-buff Shynok food MVP | first freeze exact catalog/matrix |
| `0.4.8` | curated existing consumable uses | first freeze ids/effects; OOC-only v1 |
| `0.4.9` | replay-safe take-away consumables | after 0.4.8 |
| `0.4.10` | server-owned high-value resale listings | after economy evidence |
| `0.4.11` | bounded neutral Korchmar recycling | after resale + frozen algorithm |
| `0.4.12` | cosmetic guild XP/milestones | only after weekly data |

## Чому ґільдія не перша

Membership table без спільної мети швидко стає декоративним списком. Але й
чекати guild boss не треба: у `0.4.2` ґільдія вже дає назву, герб, ролі й повторне
формування гурту; `0.4.3` дає корисну спільну дію; `0.4.4` — retention loop.

## Cutline для повернення до старого backlog

До `0.4.3` старі thematic drafts не перебивають generic group-combat lifecycle,
guild identity і першу production expedition. Після цього bounded social/economy
catch-up має явну адресу, а не один «later» рядок:

- `0.4.5` — спочатку gold-only Altar; item offerings лишаються follow-up;
- `0.4.6` — greeting лише після вибору одного effect/stacking contract;
- `0.4.7` — food, зʼїдений зараз, один active buff, без coffee state;
- `0.4.8`/`0.4.9` — existing consumable uses, потім purchase shelf;
- `0.4.10`/`0.4.11` — resale stock, потім окремий neutral recycling.

Cosmetic guild progression навмисно стоїть у `0.4.12`: дані weekly loop можуть
накопичуватися, поки виходять ці slices. Rogue/Hunt/collections далі піднімаються
лише evidence-backed.
