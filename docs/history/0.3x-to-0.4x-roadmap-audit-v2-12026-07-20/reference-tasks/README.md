# Legacy task references

These are unchanged copies from audited PR head `e223073a`. They make the audit
archive self-contained but are not extra patch targets.

- Old Altar has separate gold-only, item-offering and optional grove drafts.
- Nearby Greeting still contains several alternative effects; use the `0.4.6`
  task and design-decision prompt before implementation.
- Consumable uses mixes owned-item actions and take-away purchases; the corrected
  roadmap splits them into `0.4.8` and `0.4.9`.
- Resale/recycling is split into `0.4.10` listings and `0.4.11` neutral recycling.

Canonical activated tasks are in `repo-files/docs/tasks/`.
